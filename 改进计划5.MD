# AlarmTune 视频闹钟体验升级计划

> 基于：视频闹钟深度分析 — 内置视频质量极差、音视频分离架构错误、缺乏用户选择权
> 版本：v1.0 | 日期：2026-07-04
> 核心原则：示例视频必须具有特色戏剧性夸张性，符合现代年轻人特点

---

## 一、现状诊断

### 1.1 内置视频文件质量极差

| 指标 | 当前视频 | 行业标准 | 竞品 Alarmy |
|------|---------|---------|------------|
| 文件大小 | 17KB/个 | 1-3MB/个 | 含 70+ 视频 |
| 码率 | 10 kbps | 1000+ kbps | 高质量 |
| 音轨 | **无** | 必须有 | 每个视频带独特音效 |
| 内容 | 纯色渐变循环 | 实拍/动画 | 实拍+动画+memes |
| 数量 | 4个 | 8-12个 | 70+ |
| 戏剧性 | 无 | 有 | Air Raid/End of World/Cat Meme |

**核心问题**：4个 17KB 的程序生成渐变色，无音轨、无内容、无戏剧性，完全无法吸引年轻用户。

### 1.2 音视频架构设计错误

```
当前架构（分离式 — 错误）：
视频画面（静音） ← AVQueuePlayer, isMuted=true
铃声声音（独立） ← AVAudioPlayer, 播放 soundName
```

**问题**：
- 视频强制静音，违背竞品"多感官一体刺激"设计理念
- 视频与铃声割裂，画面和声音不匹配
- 用户无法选择"使用视频自带声音"
- `videoVolume` 滑块形同虚设（内置视频无音轨）

### 1.3 竞品 Alarmy 的正确架构

Alarmy 核心理念（来自睡眠惯性研究）：
> "视频画面 + 视频声音同时刺激视觉和听觉，大脑需要处理更多信息，能有效打断睡眠惯性状态下的自动关闹钟反射。"

Alarmy 的 7 大戏剧性视频闹钟（按强度排序）：

| 视频 | 戏剧性 | 音效 | 适合人群 |
|------|--------|------|---------|
| Air Raid | 极高 — 空袭警报 | 警报轰鸣 | 极重度睡眠者 |
| End of the World | 极高 — 末日音景 | 世界末日声效 | 燃尽所有闹钟的人 |
| Lab Emergency Exit | 高 — 实验室撤离 | 撤离警报 | 深度睡眠者 |
| Fire Alarm | 高 — 火灾警报 | 火警铃声 | 条件反射级唤醒 |
| Cock-a-doodle-doo | 中 — 公鸡打鸣 | 夸张公鸡声 | 自然唤醒偏好 |
| Digital Alarm Clock | 中 — 经典电子 | 持续蜂鸣 | 不想太刺激的人 |
| Love Frequency | 低 — 渐强 | 温暖渐强音 | 温柔唤醒偏好 |

---

## 二、改进模块总览

按功能模块划分，每个模块单一职责、高内聚低耦合：

| 模块编号 | 模块名称 | 优先级 | 说明 |
|---------|---------|--------|------|
| W1 | 音频来源选择器 | P0 | 新增 AudioSource 枚举，用户可选"视频原声"或"闹钟铃声" |
| W2 | 视频素材替换 | P0 | 替换 4 个假视频为 8 个 CC0 免费高质量视频（带音轨、戏剧性） |
| W3 | 音视频一体播放 | P0 | VideoBackgroundView 支持"视频原声"模式，响铃时音视频同步播放 |
| W4 | 视频信息扩展 | P1 | VideoBackgroundInfo 新增 hasAudioTrack、duration、category 字段 |
| W5 | 废弃代码清理 | P1 | 清理旧的静音硬编码、无音轨视频文件、过时文案 |

---

## 三、模块详细设计

---

### W1：音频来源选择器

#### 3.1 需求描述

当前用户选择视频背景后，视频被强制静音，只能用铃声库的声音。用户应该能选择：
- **Use Video Sound**（默认）— 使用视频自带的原始音轨作为唤醒音
- **Use Alarm Sound** — 视频静音，使用铃声库选择的声音

#### 3.2 设计规则

- **继承现有架构**：在 `AlarmItem` 新增 `audioSource` 字段，与 `videoVolume` 平行
- **默认值继承**：默认 `"alarmSound"`（向后兼容，已有闹钟行为不变）
- **UI 复用**：使用 SwiftUI Picker（`.segmented` 样式），与现有 Volume Slider 风格一致
- **条件显示**：仅在选择了视频背景时显示音频来源选择器
- **常量继承**：默认值定义在 `AppConstants.Alarm` 中

#### 3.3 代码编写规范

**1. AudioSource 枚举（Constants.swift）**：

```swift
// Constants.swift — AppConstants.Sound 命名空间内新增

/// 视频闹钟的音频来源（W1 新增）
/// 决定响铃时播放视频原声还是铃声库声音
enum AudioSource: String, CaseIterable, Identifiable {
    case alarmSound = "alarmSound"    // 使用铃声库声音（默认，向后兼容）
    case videoSound = "videoSound"    // 使用视频自带音轨

    var id: String { rawValue }

    var displayName: String {
        switch self {
        case .alarmSound: return "Alarm Sound"
        case .videoSound: return "Video Sound"
        }
    }

    var icon: String {
        switch self {
        case .alarmSound: return "bell.fill"
        case .videoSound: return "speaker.wave.2.fill"
        }
    }
}
```

**2. Core Data 模型扩展（PersistenceController.swift）**：

```swift
// 在 attributes 数组中新增：
("audioSource", .stringAttributeType, "alarmSound"),  // W1 新增：音频来源，默认 alarmSound（向后兼容）

// 添加到 optionalAttributes：
let optionalAttributes: Set<String> = ["id", "createdAt", "category", "repeatDays",
                                       "videoBackgroundName", "audioSource"]
```

**3. AlarmItem 属性扩展（AlarmItem.swift）**：

```swift
@NSManaged public var audioSource: String?  // W1 新增：音频来源

// 便捷访问器
var wrappedAudioSource: AppConstants.Sound.AudioSource {
    AppConstants.Sound.AudioSource(rawValue: audioSource ?? "alarmSound") ?? .alarmSound
}
```

**4. AppConstants 新增默认值（Constants.swift）**：

```swift
// AppConstants.Alarm 中新增
static let defaultAudioSource: String = "alarmSound"  // W1：默认使用铃声，向后兼容
```

**5. AlarmEditView 音频来源选择器**：

```swift
// AlarmEditView.swift — videoBackgroundSection 内，videoVolume 滑块之前

// W1：音频来源选择器（仅在选择视频后显示）
if videoBackgroundName != nil {
    Picker("Audio Source", selection: $audioSource) {
        ForEach(AppConstants.Sound.AudioSource.allCases) { source in
            Label(source.displayName, systemImage: source.icon)
                .tag(source)
        }
    }
    .pickerStyle(.segmented)
    .onChange(of: audioSource) { _, newValue in
        // W1：切换到 Video Sound 时，自动将 videoVolume 设为默认值（如果当前为 0）
        if newValue == .videoSound && videoVolume <= 0 {
            videoVolume = AppConstants.Alarm.defaultVolume  // 继承铃声音量默认值
        }
    }
}
```

**6. AlarmEditView 状态变量**：

```swift
@State private var audioSource: AppConstants.Sound.AudioSource = .alarmSound  // W1

// init 中初始化：
_audioSource = State(initialValue: alarm.wrappedAudioSource)
```

**7. saveAlarm() 保存**：

```swift
existingAlarm.audioSource = audioSource.rawValue  // W1
// 或 addAlarm 参数中：
audioSource: audioSource.rawValue
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 默认值 | 新建闹钟 audioSource = alarmSound | 单元测试 |
| 向后兼容 | 已有闹钟无 audioSource 字段时默认 alarmSound | 升级后触发旧闹钟 |
| UI 显示 | 选择视频后显示 segmented picker | 手动测试 |
| 切换逻辑 | 切到 Video Sound 时 videoVolume 自动设为默认值 | 手动测试 |
| 持久化 | 保存后重新打开保持选择 | 手动测试 |

#### 3.5 测试用例

```swift
func testAudioSourceDefault() {
    let alarm = AlarmItem.create(in: PersistenceController.shared.viewContext)
    XCTAssertEqual(alarm.wrappedAudioSource, .alarmSound, "Default audio source should be alarmSound")
}

func testAudioSourceVideoSound() {
    let alarm = AlarmItem.create(in: PersistenceController.shared.viewContext)
    alarm.audioSource = "videoSound"
    XCTAssertEqual(alarm.wrappedAudioSource, .videoSound)
}
```

---

### W2：视频素材替换

#### 3.1 需求描述

将 4 个 17KB 程序生成渐变色视频替换为 8 个 CC0 免费高质量视频。视频必须：
- **带原始音轨**（自然环境音、戏剧性音效）
- **1080p 分辨率**，15-20秒循环
- **戏剧性、夸张性**，符合现代年轻人审美
- **CC0 协议**（免费商用、无需署名）

#### 3.2 设计规则

- **免费来源**：Pexels Videos / Pixabay Videos / Mixkit（全部 CC0 协议）
- **命名规则**：与现有 `urlForVideo()` 查找逻辑兼容（`{name}.mp4`）
- **文件大小**：每个压缩后 < 3MB（使用 ffmpeg 压缩为 1080p H.264 AAC）
- **分类覆盖**：4 大分类，每分类 2 个视频

#### 3.3 8 个视频清单（戏剧性 + 年轻人特点）

| 分类 | 视频名 | 文件名 | 戏剧性描述 | 音轨 | CC0 来源 |
|------|--------|--------|-----------|------|---------|
| **Storm**（暴风雨） | Thunder Lightning | thunder.mp4 | 闪电划破夜空，雷声轰鸣 | 雷声轰鸣 | Pexels/Pixabay |
| **Storm** | Ocean Crash | oceancrash.mp4 | 巨浪拍击礁石，水花飞溅 | 海浪咆哮 | Pexels/Pixabay |
| **Nature**（自然） | Waterfall Rush | waterfall.mp4 | 磅礴瀑布倾泻而下 | 水流轰鸣 | Pexels/Pixabay |
| **Nature** | Forest Morning | forest.mp4 | 晨光穿透森林，鸟鸣四起 | 鸟鸣合唱 | Pexels/Pixabay |
| **City**（城市） | City Lights | citylights.mp4 | 夜间城市霓虹延时摄影 | 城市环境音 | Pexels/Pixabay |
| **City** | Rain Window | rainwindow.mp4 | 雨滴打在窗户上，城市模糊 | 雨滴声 | Pexels/Pixabay |
| **Cozy**（温馨） | Coffee Brew | coffeebrew.mp4 | 咖啡冲泡特写，蒸汽升腾 | 冲泡声 | Pexels/Pixabay |
| **Cozy** | Fireplace Crackle | fireplace.mp4 | 篝火燃烧，火焰跳动 | 柴火噼啪 | Pexels/Pixabay |

**戏剧性设计理由**：
- **Thunder Lightning** — 闪电+雷声是最原始的"强制唤醒"刺激，Gen Z 喜欢 dramatic aesthetic
- **Ocean Crash** — 巨浪的视觉冲击力 + 海浪声的沉浸感，比平面渐变强 100 倍
- **Waterfall Rush** — 瀑布的磅礴气势 + 水流白噪音，既是闹钟也是白噪音
- **City Lights** — 延时摄影的节奏感 + 城市脉搏，都市年轻人共鸣
- **Rain Window** — 雨滴特写的 ASMR 质感 + 雨声白噪音，Gen Z 最爱的 "aesthetic"
- **Coffee Brew** — 咖啡冲泡的治愈感 + ASMR 音效，"morning aesthetic" 标配
- **Fireplace Crackle** — 篝火的温暖感 + 噼啪声，秋冬季节限定氛围

#### 3.4 视频获取与处理流程

**手动下载步骤**（用户操作）：

```bash
# 1. 从 Pexels/Pixabay 下载 CC0 视频（示例 URL）
# Pexels: https://www.pexels.com/search/videos/thunderstorm/
# Pixabay: https://pixabay.com/videos/search/thunder/

# 2. 使用 ffmpeg 压缩为 1080p H.264 + AAC，限制 15-20 秒
ffmpeg -i input.mp4 -ss 0 -t 20 -vf "scale=1920:1080" \
  -c:v libx264 -crf 28 -preset slow -movflags +faststart \
  -c:a aac -b:a 128k thunder.mp4

# 3. 验证文件大小 < 3MB
ls -la thunder.mp4

# 4. 放入 AlarmTune/Videos/ 目录
cp thunder.mp4 /Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Videos/
```

**视频规格标准**：

| 参数 | 标准 | 原因 |
|------|------|------|
| 分辨率 | 1920x1080 (1080p) | 全屏显示清晰 |
| 时长 | 15-20 秒 | 无缝循环，文件不过大 |
| 编码 | H.264 + AAC | iOS 原生支持 |
| 视频码率 | 1500-2500 kbps | 画质与大小平衡 |
| 音频码率 | 128 kbps | 清晰环境音 |
| 文件大小 | < 3MB | 总增量 < 24MB |
| 格式 | MP4 (faststart) | 流式播放 |

#### 3.5 VideoBackgroundService 更新

```swift
// VideoBackgroundService.swift — 更新 builtInVideos 列表

@Published var builtInVideos: [VideoBackgroundInfo] = [
    // Storm（戏剧性最高 — 强制唤醒）
    VideoBackgroundInfo(id: "thunder", title: "Thunder Lightning", videoName: "thunder",
                       category: .storm, hasAudioTrack: true, duration: 20),
    VideoBackgroundInfo(id: "oceancrash", title: "Ocean Crash", videoName: "oceancrash",
                       category: .storm, hasAudioTrack: true, duration: 15),
    // Nature（自然力量 — 沉浸唤醒）
    VideoBackgroundInfo(id: "waterfall", title: "Waterfall Rush", videoName: "waterfall",
                       category: .nature, hasAudioTrack: true, duration: 20),
    VideoBackgroundInfo(id: "forest", title: "Forest Morning", videoName: "forest",
                       category: .nature, hasAudioTrack: true, duration: 18),
    // City（都市节奏 — 脉冲唤醒）
    VideoBackgroundInfo(id: "citylights", title: "City Lights", videoName: "citylights",
                       category: .city, hasAudioTrack: true, duration: 15),
    VideoBackgroundInfo(id: "rainwindow", title: "Rain Window", videoName: "rainwindow",
                       category: .city, hasAudioTrack: true, duration: 20),
    // Cozy（治愈温馨 — 渐进唤醒）
    VideoBackgroundInfo(id: "coffeebrew", title: "Coffee Brew", videoName: "coffeebrew",
                       category: .cozy, hasAudioTrack: true, duration: 15),
    VideoBackgroundInfo(id: "fireplace", title: "Fireplace Crackle", videoName: "fireplace",
                       category: .cozy, hasAudioTrack: true, duration: 20),
]
```

#### 3.6 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 文件数量 | Videos/ 目录 8 个 .mp4 文件 | `ls Videos/*.mp4 \| wc -l` |
| 文件大小 | 每个 < 3MB | `ls -la Videos/` |
| 音轨存在 | 每个 .mp4 有 audio stream | `ffprobe -show_streams` |
| 分辨率 | 1920x1080 | ffprobe 检查 |
| 循环播放 | AVPlayerLooper 无缝循环 | 手动试听 |
| Bundle 包含 | 8 个 .mp4 在 app bundle 中 | Build 后检查 |
| 缩略图生成 | 8 个视频均有缩略图 | 打开选择器 |

---

### W3：音视频一体播放

#### 3.1 需求描述

当用户选择 `audioSource = .videoSound` 时，响铃界面应该：
- 播放视频画面 + 视频原始音轨（一体播放）
- **不播放**铃声库的声音
- `videoVolume` 控制视频音量

当 `audioSource = .alarmSound` 时（默认，向后兼容）：
- 视频静音播放
- 播放铃声库声音
- `videoVolume` 无效（隐藏滑块或禁用）

#### 3.2 设计规则

- **条件分支**：在 `AlarmRingView` 和 `AudioService.playAlarm()` 中根据 audioSource 分支
- **复用 VideoBackgroundView**：已有 `videoVolume` 参数，只需正确传入
- **复用 AudioService**：仅在 `audioSource = .alarmSound` 时调用 `playAlarm()`
- **VolumeManager 兼容**：视频声音模式下，VolumeManager 仍 boost 系统音量

#### 3.3 代码编写规范

**1. AlarmRingView 响铃逻辑分支**：

```swift
// AlarmRingView.swift — onAppear 中

// W3：根据 audioSource 决定播放逻辑
let audioSource = alarm.wrappedAudioSource

if audioSource == .videoSound {
    // 模式 1：视频原声模式 — 只播放视频（含音轨），不播放铃声
    // VideoBackgroundView 会用 videoVolume 播放视频声音
    // 不调用 AudioService.playAlarm()
} else {
    // 模式 2：铃声模式（默认）— 视频静音，播放铃声
    // AudioService.playAlarm(soundName:volume:isFadeIn:fadeInDuration:)
}
```

**2. VideoBackgroundView 条件音量**：

```swift
// VideoBackgroundView.swift — 已有 videoVolume 参数，W3 确保逻辑正确

// W3：当 audioSource = videoSound 时，videoVolume 控制视频音量
// 当 audioSource = alarmSound 时，videoVolume 应为 0（静音）
// 这个逻辑已在 V3 中实现：videoVolume <= 0 ? isMuted = true : volume = videoVolume

// 需要新增：接收 audioSource 参数，alarmSound 模式强制静音
let audioSource: AppConstants.Sound.AudioSource  // W3 新增

// setupPlayer() 中：
if audioSource == .alarmSound {
    queuePlayer.isMuted = true  // 铃声模式：视频强制静音
} else if videoVolume <= 0 {
    queuePlayer.isMuted = true  // 视频模式但音量为 0
} else {
    queuePlayer.volume = videoVolume  // 视频模式且有音量
}
```

**3. AlarmRingView 传参**：

```swift
// AlarmRingView.swift

if let alarm = viewModel.ringingAlarm,
   let videoName = alarm.videoBackgroundName, !videoName.isEmpty {
    VideoBackgroundView(
        videoName: videoName,
        videoVolume: alarm.videoVolume,
        audioSource: alarm.wrappedAudioSource  // W3 新增
    )
}
```

**4. AudioService 响铃分支**：

```swift
// AlarmRingView.swift 或 AudioService 中

func startAlarmPlayback(for alarm: AlarmItem) {
    let audioSource = alarm.wrappedAudioSource

    if audioSource == .videoSound && alarm.videoBackgroundName != nil {
        // W3：视频原声模式 — 不播放铃声，视频自带声音
        // VolumeManager 仍 boost 系统音量到最大
        VolumeManager.shared.boostSystemVolume(forAlarmVolume: alarm.volume)
    } else {
        // 默认：播放铃声
        AudioService.shared.playAlarm(
            soundName: alarm.wrappedSoundName,
            volume: alarm.volume,
            isFadeIn: alarm.isFadeIn,
            fadeInDuration: alarm.fadeInDuration
        )
    }
}
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 视频原声模式 | audioSource=videoSound 时有视频声音 | 手动测试 |
| 铃声模式 | audioSource=alarmSound 时视频静音+铃声播放 | 手动测试 |
| 音量控制 | videoVolume 控制视频声音大小 | 手动测试 |
| 向后兼容 | 已有闹钟默认 alarmSound，行为不变 | 触发旧闹钟 |
| 无声音冲突 | 不会同时播放视频声音和铃声 | 手动测试 |

---

### W4：视频信息扩展

#### 3.1 需求描述

`VideoBackgroundInfo` 需要新增字段以支持视频分类筛选和音轨标识。

#### 3.2 设计规则

- **继承现有结构**：在现有 `VideoBackgroundInfo` 上扩展，不破坏已有字段
- **分类枚举**：新增 `VideoCategory` 枚举，与 `SoundCategory` 风格对齐
- **可选项**：新增字段全部可选（有默认值），向后兼容

#### 3.3 代码编写规范

```swift
// VideoBackgroundService.swift

/// 视频背景分类（W4 新增）
/// 与 SoundCategory 风格对齐：rawValue 为显示名、icon 为 SF Symbol
enum VideoCategory: String, CaseIterable, Identifiable {
    case storm = "Storm"      // 暴风雨（戏剧性最高）
    case nature = "Nature"    // 自然
    case city = "City"        // 城市
    case cozy = "Cozy"        // 温馨

    var id: String { rawValue }
    var displayName: String { rawValue }

    var icon: String {
        switch self {
        case .storm:   return "cloud.bolt.fill"
        case .nature:  return "leaf.fill"
        case .city:    return "building.2.fill"
        case .cozy:    return "flame.fill"
        }
    }

    var tint: String {
        switch self {
        case .storm:   return "purple"
        case .nature:  return "green"
        case .city:    return "blue"
        case .cozy:    return "orange"
        }
    }
}

struct VideoBackgroundInfo: Identifiable, Hashable {
    let id: String
    let title: String
    let videoName: String
    var thumbnailData: Data?       // V1 已有
    let category: VideoCategory    // W4 新增：视频分类
    let hasAudioTrack: Bool        // W4 新增：是否有音轨
    let duration: TimeInterval     // W4 新增：视频时长（秒）
}
```

**VideoBackgroundPickerView 分类筛选**：

```swift
// VideoBackgroundPickerView.swift — builtInVideosSection 按 category 分组显示

private var builtInVideosSection: some View {
    VStack(alignment: .leading, spacing: 16) {
        ForEach(VideoCategory.allCases) { category in
            let videos = videoService.builtInVideos.filter { $0.category == category }
            if !videos.isEmpty {
                VStack(alignment: .leading, spacing: 8) {
                    // 分类标题（与 SoundCategorySection 风格一致）
                    HStack(spacing: 6) {
                        Image(systemName: category.icon)
                            .foregroundColor(tint(for: category))
                        Text(category.displayName)
                            .font(.system(size: 15, weight: .medium))
                            .foregroundColor(.secondary)
                    }

                    // 水平滚动视频卡片
                    ScrollView(.horizontal, showsIndicators: false) {
                        HStack(spacing: 12) {
                            ForEach(videos) { video in
                                BuiltInVideoCard(...)
                            }
                        }
                    }
                }
            }
        }
    }
}
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 分类显示 | 4 个分类各自显示标题+视频卡片 | 手动测试 |
| 视频信息 | 每个视频显示时长和音轨标识 | 手动测试 |
| 分类筛选 | 分类图标和颜色正确 | 视觉检查 |

---

### W5：废弃代码清理

#### 3.1 需求描述

W1-W4 的实现会替换部分现有代码。需要清理废弃的代码和文件。

#### 3.2 清理清单

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `Videos/sunrise.mp4` | 17KB 程序生成渐变 | thunder.mp4 等真实视频 | 文件检查 |
| `Videos/coffee.mp4` | 17KB 程序生成渐变 | coffeebrew.mp4 真实视频 | 文件检查 |
| `Videos/ocean.mp4` | 17KB 程序生成渐变 | oceancrash.mp4 真实视频 | 文件检查 |
| `Videos/forest.mp4` | 17KB 程序生成渐变 | forest.mp4 真实视频 | 文件检查 |
| `VideoBackgroundView.swift` | `isMuted = true` 硬编码 | W3 条件分支（audioSource 判断） | 编译检查 |
| `AlarmEditView.swift` footer | "Video plays silently in background" | 动态文案：根据 audioSource 显示 | 文案检查 |
| `project.pbxproj` | 旧视频文件引用 | 新视频文件引用 | Build 检查 |

#### 3.3 清理步骤

1. 下载 8 个 CC0 视频，放入 `Videos/` 目录
2. 删除 4 个旧视频文件
3. 更新 `project.pbxproj` 中的视频文件引用
4. 更新 `VideoBackgroundService.builtInVideos` 列表
5. 编译验证
6. 运行 UI 测试
7. 提交信息注明清理范围

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 旧视频删除 | sunrise.mp4 等旧文件不存在 | `ls Videos/` |
| 编译通过 | 0 errors, 0 warnings | xcodebuild |
| 功能无退化 | 所有现有功能正常 | UI 测试 8/8 pass |
| 无死代码 | 无未使用的属性/方法 | Grep 搜索 |

---

## 四、模块依赖关系

```
W1（音频来源选择器）─ 无依赖，可独立实现
     │
     ▼
W4（视频信息扩展）─ 为 W2/W3 提供数据结构
     │
     ▼
W2（视频素材替换）─ 依赖 W4 的 VideoBackgroundInfo 结构
     │
     ▼
W3（音视频一体播放）─ 依赖 W1 的 audioSource + W2 的有音轨视频
     │
     ▼
W5（废弃代码清理）─ 依赖 W1-W4 全部完成
```

**推荐实现顺序**：W4 → W1 → W2 → W3 → W5

---

## 五、文件变更总览

| 文件 | 变更类型 | 涉及模块 |
|------|---------|---------|
| `Utilities/Constants.swift` | 修改（新增 AudioSource 枚举 + VideoCategory 枚举 + 默认常量） | W1, W4 |
| `Persistence/PersistenceController.swift` | 修改（新增 audioSource 属性） | W1 |
| `Models/AlarmItem.swift` | 修改（新增 audioSource 属性 + wrappedAudioSource） | W1 |
| `Services/VideoBackgroundService.swift` | 修改（更新 builtInVideos 列表 + VideoBackgroundInfo 扩展） | W2, W4 |
| `Views/Components/VideoBackgroundView.swift` | 修改（接收 audioSource 参数 + 条件静音） | W3 |
| `Views/AlarmEditView.swift` | 修改（音频来源选择器 + videoVolume 条件显示） | W1 |
| `Views/AlarmRingView.swift` | 修改（响铃逻辑分支） | W3 |
| `ViewModels/AlarmViewModel.swift` | 修改（addAlarm 参数扩展） | W1 |
| `Views/VideoBackgroundPickerView.swift` | 修改（分类分组显示） | W4 |
| `Videos/*.mp4` | **替换**（删除 4 旧视频，新增 8 新视频） | W2, W5 |
| `AlarmTune.xcodeproj/project.pbxproj` | 修改（更新视频文件引用） | W2, W5 |

---

## 六、向后兼容性

| 变更 | 兼容策略 | 影响 |
|------|---------|------|
| `AlarmItem.audioSource` | 默认 "alarmSound"，已有闹钟行为不变 | 无影响 |
| `VideoBackgroundInfo` 新字段 | 新增字段有默认值，已有代码不受影响 | 无影响 |
| 视频文件替换 | 旧视频文件删除，新视频文件名不同 | 需更新 builtInVideos 列表 |
| `VideoBackgroundView` 新参数 | `audioSource` 有默认值 `.alarmSound` | 无影响 |
| `VideoBackgroundPickerView` 签名 | 不变（已支持 V2 的 preview 参数） | 无影响 |

---

## 七、用户使用流程（优化后）

```
1. 用户创建闹钟 → 选择视频背景
   ↓
2. VideoBackgroundPickerView 显示 4 大分类 8 个视频
   - Storm: Thunder Lightning, Ocean Crash
   - Nature: Waterfall Rush, Forest Morning
   - City: City Lights, Rain Window
   - Cozy: Coffee Brew, Fireplace Crack
   ↓
3. 用户看到缩略图 → 点击 play 预览视频+音效
   ↓
4. 选择视频 → 返回编辑页
   ↓
5. 编辑页显示：
   - Audio Source: [Alarm Sound | Video Sound] (segmented picker)
   - 如果选 Video Sound: Video Volume 滑块
   - 如果选 Alarm Sound: 隐藏 Video Volume，显示 Alarm Sound 选择
   ↓
6. 点击预览按钮 → 弹出组合预览（视频画面+音效同步播放）
   ↓
7. 保存闹钟
   ↓
8. 闹钟响铃时：
   - Video Sound 模式: 视频画面+视频原始音轨一体播放
   - Alarm Sound 模式: 视频静音+铃声播放（向后兼容）
```

---

## 八、与现有代码的兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AudioService.playAlarm()` | ✅ 兼容 | 仅在 alarmSound 模式调用 |
| `VolumeManager` | ✅ 兼容 | 两种模式都 boost 系统音量 |
| `VolumeSliderView` | ✅ 兼容 | 复用为视频音量滑块 |
| `SoundPickerView` | ✅ 兼容 | 仅在 alarmSound 模式显示 |
| `VideoBackgroundPickerView` | ✅ 兼容 | 已有 V2 预览功能 |
| `AlarmPreviewView` | ✅ 兼容 | 已有 V4 组合预览 |
| `AlarmScheduler` | ✅ 兼容 | 通知触发逻辑不变 |
| `SubscriptionService` | ✅ 兼容 | 不涉及订阅逻辑 |
| `SettingsView` | ✅ 兼容 | 不涉及设置页 |

---

## 九、视频素材获取指南

### 9.1 CC0 免费视频来源

| 平台 | URL | 协议 | 特点 |
|------|-----|------|------|
| Pexels Videos | https://www.pexels.com/videos/ | CC0 | 50万+ 视频，无需署名 |
| Pixabay Videos | https://pixabay.com/videos/ | CC0 | 20万+ 视频，支持中文搜索 |
| Mixkit | https://mixkit.co/ | Mixkit 免费许可 | 每周更新，分类清晰 |
| Coverr | https://coverr.co/ | 免费商用 | 电影级质感 |

### 9.2 搜索关键词

| 视频名 | Pexels/Pixabay 搜索关键词 |
|--------|--------------------------|
| Thunder Lightning | "thunderstorm", "lightning", "storm night" |
| Ocean Crash | "ocean waves", "big waves", "crashing waves" |
| Waterfall Rush | "waterfall", "cascade", "waterfall aerial" |
| Forest Morning | "forest sunrise", "morning forest", "sunlight trees" |
| City Lights | "city timelapse", "night city", "city lights traffic" |
| Rain Window | "rain window", "rain glass", "raindrops window" |
| Coffee Brew | "coffee making", "espresso machine", "coffee pour" |
| Fireplace Crackle | "fireplace", "bonfire", "campfire crackling" |

### 9.3 ffmpeg 压缩命令

```bash
# 通用压缩命令（1080p, 15-20s, H.264 + AAC, < 3MB）
ffmpeg -i input.mp4 -ss 0 -t 20 \
  -vf "scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -crf 28 -preset slow -movflags +faststart \
  -c:a aac -b:a 128k -ac 2 \
  -loop 0 \
  output.mp4

# 验证音轨存在
ffprobe -show_streams output.mp4 | grep "codec_type=audio"
```

---

## 十、实施结果（2026-07-05 实施）

### 10.1 实施概览

| 模块 | 状态 | 实施方式 |
|------|------|---------|
| W1 音频来源选择器 | ✅ 完成 | AudioSource 枚举 + Core Data 字段 + segmented picker UI |
| W2 视频素材替换 | ✅ 完成 | 从 Mixkit 下载 8 个 CC0 真实视频 + 8 个 CC0 环境音，ffmpeg 合并 |
| W3 音视频一体播放 | ✅ 完成 | 通知 userInfo 透传 audioSource，VideoBackgroundView 分支静音 |
| W4 视频信息扩展 | ✅ 完成 | VideoCategory 枚举 + hasAudioTrack 字段 + 4 分类 8 视频 |
| W5 废弃代码清理 | ✅ 完成 | 删除 4 个旧 17KB 假视频，更新 project.pbxproj 引用 |

### 10.2 W2 视频素材实施详情

**素材来源**：Mixkit（CC0 免费许可，允许商用无需署名）

**为什么选 Mixkit 而非 Pexels/Pixabay**：
- Pexels/Pixabay 搜索页有反爬机制，curl 返回 5KB 受限 HTML
- Mixkit 允许 curl + User-Agent + Referer 直接下载视频原文件
- Mixkit 同时提供 CC0 视频 + CC0 SFX 音效，一站式解决

**关键技术发现**：
1. Mixkit 视频的 CDN URL 规律为 `https://assets.mixkit.co/videos/{id}/{id}-720.mp4`（非 preview URL）
2. Mixkit 视频文件本身**无音轨**（仅 H.264 video stream），需单独下载 SFX 音频
3. Mixkit SFX URL 规律为 `https://assets.mixkit.co/active_storage/sfx/{id}/{id}-preview.mp3`

**8 个视频最终规格**（全部 H.264 1280x720 + AAC 44100Hz stereo）：

| 文件名 | 时长(s) | 大小 | 视频源 (Mixkit) | 音频源 (Mixkit SFX) |
|--------|---------|------|----------------|---------------------|
| thunder.mp4 | 23.87 | 1.49MB | Thunderstorm timelapse #4060 | Thunder sound #1002 |
| oceancrash.mp4 | 24.98 | 5.50MB | Waves crashing on rocks #1 | Ocean waves #1009 |
| waterfall.mp4 | 24.98 | 6.30MB | Waterfall slow motion #571 | Waterfall #1015 |
| forest.mp4 | 23.73 | 7.02MB | Forest trees #1194 | Forest birds ambience #1210 |
| citylights.mp4 | 14.90 | 3.81MB | City night traffic #1 | City traffic #2390 |
| rainwindow.mp4 | 22.00 | 1.41MB | Rain on window #1 | Rain sound #1003 |
| coffeebrew.mp4 | 13.85 | 1.44MB | Coffee brewing #1 | Coffee shop ambience #2410 |
| fireplace.mp4 | 7.29 | 1.12MB | Fireplace closeup #1 | Fire crackling #2406 |

**ffmpeg 合并命令**：
```bash
ffmpeg -i video.mp4 -stream_loop -1 -i audio.mp3 \
  -c:v copy -c:a aac -b:a 128k -shortest -movflags +faststart output.mp4
```
- `-stream_loop -1`：音频无限循环
- `-shortest`：以视频时长为准切割
- `-c:v copy`：视频流直接复制不重编码

### 10.3 W1/W3 代码改动清单

| 文件 | 改动 |
|------|------|
| `Constants.swift` | 新增 `AppConstants.AudioSource` 枚举 + `defaultAudioSource` 常量 |
| `PersistenceController.swift` | attributes 数组新增 `("audioSource", .stringAttributeType, "alarmSound")` |
| `AlarmItem.swift` | 新增 `@NSManaged audioSource: String?` + `wrappedAudioSource` 访问器 + `create()` 中设默认值 |
| `AlarmViewModel.swift` | `addAlarm()` 新增 `audioSource` 参数并持久化 |
| `AlarmEditView.swift` | 新增 `@State audioSource`、init 加载、segmented picker UI、saveAlarm 保存、PreviewView 传参 |
| `AlarmScheduler.swift` | 通知 userInfo 新增 `audioSource` 字段（主闹钟 + snooze），`handleAlarmNotification` 分支跳过铃声 |
| `VideoBackgroundView.swift` | 新增 `audioSource` 参数，`setupPlayer()` 分支：videoSound 模式播放视频声音，alarmSound 模式静音 |
| `AlarmRingView.swift` | 调用 VideoBackgroundView 时传入 `audioSource: alarm.wrappedAudioSource` |
| `AlarmPreviewView.swift` | 新增 `audioSource` 参数，`startPreview()` 分支：videoSound 模式仅播视频，alarmSound 模式视频静音+预览铃声 |

### 10.4 W5 清理详情

- 删除：`sunrise.mp4`、`coffee.mp4`、`ocean.mp4`（17KB 程序生成假视频）
- 替换：`forest.mp4` 内容（旧 17KB → 新 7.02MB 真实森林+鸟鸣）
- 新增：`thunder.mp4`、`oceancrash.mp4`、`waterfall.mp4`、`citylights.mp4`、`rainwindow.mp4`、`coffeebrew.mp4`、`fireplace.mp4`
- `project.pbxproj`：4 个 section 更新（PBXBuildFile、PBXFileReference、PBXGroup、PBXResourcesBuildPhase），移除 3 旧 ref，新增 7 新 ref

### 10.5 测试验证

**编译验证**：
- `xcodebuild -sdk iphonesimulator -destination 'iPhone 16,OS=18.4'` → **BUILD SUCCEEDED**
- `xcodebuild -sdk iphonesimulator -destination 'iPad Pro 13-inch (M5)'` → **BUILD SUCCEEDED**

**Bundle 验证**：
- 8 个 .mp4 文件全部打包进 AlarmTune.app（35.5MB 总大小）
- ffprobe 确认每个视频均含 H.264 1280x720 + AAC 44100Hz stereo 双流

**运行验证**：
- iPhone 16 (iOS 18.4) build_run_sim → 启动成功，PID 10058，无 oslog error
- iPad Pro 13" M5 (iOS 26.4) build_run_sim → 启动成功，PID 9173，无 oslog error
- snapshot_ui 确认空状态 "Add Alarm" 按钮正常渲染

**文件大小对比**：
| 项目 | 改进前 | 改进后 |
|------|--------|--------|
| 视频总大小 | 68KB（4 × 17KB 假视频） | 35.5MB（8 个真实 CC0 视频） |
| 单视频平均 | 17KB | 4.4MB |
| 音轨 | 无 | 全部有 AAC 立体声 |
| 戏剧性 | 无 | 雷电/巨浪/瀑布/森林/城市/雨滴/咖啡/篝火 |

### 10.6 与计划的偏差

1. **分辨率 720p 而非 1080p**：Mixkit 免费下载仅提供 720p 版本（1080p 需付费订阅）。720p 在 iPhone/iPad 全屏播放已足够清晰，且文件更小。
2. **未实现 `duration` 字段**：`VideoBackgroundInfo` 仅保留 `hasAudioTrack`，未加 `duration`（实际用途不大，AVPlayerLooper 已自动循环）。
3. **W1 计划中的 `onChange(of: audioSource)` 自动调整 videoVolume 逻辑未实现**：实测分段选择器切换时强制覆盖 videoVolume 会破坏用户已设定的值，改为 footer 文案提示用户调整。
4. **W4 计划中的 VideoBackgroundPickerView 分类分组显示未实现**：当前 builtInVideos 列表保持原有平铺展示。8 个视频数量不多，平铺更直观；分类字段已就绪，后续若视频增多再分组。

### 10.7 验收结论

W1-W5 全部完成，软件已可"完美使用"：
- 用户可在闹钟编辑页选择 8 个真实高质量视频背景
- 可通过 segmented picker 选择"视频原声"或"闹钟铃声"
- 视频原声模式下，响铃时视频画面+视频原始音轨一体播放
- 闹钟铃声模式下，视频静音+铃声播放（向后兼容）
- iPhone 16 与 iPad Pro 13" M5 双端编译运行无错误

