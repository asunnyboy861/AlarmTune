# AlarmTune 铃声系统改进计划

> 基于客户反馈："铃声选择太少，且质量不佳"
> 版本：v3 | 日期：2026-07-03

---

## 一、现状诊断

| 维度 | 当前状态 | 行业基准 | 差距 |
|------|---------|---------|------|
| 内置铃声数量 | 5 首 | 20-250 首 | 严重不足 |
| 铃声分类 | 无 | 5+ 类别 | 缺失 |
| Apple Music 集成 | 无 | 竞品标配 | 缺失 |
| 自定义导入 | 代码预留路径，无 UI | 部分竞品支持 | 未实现 |
| 铃声质量 | 偏轻柔，缺刺耳/高唤醒力 | 覆盖全唤醒梯度 | 不足 |
| 预览音量 | 硬编码 0.5 | 应继承用户设置 | 不一致 |

---

## 二、改进模块总览

按功能模块划分，每个模块单一职责、高内聚低耦合：

| 模块编号 | 模块名称 | 优先级 | 说明 |
|---------|---------|--------|------|
| M1 | 铃声数据模型 | P0 | 定义铃声元数据（名称、分类、来源类型），为所有铃声功能提供数据基础 |
| M2 | 铃声资源扩充 | P0 | 从 5 首扩充至 30+ 首，覆盖 5 大分类 |
| M3 | 铃声选择器重构 | P0 | 支持分类浏览 + 内置/音乐库/自定义三段式选择 |
| M4 | Apple Music 集成 | P1 | 用户从音乐库选歌作为闹钟铃声 |
| M5 | 自定义铃声导入 | P1 | 用户从 Files App 导入音频文件 |
| M6 | 废弃代码清理 | P0 | 清理被替换的旧代码、空目录、硬编码 |
| M7 | Premium 铃声包 + 订阅 | P2 | 免费 30 首 + Premium 订阅解锁更多声音包，建立变现路径 |
| M8 | 视频/AI 铃声创新 | P3 | 视频闹钟、AI 生成铃声、随机铃声防止疲劳等差异化功能 |

---

## 三、模块详细设计

---

### M1：铃声数据模型

#### 3.1 需求描述

当前铃声系统用 `AppConstants.Sound.builtInSounds: [String]` 纯字符串数组表示铃声，无法承载分类、来源类型、文件名映射等元数据。需要定义结构化的铃声模型，为分类浏览、多来源支持提供统一数据基础。

#### 3.2 设计规则

- 遵循现有 `AppConstants` 嵌套枚举的常量组织模式
- 铃声模型使用 `enum` + 计算属性（参考 `AppTheme`/`AlarmCategory` 的枚举模式）
- 分类枚举使用 `CaseIterable` 以支持遍历（参考 `AppTheme.allCases`）
- 文件结构语义化：模型定义放在 `Utilities/Constants.swift` 的 `AppConstants.Sound` 命名空间内，与现有常量组织一致

#### 3.3 代码编写规范

**铃声分类枚举** — 扩展 `AppConstants.Sound`：

```swift
// 在 Constants.swift 的 AppConstants.Sound 枚举内新增

/// 铃声分类，与 AlarmItem.AlarmCategory 风格对齐：rawValue 为显示名、icon 为 SF Symbol、color 为主题色
enum SoundCategory: String, CaseIterable, Identifiable {
    case loud = "Loud"          // 高唤醒力
    case nature = "Nature"      // 自然
    case gentle = "Gentle"      // 舒缓
    case classic = "Classic"    // 经典
    case fun = "Fun"            // 欢快

    var id: String { rawValue }

    var displayName: String {
        switch self {
        case .loud:    return "Loud"
        case .nature:  return "Nature"
        case .gentle:  return "Gentle"
        case .classic: return "Classic"
        case .fun:     return "Fun"
        }
    }

    var icon: String {
        switch self {
        case .loud:    return "speaker.wave.3.fill"
        case .nature:  return "leaf.fill"
        case .gentle:  return "cloud.sun.fill"
        case .classic: return "bell.fill"
        case .fun:     return "music.note.house.fill"
        }
    }

    /// 分类图标背景色，使用系统语义色，兼容深色模式
    var color: String {
        switch self {
        case .loud:    return "red"
        case .nature:  return "green"
        case .gentle:  return "blue"
        case .classic: return "orange"
        case .fun:     return "purple"
        }
    }
}
```

**铃声来源枚举**：

```swift
/// 铃声来源类型，决定 urlForSound 的查找策略
enum SoundSource: String {
    case builtIn = "builtIn"           // Bundle 内 Sounds/ 目录
    case appleMusic = "appleMusic"     // 用户音乐库（MPMediaItem）
    case imported = "imported"         // Documents/ImportedSounds/ 目录
}
```

**铃声元数据结构** — 替代旧的纯字符串数组：

```swift
/// 单个铃声的元数据描述
struct SoundInfo: Identifiable, Hashable {
    let id: String              // 唯一标识，内置铃声 = 显示名，音乐库 = persistentID，导入 = 文件名
    let displayName: String     // 用户可见名称
    let category: SoundCategory // 所属分类（仅内置铃声有效）
    let source: SoundSource     // 来源类型
    let isPremium: Bool         // 是否为 Premium 专属铃声（M7 订阅变现使用）

    /// 内置铃声的便捷构造
    static func builtIn(name: String, category: SoundCategory, isPremium: Bool = false) -> SoundInfo {
        SoundInfo(id: name, displayName: name, category: category, source: .builtIn, isPremium: isPremium)
    }
}
```

**替换旧的 `builtInSounds` 数组**：

```swift
// 旧代码（将被清理）：
// static let builtInSounds = ["Gentle Morning", "Digital Beep", ...]

// 新代码：
static let builtInSounds: [SoundInfo] = [
    // Loud
    .builtIn(name: "Emergency Siren",  category: .loud),
    .builtIn(name: "Air Horn",         category: .loud),
    .builtIn(name: "Car Alarm",        category: .loud),
    .builtIn(name: "Military Bugle",   category: .loud),
    .builtIn(name: "Fire Alarm",       category: .loud),
    .builtIn(name: "School Bell",      category: .loud),
    // Nature
    .builtIn(name: "Forest Birds",     category: .nature),
    .builtIn(name: "Ocean Waves",      category: .nature),
    .builtIn(name: "Rain Morning",     category: .nature),
    .builtIn(name: "Creek Stream",     category: .nature),
    .builtIn(name: "Wind Chime",       category: .nature),
    .builtIn(name: "Thunder Roll",     category: .nature),
    // Gentle
    .builtIn(name: "Gentle Morning",   category: .gentle),
    .builtIn(name: "Soft Bell",        category: .gentle),
    .builtIn(name: "Piano Lullaby",    category: .gentle),
    .builtIn(name: "Harp Dream",       category: .gentle),
    .builtIn(name: "Zen Bowl",         category: .gentle),
    .builtIn(name: "Sunrise Glow",     category: .gentle),
    // Classic
    .builtIn(name: "Classic Alarm",    category: .classic),
    .builtIn(name: "Digital Beep",     category: .classic),
    .builtIn(name: "Retro Ring",       category: .classic),
    .builtIn(name: "Mechanical Tick",  category: .classic),
    .builtIn(name: "Office Buzz",      category: .classic),
    .builtIn(name: "Old Telephone",    category: .classic),
    // Fun
    .builtIn(name: "Nature Chirp",     category: .fun),
    .builtIn(name: "Disco Wake",       category: .fun),
    .builtIn(name: "Sunny Day",        category: .fun),
    .builtIn(name: "Rhythm Beat",      category: .fun),
    .builtIn(name: "Cheerful Tune",    category: .fun),
    .builtIn(name: "Morning Coffee",   category: .fun),
]

static let defaultSound = "Gentle Morning"  // 保持不变，确保向后兼容
```

> **兼容性说明**：`defaultSound` 保持字符串 `"Gentle Morning"` 不变，因为 Core Data 中 `AlarmItem.soundName` 存储的是字符串。所有已保存的闹钟数据无需迁移。

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 编译通过 | `SoundInfo`/`SoundCategory`/`SoundSource` 可正常使用 | Xcode Build |
| 向后兼容 | 已有 `soundName = "Gentle Morning"` 的闹钟仍能正常播放 | 手动测试：升级后打开已有闹钟 |
| 分类覆盖 | 5 个分类各 6 首，共 30 首 | 单元测试 |
| 枚举遍历 | `SoundCategory.allCases.count == 5` | 单元测试 |

#### 3.5 测试用例

```swift
// AlarmTuneTests.swift
func testSoundCategoryCount() {
    XCTAssertEqual(AppConstants.Sound.SoundCategory.allCases.count, 5)
}

func testBuiltInSoundCount() {
    XCTAssertEqual(AppConstants.Sound.builtInSounds.count, 30)
}

func testBuiltInSoundCategories() {
    let categories = Set(AppConstants.Sound.builtInSounds.map { $0.category })
    XCTAssertEqual(categories.count, 5)
}

func testDefaultSoundExists() {
    let names = AppConstants.Sound.builtInSounds.map { $0.displayName }
    XCTAssertTrue(names.contains(AppConstants.Sound.defaultSound))
}

func testSoundSourceRawValues() {
    XCTAssertEqual(AppConstants.Sound.SoundSource.builtIn.rawValue, "builtIn")
    XCTAssertEqual(AppConstants.Sound.SoundSource.appleMusic.rawValue, "appleMusic")
    XCTAssertEqual(AppConstants.Sound.SoundSource.imported.rawValue, "imported")
}

func testSoundInfoEquality() {
    let a = AppConstants.Sound.SoundInfo.builtIn(name: "Test", category: .classic)
    let b = AppConstants.Sound.SoundInfo.builtIn(name: "Test", category: .classic)
    XCTAssertEqual(a, b)
}

func testBackwardCompatibility() {
    // 已有闹钟的 soundName 仍为纯字符串，必须能在新列表中找到
    let legacySoundName = "Gentle Morning"
    let found = AppConstants.Sound.builtInSounds.contains { $0.displayName == legacySoundName }
    XCTAssertTrue(found, "Legacy sound name must exist in new built-in list")
}
```

---

### M2：铃声资源扩充

#### 3.1 需求描述

将内置铃声从 5 首扩充至 30 首，覆盖 5 大分类（Loud/Nature/Gentle/Classic/Fun），每分类 6 首。所有新增铃声为 `.caf` 格式，存放于现有 `Sounds/` 目录。

#### 3.2 设计规则

- 文件命名规则：`{DisplayName去空格}.caf`，与现有 `urlForSound` 的查找逻辑完全兼容（该逻辑会去除空格后匹配文件名）
- 文件存放路径：`Sounds/` 目录（与现有 5 个 .caf 文件同级），非 `Resources/Sounds/`
- 文件大小控制：每首 < 500KB（.caf 格式，15-30 秒片段），总包增量 < 12MB
- 音频质量：44.1kHz / 16bit，确保 AVAudioPlayer 无缝循环播放

#### 3.3 新增铃声文件清单

| 分类 | 铃声名 | 文件名 | 风格描述 |
|------|--------|--------|---------|
| Loud | Emergency Siren | EmergencySiren.caf | 急促警报，渐强渐弱循环 |
| Loud | Air Horn | AirHorn.caf | 气笛短促爆发 |
| Loud | Car Alarm | CarAlarm.caf | 汽车警报交替高低音 |
| Loud | Military Bugle | MilitaryBugle.caf | 军号悠扬但有力 |
| Loud | Fire Alarm | FireAlarm.caf | 消防警报持续高音 |
| Loud | School Bell | SchoolBell.caf | 电铃持续响铃 |
| Nature | Forest Birds | ForestBirds.caf | 森林鸟鸣合唱 |
| Nature | Ocean Waves | OceanWaves.caf | 海浪拍岸循环 |
| Nature | Rain Morning | RainMorning.caf | 雨声+远处雷声 |
| Nature | Creek Stream | CreekStream.caf | 溪流潺潺 |
| Nature | Wind Chime | WindChime.caf | 风铃清脆 |
| Nature | Thunder Roll | ThunderRoll.caf | 远处雷鸣滚动 |
| Gentle | Gentle Morning | GentleMorning.caf | *(保留已有)* |
| Gentle | Soft Bell | SoftBell.caf | *(保留已有)* |
| Gentle | Piano Lullaby | PianoLullaby.caf | 钢琴轻柔旋律 |
| Gentle | Harp Dream | HarpDream.caf | 竖琴梦幻音 |
| Gentle | Zen Bowl | ZenBowl.caf | 颂钵共鸣 |
| Gentle | Sunrise Glow | SunriseGlow.caf | 合成器温暖渐升 |
| Classic | Classic Alarm | ClassicAlarm.caf | *(保留已有)* |
| Classic | Digital Beep | DigitalBeep.caf | *(保留已有)* |
| Classic | Retro Ring | RetroRing.caf | 老式电话振铃 |
| Classic | Mechanical Tick | MechanicalTick.caf | 机械钟表滴答+响铃 |
| Classic | Office Buzz | OfficeBuzz.caf | 电子蜂鸣器 |
| Classic | Old Telephone | OldTelephone.caf | 旋转拨号电话铃 |
| Fun | Nature Chirp | NatureChirp.caf | *(保留已有)* |
| Fun | Disco Wake | DiscoWake.caf | 迪斯科节拍 |
| Fun | Sunny Day | SunnyDay.caf | 欢快口哨旋律 |
| Fun | Rhythm Beat | RhythmBeat.caf | 节奏鼓点 |
| Fun | Cheerful Tune | CheerfulTune.caf | 欢乐短旋律 |
| Fun | Morning Coffee | MorningCoffee.caf | 咖啡馆爵士 |

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 文件数量 | Sounds/ 目录下共 30 个 .caf 文件 | `ls Sounds/*.caf \| wc -l` |
| 文件大小 | 每首 < 500KB | `ls -la Sounds/` |
| 文件命名 | 与 `SoundInfo.displayName` 去空格一致 | 单元测试 |
| Xcode 项目 | 所有 .caf 在 project.pbxproj 中注册为资源 | Build 产物检查 |
| 循环播放 | 每首铃声 `numberOfLoops = -1` 时无缝循环 | 手动试听 |
| 兼容旧数据 | GentleMorning.caf / SoftBell.caf / ClassicAlarm.caf / DigitalBeep.caf / NatureChirp.caf 保留不变 | 回归测试 |

#### 3.5 测试用例

```swift
func testAllBuiltInSoundsHaveFiles() {
    // 验证每个 SoundInfo 都能通过 urlForSound 找到对应文件
    for sound in AppConstants.Sound.builtInSounds {
        let url = AudioService.shared.urlForSound(sound.displayName)
        XCTAssertNotNil(url, "Sound file missing for: \(sound.displayName)")
    }
}

func testSoundFileNamingConvention() {
    for sound in AppConstants.Sound.builtInSounds {
        let sanitizedName = sound.displayName.replacingOccurrences(of: " ", with: "")
        // 文件名应为 "SanitizedName.caf"
        let expectedFileName = sanitizedName + ".caf"
        XCTAssertTrue(
            Bundle.main.url(forResource: sanitizedName, withExtension: "caf", subdirectory: "Sounds") != nil,
            "Expected file \(expectedFileName) not found in Sounds/"
        )
    }
}

func testNoDuplicateSoundNames() {
    let names = AppConstants.Sound.builtInSounds.map { $0.displayName }
    XCTAssertEqual(names.count, Set(names).count, "Duplicate sound names found")
}
```

---

### M3：铃声选择器重构

#### 3.1 需求描述

当前 `SoundPickerView` 是一个简单的平铺列表，仅展示 5 个铃声名称。需重构为分类浏览模式，支持：
- 内置铃声按分类分组展示
- 音乐库铃声区域（M4 实现时激活）
- 自定义导入铃声区域（M5 实现时激活）
- 预览音量继承用户设置（而非硬编码 0.5）

#### 3.2 设计规则

- **单一职责**：铃声选择器仅负责展示和选择，不负责铃声数据获取（数据由 `SoundRepository` 提供）
- **代码复用**：分类 Section 复用同一 `SoundCategorySection` 子视图，避免 5 个分类写 5 遍
- **风格一致**：沿用现有 `SoundPickerView` 的视觉风格（圆角高亮行、checkmark/play 图标），沿用 `SettingsView` 的 Form + Section 布局
- **iPad 适配**：沿用 `horizontalSizeClass == .regular` 的自适应模式
- **最小代码**：铃声行视图抽为独立 `SoundRow` 组件（因为 M4/M5 的不同来源铃声行也需复用），符合"三次法则"——内置/音乐库/导入三处复用
- **继承设置**：预览音量从闹钟编辑页的 `volume` 绑定继承，而非硬编码

#### 3.3 文件结构

```
Views/
├── SoundPickerView.swift          ← 重构（替换现有文件）
└── Components/
    └── SoundRow.swift              ← 新增，铃声行组件（内置/音乐库/导入三处复用）
```

> **说明**：`SoundRow` 独立为组件，因为 M4（Apple Music 铃声行）和 M5（导入铃声行）都需要复用相同的行布局。

#### 3.4 代码编写规范

**SoundRow.swift** — 可复用的铃声行组件：

```swift
import SwiftUI

/// 单个铃声行，被内置/音乐库/导入三处复用
/// 设计原则：三次法则 — 内置铃声列表、Apple Music 列表、自定义导入列表均使用此组件
struct SoundRow: View {
    let name: String
    let isSelected: Bool
    let onPlay: () -> Void
    let onSelect: () -> Void

    @Environment(\.horizontalSizeClass) private var horizontalSizeClass

    var body: some View {
        Button(action: onSelect) {
            HStack(spacing: isPad ? 14 : 12) {
                Image(systemName: isSelected ? "checkmark.circle.fill" : "circle")
                    .font(.system(size: iconSize))
                    .foregroundColor(isSelected ? .accentColor : .secondary)

                Text(name)
                    .font(.system(size: soundFontSize))
                    .foregroundColor(.primary)

                Spacer()

                Button {
                    onPlay()
                    HapticService.shared.selection()
                } label: {
                    Image(systemName: "play.circle.fill")
                        .font(.system(size: playIconSize))
                        .foregroundColor(.accentColor.opacity(0.7))
                }
                .buttonStyle(.plain)
            }
            .padding(.vertical, isPad ? 14 : 8)
            .padding(.horizontal, isPad ? 16 : 12)
            .background(
                RoundedRectangle(cornerRadius: AppConstants.Layout.largeCardCornerRadius)
                    .fill(isSelected ? Color.accentColor.opacity(0.1) : Color.clear)
            )
            .contentShape(Rectangle())
        }
        .buttonStyle(.plain)
    }

    private var isPad: Bool { horizontalSizeClass == .regular }
    private var iconSize: CGFloat { isPad ? 24 : 20 }
    private var soundFontSize: CGFloat { isPad ? 18 : 16 }
    private var playIconSize: CGFloat { isPad ? 30 : 26 }
}
```

**SoundPickerView.swift** — 重构为分类浏览：

```swift
import SwiftUI

struct SoundPickerView: View {
    @Binding var selectedSound: String
    let previewVolume: Float    // 继承自 AlarmEditView 的 volume 绑定

    @Environment(\.horizontalSizeClass) private var horizontalSizeClass
    @State private var playingSound: String?

    private let categories = AppConstants.Sound.SoundCategory.allCases
    private let builtInSounds = AppConstants.Sound.builtInSounds

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: sectionSpacing) {
                // 内置铃声 — 按分类分组
                ForEach(categories) { category in
                    let soundsInCategory = builtInSounds.filter { $0.category == category }
                    if !soundsInCategory.isEmpty {
                        SoundCategorySection(
                            category: category,
                            sounds: soundsInCategory,
                            selectedSound: selectedSound,
                            playingSound: playingSound,
                            onSelect: { name in
                                selectedSound = name
                                HapticService.shared.selection()
                            },
                            onPlay: { name in
                                playingSound = name
                                AudioService.shared.previewSound(soundName: name, volume: previewVolume)
                            }
                        )
                    }
                }

                // Apple Music 区域（M4 实现时启用，当前为占位）
                // appleMusicSection

                // 自定义导入区域（M5 实现时启用，当前为占位）
                // importedSection
            }
            .padding()
        }
    }

    private var sectionSpacing: CGFloat { horizontalSizeClass == .regular ? 20 : 16 }
}

/// 分类 Section — 每个分类的标题 + 铃声列表
private struct SoundCategorySection: View {
    let category: AppConstants.Sound.SoundCategory
    let sounds: [AppConstants.Sound.SoundInfo]
    let selectedSound: String
    let playingSound: String?
    let onSelect: (String) -> Void
    let onPlay: (String) -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            HStack(spacing: 6) {
                Image(systemName: category.icon)
                    .foregroundColor(colorFromName(category.color))
                Text(category.displayName)
                    .font(.system(size: 15, weight: .medium))
                    .foregroundColor(.secondary)
            }

            ForEach(sounds) { sound in
                SoundRow(
                    name: sound.displayName,
                    isSelected: selectedSound == sound.displayName,
                    onPlay: { onPlay(sound.displayName) },
                    onSelect: { onSelect(sound.displayName) }
                )
            }
        }
    }

    /// 复用 SettingsView 中已有的颜色映射逻辑
    private func colorFromName(_ name: String) -> Color {
        switch name {
        case "red":    return .red
        case "green":  return .green
        case "blue":   return .blue
        case "orange": return .orange
        case "purple": return .purple
        default:       return .secondary
        }
    }
}
```

**AlarmEditView.swift 修改** — 传递音量绑定到 SoundPickerView：

```swift
// 修改 soundSection 中的 sheet 调用
.sheet(isPresented: $showSoundPicker) {
    SoundPickerView(selectedSound: $soundName, previewVolume: volume)  // 传入 volume 而非硬编码
        .presentationDetents([.large])  // 从 .medium 改为 .large，因为铃声多了需要更大空间
        .presentationDragIndicator(.visible)
}
```

#### 3.5 废弃代码清理

| 废弃代码 | 位置 | 清理方式 |
|---------|------|---------|
| `SoundPickerView` 旧的平铺列表 | `SoundPickerView.swift` | 整文件替换为新版 |
| `previewVolume: Float = 0.5` 硬编码 | 旧 `SoundPickerView.swift` | 替换为参数传入 |
| `.presentationDetents([.medium])` | `AlarmEditView.swift` soundSection | 替换为 `.large` |

#### 3.6 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 分类显示 | 5 个分类标题 + 图标正确显示 | 手动测试 |
| 铃声选择 | 点击行更新选中状态，checkmark 同步 | 手动测试 |
| 铃声预览 | 点击播放图标试听，预览音量与滑块一致 | 手动测试 |
| 预览不中断闹钟 | 闹钟响铃时预览播放被拒绝（现有逻辑） | 手动测试 |
| iPad 布局 | 水平常规模式下间距/字号自适应 | 模拟器测试 |
| 向后兼容 | 已保存的 soundName 仍能正确显示选中态 | 回归测试 |

#### 3.7 测试用例

```swift
func testSoundCategoriesAllHaveSounds() {
    for category in AppConstants.Sound.SoundCategory.allCases {
        let sounds = AppConstants.Sound.builtInSounds.filter { $0.category == category }
        XCTAssertFalse(sounds.isEmpty, "Category \(category.displayName) has no sounds")
    }
}

func testEachCategoryHasSixSounds() {
    for category in AppConstants.Sound.SoundCategory.allCases {
        let sounds = AppConstants.Sound.builtInSounds.filter { $0.category == category }
        XCTAssertEqual(sounds.count, 6, "Category \(category.displayName) should have 6 sounds")
    }
}

func testPreviewVolumeInheritedFromAlarm() {
    // AlarmEditView 传递 volume 给 SoundPickerView
    // 验证：设置闹钟音量为 0.8 后，在铃声选择器中预览，音量应为 0.8
    // 此为 UI 测试，在 UITest 中验证
}
```

---

### M4：Apple Music 集成

#### 3.1 需求描述

允许用户从自己的 Apple Music / iTunes 音乐库中选择歌曲作为闹钟铃声。这是 iOS 闹钟 App 的标配功能。

#### 3.2 设计规则

- **单一职责**：音乐库选择逻辑封装在 `MusicLibraryService` 中，View 层仅负责展示
- **代码复用**：音乐库铃声行复用 `SoundRow` 组件
- **风格一致**：音乐库选择使用 `MPMediaPickerController`（系统标准 UI），与 App 风格自然融合
- **数据兼容**：音乐库铃声的 `soundName` 存储格式为 `"appleMusic:{persistentID}"`，与内置铃声的纯名称格式无冲突
- **权限处理**：需在 Info.plist 添加 `NSAppleMusicUsageDescription`，通过 `MPMediaLibrary.requestAuthorization` 请求权限
- **播放逻辑**：音乐库铃声通过 `MPMusicPlayerController.applicationQueuePlayer` 播放，与 `AVAudioPlayer` 播放内置铃声并行在 `AudioService` 中处理

#### 3.3 文件结构

```
Services/
├── AudioService.swift              ← 修改（增加音乐库播放分支）
├── MusicLibraryService.swift       ← 新增，封装 MPMediaLibrary 交互
Utilities/
├── Constants.swift                 ← 修改（SoundSource 枚举已在 M1 定义）
Views/
├── SoundPickerView.swift           ← 修改（增加 Apple Music Section）
Info.plist                          ← 修改（添加 NSAppleMusicUsageDescription）
```

#### 3.4 代码编写规范

**MusicLibraryService.swift** — 音乐库服务：

```swift
import Foundation
import MediaPlayer

/// 封装 Apple Music / iTunes 音乐库交互
/// 单例模式，与 AudioService/VolumeManager 等保持一致
final class MusicLibraryService: ObservableObject {
    static let shared = MusicLibraryService()

    @Published var authorized: Bool = false
    @Published var selectedItems: [MPMediaItem] = []   // 用户选择过的音乐库歌曲

    private init() {
        checkAuthorization()
    }

    func checkAuthorization() {
        let status = MPMediaLibrary.authorizationStatus()
        authorized = (status == .authorized)
    }

    func requestAuthorization(completion: @escaping (Bool) -> Void) {
        MPMediaLibrary.requestAuthorization { status in
            DispatchQueue.main.async {
                self.authorized = (status == .authorized)
                completion(self.authorized)
            }
        }
    }

    /// 保存选中的音乐库歌曲信息
    func saveSelectedSong(_ item: MMediaItem) -> String {
        // 格式："appleMusic:{persistentID}"，存入 AlarmItem.soundName
        let identifier = "appleMusic:\(item.persistentID)"
        // 缓存歌曲元数据到 UserDefaults（名称、艺术家），避免每次重新查询
        cacheSongMetadata(item)
        return identifier
    }

    /// 从 persistentID 查询歌曲信息
    func song(for persistentID: UInt64) -> MPMediaItem? {
        let query = MPMediaQuery.songs()
        query.addFilterPredicate(MPMediaPropertyPredicate(
            value: NSNumber(value: persistentID),
            forProperty: MPMediaItemPropertyPersistentID
        ))
        return query.items?.first
    }

    /// 获取缓存的显示名（无需查询音乐库）
    func displayName(for identifier: String) -> String? {
        guard identifier.hasPrefix("appleMusic:") else { return nil }
        let cache = UserDefaults.standard.dictionary(forKey: "appleMusicCache") as? [String: String] ?? [:]
        return cache[identifier]
    }

    private func cacheSongMetadata(_ item: MPMediaItem) {
        let key = "appleMusic:\(item.persistentID)"
        var cache = UserDefaults.standard.dictionary(forKey: "appleMusicCache") as? [String: String] ?? [:]
        cache[key] = "\(item.title ?? "Unknown") - \(item.artist ?? "")"
        UserDefaults.standard.set(cache, forKey: "appleMusicCache")
    }
}
```

**AudioService.swift 修改** — 增加 `source` 参数区分播放策略：

```swift
// playAlarm 方法签名修改：
func playAlarm(soundName: String, volume: Float, source: SoundSource = .builtIn, fadeIn: Bool = false, fadeInDuration: Double = 5.0) {
    stopAlarm()
    beginBackgroundTask()
    VolumeManager.shared.boostSystemVolume(forAlarmVolume: volume)
    guard configureAudioSession() else { ... }

    switch source {
    case .builtIn, .imported:
        playLocalSound(name: soundName, volume: volume, fadeIn: fadeIn, fadeInDuration: fadeInDuration)
    case .appleMusic:
        playAppleMusicSound(identifier: soundName, volume: volume, fadeIn: fadeIn, fadeInDuration: fadeInDuration)
    }
}

/// 播放本地文件（内置 + 导入），复用现有 AVAudioPlayer 逻辑
private func playLocalSound(name: String, volume: Float, fadeIn: Bool, fadeInDuration: Double) {
    guard let soundURL = urlForSound(name) else {
        print("Sound file not found: \(name)")
        VolumeManager.shared.restoreSystemVolume()
        endBackgroundTask()
        return
    }
    // ... 现有 AVAudioPlayer 逻辑不变 ...
}

/// 播放 Apple Music 歌曲
private func playAppleMusicSound(identifier: String, volume: Float, fadeIn: Bool, fadeInDuration: Double) {
    guard identifier.hasPrefix("appleMusic:"),
          let persistentIDStr = identifier.split(separator: ":").last,
          let persistentID = UInt64(persistentIDStr) else {
        print("Invalid Apple Music identifier: \(identifier)")
        return
    }

    let player = MPMusicPlayerController.applicationQueuePlayer
    // 设置播放队列
    let descriptor = MPMusicPlayerStoreDescriptorQueueDescriptor(storeIDs: [String(persistentID)])
    player.setQueue(with: descriptor)
    player.volume = volume  // 注意：MPMusicPlayerController 的音量控制与 AVAudioPlayer 不同

    if fadeIn {
        player.volume = AppConstants.Volume.fadeInMinStartVolume
        player.play()
        startFadeIn(targetVolume: volume, duration: fadeInDuration, startVolume: AppConstants.Volume.fadeInMinStartVolume)
    } else {
        player.play()
    }

    DispatchQueue.main.async {
        self.isPlaying = true
        self.currentVolume = volume
    }
}
```

**SoundPickerView.swift 增加 Apple Music Section**：

```swift
// 在 body 的 VStack 中，内置铃声分类之后添加：

if MusicLibraryService.shared.authorized {
    appleMusicSection
} else {
    appleMusicAuthSection
}

// Apple Music Section
private var appleMusicSection: some View {
    VStack(alignment: .leading, spacing: 8) {
        HStack(spacing: 6) {
            Image(systemName: "music.note")
                .foregroundColor(.pink)
            Text("Apple Music")
                .font(.system(size: 15, weight: .medium))
                .foregroundColor(.secondary)
        }

        // 已选择过的音乐库歌曲
        ForEach(cachedAppleMusicSongs, id: \.key) { key, name in
            SoundRow(
                name: name,
                isSelected: selectedSound == key,
                onPlay: { /* 预览播放 */ },
                onSelect: { selectedSound = key }
            )
        }

        // "Choose from Music Library" 按钮
        Button {
            showMusicPicker = true
        } label: {
            HStack {
                Image(systemName: "plus.circle.fill")
                Text("Choose from Music Library")
            }
            .font(.system(size: 15))
            .foregroundColor(.accentColor)
        }
    }
    .sheet(isPresented: $showMusicPicker) {
        MusicPickerWrapper(selectedSound: $selectedSound)
    }
}
```

**Info.plist 添加**：

```xml
<key>NSAppleMusicUsageDescription</key>
<string>AlarmTune needs access to your music library to let you set songs as alarm sounds.</string>
```

#### 3.5 废弃代码清理

| 废弃代码 | 位置 | 清理方式 |
|---------|------|---------|
| `playAlarm` 旧的单一播放路径 | `AudioService.swift` | 重构为 `source` 参数分发 |
| `previewSound` 缺少 `source` 参数 | `AudioService.swift` | 增加 `source` 参数，默认 `.builtIn` |

#### 3.6 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 权限请求 | 首次点击 Music 区域弹出系统授权弹窗 | 手动测试 |
| 歌曲选择 | MPMediaPickerController 正常弹出并可选择歌曲 | 手动测试 |
| 铃声播放 | 选择的歌曲能正常播放为闹钟 | 手动测试 |
| 数据持久化 | soundName 存为 `"appleMusic:{ID}"` 格式 | Core Data 检查 |
| 显示名缓存 | 退出重进后仍能显示歌曲名 | 手动测试 |
| 内置铃声不受影响 | source 默认 `.builtIn`，已有闹钟正常 | 回归测试 |

#### 3.7 测试用例

```swift
func testAppleMusicIdentifierFormat() {
    let identifier = "appleMusic:1234567890"
    XCTAssertTrue(identifier.hasPrefix("appleMusic:"))
}

func testAppleMusicDisplayNameCache() {
    let service = MusicLibraryService.shared
    // 存入缓存
    let cache = ["appleMusic:1234567890": "Test Song - Artist"]
    UserDefaults.standard.set(cache, forKey: "appleMusicCache")
    // 读取
    let name = service.displayName(for: "appleMusic:1234567890")
    XCTAssertEqual(name, "Test Song - Artist")
    // 清理
    UserDefaults.standard.removeObject(forKey: "appleMusicCache")
}

func testSoundSourceDefaultIsBuiltIn() {
    // AlarmItem.soundName 如果不含 "appleMusic:" 前缀，应识别为 builtIn
    let name = "Gentle Morning"
    XCTAssertFalse(name.hasPrefix("appleMusic:"))
}
```

---

### M5：自定义铃声导入

#### 3.1 需求描述

允许用户从 Files App 导入 MP3/AAC 音频文件作为闹钟铃声。代码中 `AudioService.urlForSound()` 已预留 `Documents/ImportedSounds/` 路径查找逻辑，只需补齐导入 UI 和文件管理。

#### 3.2 设计规则

- **复用现有逻辑**：`urlForSound` 已有 `ImportedSounds` 路径查找，无需修改
- **单一职责**：文件导入逻辑封装在 `SoundImportService` 中
- **代码复用**：导入铃声行复用 `SoundRow` 组件
- **数据兼容**：导入铃声的 `soundName` 存储格式为 `"imported:{fileNameNoExt}"`
- **风格一致**：导入按钮风格与 Apple Music 的 "Choose from Music Library" 按钮一致
- **防绕过设计**：免费用户自定义导入上限为 1 个，防止用户通过批量导入网络资源绕过 Premium 声音包
- **边界兼容**：已导入的铃声在升级/降级后均保留可播放，删除后若免费配额已满则无法再次导入

#### 3.3 文件结构

```
Services/
├── SoundImportService.swift        ← 新增，封装文件导入/管理
Views/
├── SoundPickerView.swift           ← 修改（增加导入 Section）
```

#### 3.4 代码编写规范

**SoundImportService.swift** — 声音导入服务：

```swift
import Foundation
import UniformTypeIdentifiers

/// 封装自定义铃声的导入/列表/删除
/// 单例模式，与项目其他 Service 保持一致
final class SoundImportService: ObservableObject {
    static let shared = SoundImportService()

    /// 免费用户自定义导入数量上限，防止绕过 Premium 声音包
    static let freeImportLimit: Int = 1

    @Published var importedSounds: [ImportedSoundInfo] = []

    struct ImportedSoundInfo: Identifiable, Hashable {
        let id: String           // "imported:{fileNameNoExt}"
        let displayName: String  // 文件名（去扩展名）
        let fileName: String     // 完整文件名（含扩展名）
        let fileSize: Int64      // 文件大小
    }

    private let importedDir: URL

    private init() {
        let docs = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!
        importedDir = docs.appendingPathComponent("ImportedSounds", isDirectory: true)
        createDirectoryIfNeeded()
        refreshImportedSounds()
    }

    /// 当前用户是否还可以继续导入
    /// M5 阶段：所有用户统一按免费配额限制（1 个）
    /// M7 阶段：扩展为 `SubscriptionService.shared.isPremium || importedSounds.count < Self.freeImportLimit`
    var canImportMore: Bool {
        importedSounds.count < Self.freeImportLimit
    }

    /// 距离免费配额上限还差几个
    var remainingFreeImports: Int {
        max(0, Self.freeImportLimit - importedSounds.count)
    }

    /// 确保 ImportedSounds 目录存在
    private func createDirectoryIfNeeded() {
        if !FileManager.default.fileExists(atPath: importedDir.path) {
            try? FileManager.default.createDirectory(at: importedDir, withIntermediateDirectories: true)
        }
    }

    /// 刷新导入铃声列表
    func refreshImportedSounds() {
        let allowedExtensions = Set(["mp3", "m4a", "wav", "aiff", "caf"])
        let files = (try? FileManager.default.contentsOfDirectory(at: importedDir, includingPropertiesForKeys: [.fileSizeKey])) ?? []

        importedSounds = files.compactMap { url in
            let ext = url.pathExtension.lowercased()
            guard allowedExtensions.contains(ext) else { return nil }
            let name = url.deletingPathExtension().lastPathComponent
            let size = (try? url.resourceValues(forKeys: [.fileSizeKey]))?.fileSize ?? 0
            return ImportedSoundInfo(
                id: "imported:\(name)",
                displayName: name,
                fileName: url.lastPathComponent,
                fileSize: Int64(size)
            )
        }
    }

    /// 从 URL 导入文件（DocumentPicker 回调）
    /// - Returns: true 导入成功；false 失败或超出配额
    func importFile(from url: URL) -> Bool {
        // 配额检查：免费用户达到上限后禁止继续导入
        guard canImportMore else { return false }

        let fileName = url.lastPathComponent
        let destURL = importedDir.appendingPathComponent(fileName)

        // 防止覆盖
        if FileManager.default.fileExists(atPath: destURL.path) {
            return false
        }

        // 安全访问
        guard url.startAccessingSecurityScopedResource() else { return false }
        defer { url.stopAccessingSecurityScopedResource() }

        do {
            try FileManager.default.copyItem(at: url, to: destURL)
            refreshImportedSounds()
            return true
        } catch {
            print("Import failed: \(error.localizedDescription)")
            return false
        }
    }

    /// 删除导入的铃声
    func deleteSound(_ info: ImportedSoundInfo) -> Bool {
        let fileURL = importedDir.appendingPathComponent(info.fileName)
        do {
            try FileManager.default.removeItem(at: fileURL)
            refreshImportedSounds()
            return true
        } catch {
            print("Delete failed: \(error.localizedDescription)")
            return false
        }
    }
}
```

**SoundPickerView.swift 增加导入 Section**：

```swift
// 在 Apple Music Section 之后添加：

importedSection

// Imported Section
private var importedSection: some View {
    VStack(alignment: .leading, spacing: 8) {
        HStack(spacing: 6) {
            Image(systemName: "doc.badge.plus")
                .foregroundColor(.orange)
            Text("Imported Sounds")
                .font(.system(size: 15, weight: .medium))
                .foregroundColor(.secondary)
        }

        ForEach(SoundImportService.shared.importedSounds) { sound in
            SoundRow(
                name: sound.displayName,
                isSelected: selectedSound == sound.id,
                onPlay: {
                    AudioService.shared.previewSound(
                        soundName: sound.displayName,  // urlForSound 会查找 ImportedSounds 目录
                        volume: previewVolume
                    )
                },
                onSelect: { selectedSound = sound.id }
            )
            .swipeActions(edge: .trailing, allowsFullSwipe: true) {
                Button(role: .destructive) {
                    _ = SoundImportService.shared.deleteSound(sound)
                } label: {
                    Label("Delete", systemImage: "trash")
                }
            }
        }

        if SoundImportService.shared.canImportMore {
            Button {
                showDocumentPicker = true
            } label: {
                HStack {
                    Image(systemName: "plus.circle.fill")
                    Text("Import from Files")
                    Text("(\(SoundImportService.shared.remainingFreeImports) left)")
                        .font(.caption)
                        .foregroundColor(.secondary)
                }
                .font(.system(size: 15))
                .foregroundColor(.accentColor)
            }
        } else {
            // M5 阶段：仅显示配额已达上限的禁用提示
            // M7 阶段：将其替换为打开 Paywall 的升级按钮，并移除 `remainingFreeImports` 显示
            HStack {
                Image(systemName: "lock.fill")
                    .foregroundColor(.yellow)
                Text("Free import limit reached")
                    .font(.system(size: 15))
                    .foregroundColor(.secondary)
            }
        }
    }
}
```

#### 3.5 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 文件导入 | 通过 DocumentPicker 选择 MP3 后文件出现在 ImportedSounds 目录 | 手动测试 |
| 列表刷新 | 导入后铃声立即可见 | 手动测试 |
| 播放预览 | 导入铃声可正常预览播放 | 手动测试 |
| 闹钟播放 | 设置为闹钟后正常响铃 | 手动测试 |
| 删除功能 | 滑动删除后文件从磁盘移除 | 手动测试 |
| 格式限制 | 仅允许音频格式（mp3/m4a/wav/aiff/caf） | 测试导入 .txt 文件应被拒绝 |
| 免费导入配额 | 免费用户最多导入 1 个；达到上限后按钮变为升级提示 | 手动测试 |
| Premium 无限导入 | Premium 用户导入无数量限制 | 手动测试 |
| 配额绕过防护 | `importFile` 在服务端（本地）二次校验配额 | 单元测试 |

#### 3.6 测试用例

```swift
func testImportedSoundIdentifierFormat() {
    let id = "imported:MySong"
    XCTAssertTrue(id.hasPrefix("imported:"))
}

func testSoundImportServiceRefresh() {
    let service = SoundImportService.shared
    service.refreshImportedSounds()
    // 验证：如果 ImportedSounds 目录为空，列表为空
    XCTAssertTrue(service.importedSounds.isEmpty || service.importedSounds.count >= 0)
}

func testUrlForSoundFindsImportedFiles() {
    // AudioService.urlForSound 已有 ImportedSounds 路径查找
    // 验证：在 ImportedSounds 放入测试文件后能找到
    // 此为集成测试，需在真机/模拟器上运行
}

func testFreeImportLimit() {
    // 模拟免费用户已导入 1 个文件
    let service = SoundImportService.shared
    // 注意：实际测试中需配合 SubscriptionService.isPremium = false
    // 且 importedSounds.count >= 1 时 canImportMore 应为 false
    XCTAssertEqual(SoundImportService.freeImportLimit, 1)
}

func testImportLimitEnforced() {
    let service = SoundImportService.shared
    // 前置条件：测试中需先向 ImportedSounds 目录写入 1 个测试音频文件
    // 当 importedSounds.count >= SoundImportService.freeImportLimit 时：
    XCTAssertFalse(service.canImportMore, "Free user should not be able to import more than \(SoundImportService.freeImportLimit) sounds")
}
```

---

### M6：废弃代码清理

#### 3.1 需求描述

在 M1-M5 实现过程中，会有旧代码被替换。需要系统性地清理所有废弃代码，保持代码整洁。

#### 3.2 清理清单

| 序号 | 废弃代码 | 所在文件 | 清理条件 | 清理方式 |
|------|---------|---------|---------|---------|
| 1 | `builtInSounds: [String]` 旧数组 | `Constants.swift` | M1 完成后 | 替换为 `[SoundInfo]`，删除旧定义 |
| 2 | `SoundPickerView` 旧的平铺实现 | `SoundPickerView.swift` | M3 完成后 | 整文件替换为新版 |
| 3 | `previewVolume: Float = 0.5` 硬编码 | 旧 `SoundPickerView.swift` | M3 完成后 | 替换为参数传入 |
| 4 | `.presentationDetents([.medium])` | `AlarmEditView.swift` | M3 完成后 | 改为 `.large` |
| 5 | `Resources/Sounds/` 空目录 | 项目目录 | M2 完成后 | 删除空目录 |
| 6 | `playAlarm` 旧签名 | `AudioService.swift` | M4 完成后 | 替换为带 `source` 参数的新签名 |
| 7 | `previewSound` 旧签名 | `AudioService.swift` | M4 完成后 | 替换为带 `source` 参数的新签名 |

#### 3.3 清理流程

```
标记废弃 → 全局搜索引用 → 确认无遗漏 → 删除旧代码 → 编译验证 → 提交（注明清理范围）
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 编译通过 | 清理后项目零 error 零 warning | Xcode Build |
| 全局搜索 | 旧代码的所有引用已更新 | Find in Project |
| 功能回归 | 所有铃声功能正常 | 手动测试 |
| Git 提交信息 | 每次清理提交注明 "chore: 清理 {模块} 废弃代码" | Git log |

---

### M7：Premium 铃声包 + 订阅

#### 3.1 需求描述

在 M1-M5 完成基础铃声体验后，通过 Freemium 模式建立变现路径。免费用户可使用 30 首基础铃声 + 1 个自定义导入名额；Premium 订阅用户解锁：

1. 90+ 首高级声音包（专业录制、ASMR、睡眠故事、白噪音等）
2. 无限自定义音频导入（免费用户限 1 个，防止绕过 Premium 声音包）

这是 iOS 闹钟 App 行业标准的商业模式。

#### 3.2 设计规则

- **单一职责**：订阅状态管理封装在 `SubscriptionService` 中；铃声是否 Premium 锁定由 `SoundInfo` 的 `isPremium` 字段决定；UI 锁定状态由 `PremiumLockView` 统一展示
- **代码复用**：Premium 提示视图复用 `SoundRow` 的样式，仅添加锁定图标和蒙层
- **风格一致**：订阅页使用与 `SettingsView` 相同的 Form + Section 布局，价格卡片沿用圆角卡片风格
- **最小代码**：付费判断逻辑集中在 `SubscriptionService` 和 `SoundInfo.isPremium` 两处，避免在 View 中硬编码 "哪些铃声收费"
- **继承现有**：复用 `AppConstants` 组织价格常量；复用 `ThemeManager` 的主题色

#### 3.3 文件结构

```
Services/
├── SubscriptionService.swift       ← 新增，封装 StoreKit/IAP 交互
Models/
├── SubscriptionProduct.swift       ← 新增，定义订阅商品元数据
Views/
├── Components/
│   └── PremiumLockView.swift       ← 新增，Premium 锁定提示组件
├── PaywallView.swift               ← 新增，订阅购买页面
Utilities/
├── Constants.swift                 ← 修改（增加 SoundInfo.isPremium 字段 + Subscription 价格常量）
```

#### 3.4 代码编写规范

**复用 M1 的 SoundInfo 模型**：

M1 已定义 `SoundInfo.isPremium: Bool` 字段（默认 `false`）。M7 直接复用该模型：

- `builtInSounds` 中的 30 首全部免费（`isPremium = false`），确保用户升级前已有充足选择
- Premium 专属铃声放在独立的 `premiumSounds` 数组中（`isPremium = true`），与免费铃声逻辑隔离

**Premium 声音包定义示例**：

```swift
// 在 Constants.swift 的 AppConstants.Sound 命名空间内
// 示例：完整列表按「Premium 声音包规划」表格执行，共约 90 首
static let premiumSounds: [SoundInfo] = [
    // Loud Pro（20 首，示例 2 首）
    .builtIn(name: "Air Raid",      category: .loud,    isPremium: true),
    .builtIn(name: "Submarine",     category: .loud,    isPremium: true),
    // Nature HD（20 首，示例 2 首）
    .builtIn(name: "Amazon Rain",   category: .nature,  isPremium: true),
    .builtIn(name: "Whale Song",    category: .nature,  isPremium: true),
    // Sleep & Wake（20 首，示例 2 首）
    .builtIn(name: "Soft ASMR",     category: .gentle,  isPremium: true),
    .builtIn(name: "Sleep Story",   category: .gentle,  isPremium: true),
    // White Noise（15 首，示例 2 首）
    .builtIn(name: "Pink Noise",    category: .nature,  isPremium: true),
    .builtIn(name: "Brown Noise",   category: .nature,  isPremium: true),
    // Funny Voices（15 首，示例 2 首）
    .builtIn(name: "Robot Wake",    category: .fun,     isPremium: true),
    .builtIn(name: "Pirate Alarm",  category: .fun,     isPremium: true),
]

/// 合并后的所有铃声（免费 + Premium），用于选择器展示
static var allSounds: [SoundInfo] { builtInSounds + premiumSounds }
```

**订阅商品模型**：

```swift
// Models/SubscriptionProduct.swift
import Foundation

struct SubscriptionProduct: Identifiable {
    let id: String
    let title: String
    let subtitle: String
    let price: String
    let period: String        // "month" / "year"
    let isBestValue: Bool
    let productId: String

    static let all: [SubscriptionProduct] = [
        SubscriptionProduct(
            id: "monthly",
            title: "Monthly",
            subtitle: "Full access, cancel anytime",
            price: "$2.99",
            period: "month",
            isBestValue: false,
            productId: "com.zzoutuo.AlarmTune.premium.monthly"
        ),
        SubscriptionProduct(
            id: "yearly",
            title: "Yearly",
            subtitle: "Save 50%",
            price: "$14.99",
            period: "year",
            isBestValue: true,
            productId: "com.zzoutuo.AlarmTune.premium.yearly"
        )
    ]
}
```

**SubscriptionService**：

```swift
// Services/SubscriptionService.swift
import Foundation
import StoreKit

/// 管理 IAP 订阅状态
/// 单例模式，与 MusicLibraryService 等风格一致
final class SubscriptionService: ObservableObject {
    static let shared = SubscriptionService()

    @Published var isPremium: Bool = false
    @Published var products: [Product] = []
    @Published var isLoading: Bool = false

    private init() {
        Task { await loadProducts() }
    }

    func loadProducts() async {
        // iOS 15+ StoreKit 2 API
        do {
            let ids = Set(SubscriptionProduct.all.map { $0.productId })
            products = try await Product.products(for: ids)
        } catch {
            print("Failed to load products: \(error.localizedDescription)")
        }
    }

    func purchase(_ product: Product) async -> Bool {
        isLoading = true
        defer { isLoading = false }
        do {
            let result = try await product.purchase()
            switch result {
            case .success(let verification):
                let transaction = try checkVerified(verification)
                await transaction.finish()
                isPremium = true
                return true
            case .userCancelled, .pending:
                return false
            @unknown default:
                return false
            }
        } catch {
            print("Purchase failed: \(error.localizedDescription)")
            return false
        }
    }

    func restorePurchases() async {
        do {
            for await result in Transaction.currentEntitlements {
                let transaction = try checkVerified(result)
                if transaction.productType == .autoRenewable {
                    isPremium = true
                    return
                }
            }
            isPremium = false
        } catch {
            print("Restore failed: \(error.localizedDescription)")
        }
    }

    private func checkVerified<T>(_ result: VerificationResult<T>) throws -> T {
        switch result {
        case .unverified:
            throw StoreError.failedVerification
        case .verified(let safe):
            return safe
        }
    }

    enum StoreError: Error {
        case failedVerification
    }
}
```

**PremiumLockView 组件**：

```swift
// Views/Components/PremiumLockView.swift
import SwiftUI

/// 当用户点击 Premium 铃声时展示的锁定提示
struct PremiumLockView: View {
    let product: SubscriptionProduct?
    let onUpgrade: () -> Void

    var body: some View {
        VStack(spacing: 20) {
            Image(systemName: "crown.fill")
                .font(.system(size: 56))
                .foregroundColor(.yellow)

            Text("Unlock Premium Sounds")
                .font(.title2.weight(.bold))

            Text("Get 100+ exclusive alarm sounds, ASMR, sleep stories and white noise.")
                .font(.subheadline)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)

            Button(action: onUpgrade) {
                Text("Upgrade to Premium")
                    .font(.headline)
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(Color.accentColor)
                    .foregroundColor(.white)
                    .cornerRadius(AppConstants.Layout.cardCornerRadius)
            }
        }
        .padding(AppConstants.Layout.largeCardPadding)
    }
}
```

**SoundPickerView 集成 Premium 锁定**：

```swift
// 在 SoundCategorySection 的 ForEach 中：
ForEach(sounds) { sound in
    if sound.isPremium && !SubscriptionService.shared.isPremium {
        // Premium 锁定行：显示锁图标，点击弹出 Paywall
        SoundRow(
            name: sound.displayName,
            isSelected: false,
            onPlay: { /* 未订阅不可预览 */ },
            onSelect: { showPaywall = true }
        )
        .overlay(
            HStack {
                Spacer()
                Image(systemName: "lock.fill")
                    .foregroundColor(.yellow)
                    .padding(.trailing, 12)
            }
        )
    } else {
        // 普通行：与 M3 一致
        SoundRow(...)
    }
}
```

**扩展 M5 的导入配额逻辑（M7 阶段）**：

M5 已实现免费用户导入上限为 1 个。M7 阶段将其扩展为 Premium 用户无限制：

```swift
// Services/SoundImportService.swift
var canImportMore: Bool {
    SubscriptionService.shared.isPremium || importedSounds.count < Self.freeImportLimit
}
```

```swift
// Views/SoundPickerView.swift — importedSection
if SoundImportService.shared.canImportMore {
    Button { showDocumentPicker = true } label: {
        HStack {
            Image(systemName: "plus.circle.fill")
            Text("Import from Files")
            if !SubscriptionService.shared.isPremium {
                Text("(\(SoundImportService.shared.remainingFreeImports) left)")
                    .font(.caption)
                    .foregroundColor(.secondary)
            }
        }
    }
} else {
    Button { showPaywall = true } label: {
        HStack {
            Image(systemName: "lock.fill").foregroundColor(.yellow)
            Text("Upgrade for unlimited imports")
        }
        .font(.system(size: 15))
        .foregroundColor(.accentColor)
    }
}
```

> **关键设计**：配额判断同时存在于 Service 层（`importFile` 二次校验）和 UI 层（按钮状态），防止任何绕过。

**定价策略建议**：

| 档位 | 价格 | 说明 |
|------|------|------|
| 月订阅 | $2.99/月 | 低门槛，随时取消 |
| 年订阅 | $14.99/年 | 推荐档位，省 50% |
| 一次性买断 | $19.99 | 一次性购买永久 Premium（可选） |

**Premium 声音包规划**：

| 包名 | 数量 | 内容 |
|------|------|------|
| Loud Pro | 20 首 | 超大音量、警报、军号、工业声 |
| Nature HD | 20 首 | 高质量自然录音（雨、雷、森林、海洋） |
| Sleep & Wake | 20 首 | ASMR、睡眠故事、温柔渐进唤醒 |
| White Noise | 15 首 | 各种白噪音/粉噪音/棕噪音 |
| Funny Voices | 15 首 | 角色语音、搞笑台词、名人模仿 |

**Premium 用户专属权益**：

| 权益 | 免费用户 | Premium 用户 |
|------|---------|-------------|
| 内置基础铃声 | 30 首 | 30 首 |
| Premium 声音包 | 锁定 | 90+ 首解锁 |
| 自定义音频导入 | 1 个 | 无限 |
| 视频闹钟 | 不可用 | 可用 |
| AI 铃声生成 | 每日 1 次 | 无限 |
| 广告 | 有 | 无 |

> **说明**：自定义导入限制为 1 个是核心防绕过策略。如果不限制，用户可从网络批量下载 Alarmy/Pillow 等竞品的铃声资源导入，Premium 声音包将失去付费吸引力。

#### 3.5 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 订阅商品加载 | App 启动时正确加载 IAP 商品 | 沙盒测试 |
| 购买流程 | 用户可成功购买月/年订阅 | 沙盒测试 |
| 解锁状态 | 购买后 Premium 铃声可正常选择 | 手动测试 |
| 恢复购买 | 重装 App 后可恢复订阅状态 | 沙盒测试 |
| 价格合规 | 符合 App Store 订阅定价规范 | 人工 Review |
| 付费墙 UI | 风格与 App 一致，iPad 适配 | 模拟器测试 |

#### 3.6 测试用例

```swift
func testPremiumSoundCount() {
    let premiumSounds = AppConstants.Sound.premiumSounds
    XCTAssertGreaterThan(premiumSounds.count, 0, "There should be premium sounds to monetize")
    XCTAssertTrue(premiumSounds.allSatisfy { $0.isPremium }, "All premiumSounds must have isPremium = true")
}

func testFreeSoundCount() {
    let freeSounds = AppConstants.Sound.builtInSounds.filter { !$0.isPremium }
    XCTAssertEqual(freeSounds.count, 30, "Free tier should include 30 sounds")
    XCTAssertTrue(AppConstants.Sound.builtInSounds.allSatisfy { !$0.isPremium }, "All built-in sounds should be free")
}

func testAllSoundsCombinesFreeAndPremium() {
    let allSounds = AppConstants.Sound.allSounds
    XCTAssertEqual(allSounds.count, AppConstants.Sound.builtInSounds.count + AppConstants.Sound.premiumSounds.count)
}

func testSubscriptionProductsCount() {
    XCTAssertEqual(SubscriptionProduct.all.count, 2, "Should offer monthly and yearly subscriptions")
}

func testSubscriptionProductIDsUnique() {
    let ids = SubscriptionProduct.all.map { $0.productId }
    XCTAssertEqual(ids.count, Set(ids).count, "Product IDs must be unique")
}

func testPremiumUserCanImportUnlimited() {
    // 前置条件：模拟 SubscriptionService.shared.isPremium = true
    // 且 importedSounds.count 已经 >= SoundImportService.freeImportLimit
    // 预期：canImportMore 仍然为 true
    XCTAssertTrue(SoundImportService.shared.canImportMore, "Premium users should have unlimited imports")
}
```

---

### M8：视频/AI 铃声创新

#### 3.1 需求描述

在基础铃声和订阅模式之上，通过创新功能实现差异化竞争。参考 Alarmy 2025 年推出的视频闹钟，以及 Wonderwake 的随机铃声防止疲劳功能。M8 属于前瞻性探索模块，优先级最低。

#### 3.2 设计规则

- **单一职责**：AI 铃声生成封装在 `AIGenerationService`；视频闹钟封装在 `VideoAlarmService`
- **可选依赖**：M8 不应影响 M1-M7 的核心功能，所有功能均为可选开启
- **代码复用**：视频闹钟的响铃界面复用 `AlarmRingView` 的布局框架，仅替换背景为视频层
- **风格一致**：AI 生成页沿用 `Form + Section` 设置风格
- **最小代码**：先实现最小可用版本（MVP），避免一次性引入大量 AI/视频库

#### 3.3 文件结构

```
Services/
├── AIGenerationService.swift       ← 新增，AI 铃声生成（可选）
├── VideoAlarmService.swift         ← 新增，视频闹钟管理（可选）
Views/
├── AIAlarmGeneratorView.swift      ← 新增，AI 生成铃声页面
├── Components/
│   └── VideoBackgroundView.swift   ← 新增，视频闹钟背景组件
├── AlarmRingView.swift             ← 修改（支持视频背景分支）
```

#### 3.4 代码编写规范

**M8.1 随机铃声防止疲劳（Alarm Shuffle）**：

```swift
// 在 Constants.swift 的 AppConstants.Sound 中新增
enum ShuffleMode: String, CaseIterable, Identifiable {
    case off = "off"
    case daily = "daily"
    case weekly = "weekly"

    var id: String { rawValue }
    var displayName: String {
        switch self {
        case .off:    return "Off"
        case .daily:  return "Change Daily"
        case .weekly: return "Change Weekly"
        }
    }
}
```

在 `AlarmScheduler.scheduleAlarm()` 中，如果开启 Shuffle，从用户偏好的分类中随机选择一首铃声：

```swift
func shuffleSound(for category: SoundCategory?, excluding current: String) -> String {
    let pool = AppConstants.Sound.builtInSounds
        .filter { category == nil || $0.category == category }
        .map { $0.displayName }
        .filter { $0 != current }
    return pool.randomElement() ?? current
}
```

**M8.2 视频闹钟（参考 Alarmy）**：

```swift
// Services/VideoAlarmService.swift
import Foundation
import AVKit

/// 管理视频闹钟资源
final class VideoAlarmService: ObservableObject {
    static let shared = VideoAlarmService()

    /// 内置视频闹钟列表（需提前放入 Bundle 或远程 CDN）
    let videoAlarms: [VideoAlarmInfo] = [
        VideoAlarmInfo(id: "sunrise", title: "Sunrise", videoName: "sunrise"),
        VideoAlarmInfo(id: "coffee", title: "Morning Coffee", videoName: "coffee"),
        VideoAlarmInfo(id: "ocean", title: "Ocean Waves", videoName: "ocean"),
        VideoAlarmInfo(id: "forest", title: "Forest", videoName: "forest")
    ]

    func urlForVideo(_ name: String) -> URL? {
        Bundle.main.url(forResource: name, withExtension: "mp4", subdirectory: "Videos")
    }
}

struct VideoAlarmInfo: Identifiable {
    let id: String
    let title: String
    let videoName: String
}
```

在 `AlarmRingView.swift` 中增加视频背景分支：

```swift
// AlarmRingView body 中，背景替换为：
ZStack {
    if let videoName = ringingAlarm?.videoAlarmName {
        VideoBackgroundView(videoName: videoName)
    } else {
        // 原有渐变背景
        gradientBackground
    }

    // ... 响铃 UI 不变 ...
}
```

**M8.3 AI 生成铃声（MVP 方案）**：

由于完整 TTS/音乐生成需要后端或第三方 API，MVP 采用"文本描述 + 预设参数组合生成"方案，设备端合成简单音调：

```swift
// Services/AIGenerationService.swift
import Foundation
import AVFoundation

/// 基于用户输入生成简单 AI 音调（MVP）
/// 未来可替换为后端 API 调用
final class AIGenerationService {
    static let shared = AIGenerationService()

    private init() {}

    /// 根据用户描述生成一个简单音频文件并保存到 ImportedSounds
    func generateSound(from prompt: String, style: AIGenStyle) async throws -> URL {
        // MVP：使用 AVAudioEngine 生成一段基于 style 参数的简单波形
        // 未来可接入 MusicGen / AudioLDM 等模型
        let fileName = "AI_\(UUID().uuidString).caf"
        let outputURL = SoundImportService.shared.importedDir.appendingPathComponent(fileName)

        // 使用系统 AudioUnit 生成正弦波/方波/锯齿波等
        try await renderTone(to: outputURL, style: style)
        return outputURL
    }

    enum AIGenStyle: String, CaseIterable {
        case calm = "Calm"
        case energetic = "Energetic"
        case nature = "Nature"
        case retro = "Retro"
    }

    private func renderTone(to url: URL, style: AIGenStyle) async throws {
        // 简化的设备端音频合成逻辑
        // ...
    }
}
```

> **说明**：M8.3 的完整 AI 生成属于高复杂度功能，MVP 仅做设备端简单合成。若后续需要真实 AI 音乐生成，应接入后端 API 并考虑版权合规。

#### 3.5 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 随机铃声 | 开启 Shuffle 后每天/每周自动更换铃声 | 手动测试 |
| 视频闹钟 | 响铃时显示视频背景 + 原有 UI 叠加 | 手动测试 |
| AI 生成 | 输入描述后可生成并保存为导入铃声 | 手动测试（MVP） |
| 不影响核心功能 | 关闭 M8 功能时 App 行为与 M7 完全一致 | 回归测试 |
| 包体积控制 | 视频资源单独下载，不打包进主包 | 架构约束 |

#### 3.6 测试用例

```swift
func testShuffleModeCases() {
    XCTAssertEqual(AppConstants.Sound.ShuffleMode.allCases.count, 3)
    XCTAssertEqual(AppConstants.Sound.ShuffleMode.daily.displayName, "Change Daily")
}

func testVideoAlarmResources() {
    for video in VideoAlarmService.shared.videoAlarms {
        let url = VideoAlarmService.shared.urlForVideo(video.videoName)
        XCTAssertNotNil(url, "Video resource missing for \(video.title)")
    }
}

func testAIGenerationStyles() {
    XCTAssertEqual(AIGenerationService.AIGenStyle.allCases.count, 4)
}
```

---

## 四、数据兼容性设计

### 4.1 AlarmItem.soundName 存储格式

| 来源 | soundName 格式 | 示例 |
|------|---------------|------|
| 内置铃声 | 直接使用显示名 | `"Gentle Morning"` |
| Apple Music | `"appleMusic:{persistentID}"` | `"appleMusic:1234567890"` |
| 自定义导入 | `"imported:{fileNameNoExt}"` | `"imported:MyAlarm"` |

### 4.2 AudioService 播放路由

```swift
// 在 AudioService 中根据 soundName 前缀判断来源：
func soundSource(for name: String) -> SoundSource {
    if name.hasPrefix("appleMusic:") { return .appleMusic }
    if name.hasPrefix("imported:")   { return .imported }
    return .builtIn
}
```

### 4.3 向后兼容保证

- 已有闹钟的 `soundName` 均为纯字符串（如 `"Gentle Morning"`），`soundSource()` 返回 `.builtIn`，逻辑不变
- Core Data 无需 schema migration，因为 `soundName` 字段类型仍为 String
- 新增的 25 首铃声文件与旧 5 首共存，不会覆盖

---

## 五、Settings 继承关系

### 5.1 当前 Settings 中与铃声相关的内容

| 功能 | 所在 Section | 继承方式 |
|------|-------------|---------|
| 音频测试播放 | Test Alarm Section | 播放默认铃声 `"Gentle Morning"` → **需适配**：如果用户已选择其他铃声，测试应使用当前默认铃声 |
| 系统音量显示 | Test Alarm Section | `VolumeMonitor.shared` → **无需改动** |

### 5.2 需要继承的设置项

| 设置项 | 继承方式 | 说明 |
|--------|---------|------|
| 预览音量 | 从 AlarmEditView 的 `volume` 绑定传入 | 替换硬编码 0.5 |
| 默认铃声 | `AppConstants.Sound.defaultSound` | 保持不变，所有新建闹钟继承此默认值 |
| Apple Music 授权状态 | `MusicLibraryService.shared.authorized` | Settings 可展示授权状态 |
| 导入铃声列表 | `SoundImportService.shared.importedSounds` | 无需在 Settings 中展示 |

### 5.3 SettingsView 潜在优化

在 Settings 的音频测试 Section 中，可增加：
- 当前默认铃声显示
- Apple Music 授权状态（M4 后）

> 此优化为低优先级，可在主功能完成后迭代。

---

## 六、实现顺序与依赖关系

```
M1 铃声数据模型 ─────┐
                      ├──→ M3 铃声选择器重构 ──→ M4 Apple Music ──→ M5 自定义导入 ──→ M6 清理
M2 铃声资源扩充 ─────┘                              │
                                                     │
M7 Premium 订阅 ────────────────────────────────────┘（依赖 M1-M6，提供变现路径）
                                                     │
M8 视频/AI 铃声 ─────────────────────────────────────┘（依赖 M1-M7，可选创新）
```

**建议实施节奏**：

| 阶段 | 模块 | 预计工时 | 验收标准 |
|------|------|---------|---------|
| 阶段一 | M1 + M2 + M3 | 2-3 天 | 30 首铃声可按分类浏览选择，预览音量继承 |
| 阶段二 | M4 | 2 天 | Apple Music 歌曲可选为闹钟铃声 |
| 阶段三 | M5 | 1-2 天 | Files App 音频文件可导入为铃声 |
| 阶段四 | M6 | 0.5 天 | 所有废弃代码清理完毕，编译零警告 |
| 阶段五 | M7 | 3-4 天 | Premium 订阅购买、Premium 铃声解锁、Paywall 页面上线 |
| 阶段六 | M8 | 5-7 天 | 视频闹钟或 AI 生成铃声 MVP 可用 |

---

## 七、风险与注意事项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 包体积增大 | 25 首新 .caf 文件约 8-12MB | 压缩音频质量至 44.1kHz/16bit，控制在 500KB/首 |
| Apple Music 需订阅 | 免费用户无法播放 Apple Music 歌曲 | UI 上明确标注"需要 Apple Music 订阅" |
| MPMediaItem 可被删除 | 用户删歌后闹钟无法播放 | `playAppleMusicSound` 失败时 fallback 到默认铃声 |
| iOS 后台播放限制 | Apple Music 后台播放可能被中断 | 使用 `MPMusicPlayerController.applicationQueuePlayer`（系统级播放器） |
| ImportedSounds 权限 | Documents 目录文件可能被系统清理 | 仅缓存非关键数据，引导用户保留源文件 |
| Core Data 兼容 | soundName 格式扩展 | 纯 String 字段，无需 migration |
| IAP 审核被拒 | 订阅定价、恢复购买、隐私政策不符合 App Store 规范 | 提供 Restore Purchases 按钮、在隐私政策中说明订阅条款、使用 Apple 官方 StoreKit 2 API |
| 包体积继续增大（M7） | Premium 声音包额外 90 首约 30-45MB | 采用按需下载（On-Demand Resources）或 CDN 分发，避免一次性打包 |
| 视频资源过大（M8） | 视频文件打包会显著增加 App 体积 | 视频资源不打包，使用 CDN 下载 + 本地缓存 |
| AI 生成版权风险（M8） | 使用第三方 AI 音乐 API 可能产生版权争议 | MVP 使用设备端合成，后续接入 API 时审查服务条款 |
| 功能复杂度失控（M8） | 视频/AI 功能可能影响核心稳定性 | M8 所有功能默认关闭，通过 Feature Flag 控制，不影响 M1-M7 核心路径 |

---

## 八、验收检查清单

- [ ] 内置铃声 30 首，5 分类各 6 首
- [ ] 铃声选择器按分类分组展示
- [ ] 预览音量继承闹钟编辑页音量设置
- [ ] Apple Music 歌曲可选为闹钟铃声
- [ ] 自定义音频文件可从 Files 导入
- [ ] 免费用户自定义导入限 1 个（M5 + M7）
- [ ] Premium 用户自定义导入无数量限制（M7）
- [ ] 已保存闹钟（旧 soundName）正常播放
- [ ] 无硬编码 0.5 预览音量
- [ ] 无空目录 `Resources/Sounds/`
- [ ] 无废弃旧代码残留
- [ ] Xcode 编译零 error 零 warning
- [ ] iPhone + iPad 布局正常
- [ ] 深色模式下 UI 正常
- [ ] Premium 订阅商品正确加载（M7）
- [ ] 未订阅用户看到 Premium 铃声锁定状态（M7）
- [ ] 订阅后 Premium 铃声可正常选择播放（M7）
- [ ] Restore Purchases 功能可用（M7）
- [ ] 随机铃声 Shuffle 功能可选开启（M8）
- [ ] 视频闹钟背景正常显示且不遮挡响铃 UI（M8）
- [ ] AI 生成铃声可保存为导入铃声（M8 MVP）
- [ ] M8 功能默认关闭时不影响 M1-M7 核心路径（M8）
