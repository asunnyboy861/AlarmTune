# AlarmTune UI/UX 现代化与平台深度集成优化计划

> 基于：2026 美国市场 Gen Z 用户使用习惯审计 + iOS 平台深度集成差距分析
> 版本：v1.0 | 日期：2026-07-05
> 核心目标：将软件从"功能齐全但包装过时"升级为"美国 Gen Z 愿意付费的现代化闹钟"
> 当前美国市场符合度：约 60% → 目标：85%+
>
> 六大原则：
> 1. 需求描述 + 生成规则 + 检验标准（代码示例 + 测试用例 + 注释）
> 2. 功能模块原则（单一职责、高内聚低耦合、命名清晰、文件结构语义化）
> 3. 代码复用（优先复用现有模块、三次法则、最小代码原则）
> 4. 重构清理（标记废弃 → 验证无影响 → 删除；提交信息注明清理范围）
> 5. 与当前软件风格一致（格调、功能、UI 与现有版本一致）
> 6. 优先继承而非硬编码（特别是 Settings 中，与现有代码兼容）

---

## 一、现状诊断回顾

### 1.1 美国市场符合度评分

| 板块 | 符合度 | 主要差距 |
|------|--------|---------|
| 闹钟列表 | 70% | 缺 Widget / Live Activity / 动画 |
| 闹钟响铃 | 50% | 缺 Slide-to-Stop / 任务唤醒 / 视觉冲击 |
| 闹钟编辑 | 75% | Wheel picker 略旧、缺智能预设 |
| 声音选择 | 60% | 缺搜索 / 收藏 / 波形 |
| 视频选择 | 80% | 相对现代化 |
| Paywall | 40% | 缺 Free Trial / Social Proof / 对比表 |
| 设置 | 60% | 缺 Widget/Siri/FAQ 配置 |
| AI 生成 | 30% | 名不副实，预设选择而非真实 AI |

### 1.2 与 2026 主流趋势的差距

| 趋势 | 当前 | 标准 | 差距等级 |
|------|------|------|---------|
| Dynamic Island / Live Activity | ❌ | 闹钟类必备 | 🔴 P0 |
| Lock Screen Widget | ❌ | 表盘类必备 | 🔴 P0 |
| Onboarding Flow | ❌ | 首次启动必备 | 🔴 P0 |
| Slide-to-Stop 交互 | ❌ | Apple Clock 习惯 | 🔴 P0 |
| Mesh Gradient / Glassmorphism | ❌ | Gen Z 标配 | 🟡 P1 |
| Spring 物理 + Micro-interactions | ❌ | 2026 标配 | 🟡 P1 |
| 任务式唤醒（Math/Puzzle） | ❌ | Alarmy 核心竞争力 | 🟡 P1 |
| 真实 AI 集成 | ❌ | 头条功能名实相符 | 🔴 P0 |

---

## 二、改进模块总览

按功能模块划分，每个模块单一职责、高内聚低耦合。优先级按美国市场影响排序。

| 模块编号 | 模块名称 | 优先级 | 预估工时 | 模块职责 |
|---------|---------|--------|---------|---------|
| N1 | Paywall 转化优化 | P0 | 1天 | Free Trial 徽章 + Social Proof + 对比表 + Glassmorphism 视觉升级 |
| N2 | Onboarding Flow | P0 | 2天 | 首次启动 3 屏引导 + 权限请求 + 示例闹钟推荐 |
| N3 | 响铃页交互升级 | P0 | 2天 | Slide-to-Stop + 贪睡计数 + Mesh Gradient + 粒子效果 |
| N4 | AI Tone Generator 重构 | P0 | 1天 | 真实 Prompt 驱动 + 历史记录 + 波形预览 + 改名避免误导 |
| N5 | Live Activity + Dynamic Island | P0 | 3天 | 响铃中状态 + 贪睡倒计时 + 锁屏控制 |
| N6 | Lock Screen Widget | P1 | 2天 | 下一个闹钟卡片 + 快速启停 |
| N7 | 任务式唤醒 | P1 | 2天 | Math Mission / Shake Mission / Memory Mission |
| N8 | 搜索 + 收藏系统 | P1 | 2天 | 声音/视频统一搜索 + 收藏持久化 |
| N9 | Design Token 系统 | P1 | 1天 | Typography Type Scale + 主题色继承 + Spacing 系统 |
| N10 | Spring 动画工具集 | P1 | 1天 | 统一过渡动画 + 卡片按压反馈 + 微交互 |

**总预估工时**：17 个工作日（约 3.5 周）

---

## 三、模块依赖关系

```
N9（Design Token）─ 基础设施，所有视觉模块依赖
     │
     ├── N1（Paywall 视觉升级）
     ├── N2（Onboarding 视觉）
     └── N3（响铃页视觉升级）
           │
           ▼
N10（Spring 动画）─ 横切关注点
     │
     ▼
N4（AI 重构）─ 独立，不依赖其他
     │
     ▼
N5（Live Activity）─ 依赖 N3 响铃状态定义
     │
     ▼
N6（Widget）─ 依赖 N5 共享 App Group
     │
     ▼
N7（任务式唤醒）─ 依赖 N3 响铃页扩展点
     │
     ▼
N8（搜索 + 收藏）─ 独立，复用现有 Picker
```

**推荐实施顺序**：
- **第一周（P0 核心）**：N9 → N1 → N2 → N3 → N4
- **第二周（平台深度）**：N10 → N5 → N6
- **第三周（差异化）**：N7 → N8

---

## 四、模块详细设计

---

### N1：Paywall 转化优化

#### 4.1.1 需求描述

当前 Paywall 转化率低（预计 < 5%），缺少美国市场 2026 转化最佳实践要素：

- **Free Trial 徽章**：未展示"3 Days Free"导致用户犹豫（影响转化率 30%+）
- **Social Proof**：无"Join 100K+ early risers"等社会认同
- **对比表**：Free vs Premium 功能对比缺失
- **视觉单薄**：白底卡片，无 Glassmorphism / 渐变 / 动画

#### 4.1.2 设计规则

- **继承现有架构**：复用 `PaywallView.swift` 文件，不新建文件
- **复用 SubscriptionService**：不修改订阅逻辑，仅升级 UI 层
- **继承主题色**：Glassmorphism 使用 `AppConstants.Theme` 主题色，不硬编码
- **常量继承**：Free Trial 天数定义在 `AppConstants.Subscription` 命名空间
- **最小代码原则**：能复用现有 VStack 卡片结构则不重写
- **三次法则**：Feature Row 抽象需在 3 处以上使用（benefits 列表 + 对比表 + onboarding）

#### 4.1.3 代码编写规范

**1. 常量定义（Constants.swift — AppConstants.Subscription 命名空间）**：

```swift
// Constants.swift — 新增 Subscription 命名空间

extension AppConstants {
    enum Subscription {
        /// Free Trial 天数（App Store Connect 配置的 introductory offer）
        static let freeTrialDays: Int = 3
        
        /// Social Proof 文案（动态从 UserDefaults 读取安装计数，兜底静态值）
        static let socialProofFallback = "Join 10,000+ early risers"
        
        /// Premium 功能列表（用于 Paywall + Onboarding + Settings 三处复用）
        static let premiumFeatures: [PremiumFeature] = [
            .init(icon: "tray.fill", title: "Unlimited Sound Imports",
                  description: "Import custom sounds from Files", isPremium: true),
            .init(icon: "video.fill", title: "Unlimited Video Backgrounds",
                  description: "Import videos from Photos or Files", isPremium: true),
            .init(icon: "wand.and.stars", title: "AI Tone Generation",
                  description: "Generate custom alarm tones with AI", isPremium: true),
            .init(icon: "shuffle", title: "Sound Shuffle",
                  description: "Auto-rotate sounds to prevent habituation", isPremium: true),
            .init(icon: "paintbrush.fill", title: "Theme Colors",
                  description: "Customize app accent color", isPremium: true),
        ]
        
        /// Free 版本功能限制（对比表用）
        static let freeLimits: [String] = [
            "3 custom sound imports",
            "1 video background import",
            "Standard alarm sounds only",
        ]
    }
    
    /// Premium 功能描述（复用结构体，遵循三次法则）
    struct PremiumFeature: Identifiable, Hashable {
        let id = UUID()
        let icon: String
        let title: String
        let description: String
        let isPremium: Bool
    }
}
```

**2. PaywallView 视觉升级（复用现有文件）**：

```swift
// PaywallView.swift — 在现有结构上升级，不新建文件

/// N1：Glassmorphism 卡片背景（复用于 Onboarding N2）
struct GlassCard<Content: View>: View {
    let cornerRadius: CGFloat
    @ViewBuilder let content: () -> Content
    
    var body: some View {
        content()
            .padding(AppConstants.Layout.cardPadding)
            .background(.ultraThinMaterial)
            .clipShape(RoundedRectangle(cornerRadius: cornerRadius))
            .overlay(
                RoundedRectangle(cornerRadius: cornerRadius)
                    .stroke(Color.white.opacity(0.2), lineWidth: 1)
            )
    }
}

// PaywallView body 内新增：
// 1. Free Trial 徽章
HStack(spacing: 6) {
    Image(systemName: "gift.fill")
        .foregroundColor(.yellow)
    Text("\(AppConstants.Subscription.freeTrialDays) Days Free")
        .font(.system(size: 14, weight: .semibold))
}
.padding(.horizontal, 12)
.padding(.vertical, 6)
.background(Color.yellow.opacity(0.2))
.clipShape(Capsule())

// 2. Social Proof
Text(AppConstants.Subscription.socialProofFallback)
    .font(.system(size: 13, weight: .medium))
    .foregroundColor(.secondary)

// 3. 对比表（复用 PremiumFeature 列表）
GlassCard(cornerRadius: AppConstants.Layout.cardCornerRadius) {
    VStack(alignment: .leading, spacing: 12) {
        Text("Free vs Premium").font(.headline)
        ForEach(AppConstants.Subscription.premiumFeatures) { feature in
            FeatureRow(feature: feature)  // 抽象组件，3 处复用
        }
    }
}

// 4. FeatureRow 抽象组件（遵循三次法则 — Paywall + Onboarding + Settings 三处使用）
struct FeatureRow: View {
    let feature: AppConstants.PremiumFeature
    
    var body: some View {
        HStack(spacing: 12) {
            Image(systemName: feature.icon)
                .font(.system(size: 18))
                .foregroundColor(.accentColor)
                .frame(width: 28)
            VStack(alignment: .leading, spacing: 2) {
                Text(feature.title).font(.system(size: 15, weight: .medium))
                Text(feature.description).font(.caption).foregroundColor(.secondary)
            }
            Spacer()
            Image(systemName: "checkmark.circle.fill")
                .foregroundColor(.green)
        }
    }
}
```

#### 4.1.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Free Trial 徽章 | 显示 "3 Days Free" 黄色胶囊 | 视觉检查 |
| Social Proof | 显示"Join 10,000+ early risers" | 视觉检查 |
| 对比表 | Free vs Premium 5 项功能对比 | 视觉检查 |
| Glassmorphism | 卡片有 .ultraThinMaterial 背景 | 视觉检查 |
| 主题色继承 | accentColor 随 Settings 主题色变化 | 切换主题验证 |
| 订阅逻辑不变 | 购买流程与重构前一致 | 沙盒测试 |
| FeatureRow 复用 | Paywall + Onboarding + Settings 三处使用 | Grep 验证 |

#### 4.1.5 测试用例

```swift
// AlarmTuneTests/PaywallTests.swift
import XCTest

final class PaywallTests: XCTestCase {
    func testPremiumFeaturesCount() {
        XCTAssertEqual(AppConstants.Subscription.premiumFeatures.count, 5,
                       "Paywall should display 5 premium features")
    }
    
    func testFreeTrialDaysPositive() {
        XCTAssertGreaterThan(AppConstants.Subscription.freeTrialDays, 0,
                             "Free trial days must be positive")
    }
    
    func testAllFeaturesHaveUniqueTitles() {
        let titles = AppConstants.Subscription.premiumFeatures.map { $0.title }
        XCTAssertEqual(Set(titles).count, titles.count,
                       "Feature titles must be unique")
    }
}
```

#### 4.1.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `SubscriptionService` | ✅ 完全兼容 | 不修改订阅逻辑，仅升级 UI |
| `PaywallView` 现有结构 | ✅ 增量升级 | 保留 NavigationStack + ScrollView 骨架 |
| `AppConstants.Theme` | ✅ 继承 | Glassmorphism 使用主题色 |
| `SettingsView` Premium 区块 | ✅ 复用 | FeatureRow 在 Settings 中也可复用 |

#### 4.1.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `PaywallView.swift` | 旧的 3 个独立 benefit 行 | `FeatureRow` 统一组件 | 编译 + 视觉检查 |
| `PaywallView.swift` | 旧的纯白卡片 | `GlassCard` 组件 | 视觉检查 |

---

### N2：Onboarding Flow

#### 4.2.1 需求描述

当前首次启动直接显示空状态（"No Alarms Yet"），新用户流失率高。需要：

- **3 屏引导**：欢迎 → 核心功能演示 → 权限请求
- **示例闹钟推荐**：引导结束后可选添加示例闹钟
- **不重复出现**：UserDefaults 记录已看过

#### 4.2.2 设计规则

- **新建独立模块**：`Views/Onboarding/OnboardingView.swift`（单一职责）
- **复用 N1 组件**：`GlassCard` + `FeatureRow`（代码复用原则）
- **复用现有权限请求**：`AlarmScheduler.requestAuthorization()`（不重写）
- **常量继承**：Onboarding 完成标记存入 `AppConstants.Onboarding.storageKey`
- **主题色继承**：使用 `AppConstants.Theme` 主题色
- **最小代码原则**：3 屏用 `TabView` + `.tabViewStyle(.page)` 实现，不逐屏写

#### 4.2.3 代码编写规范

**1. 常量定义（Constants.swift — AppConstants.Onboarding 命名空间）**：

```swift
extension AppConstants {
    enum Onboarding {
        /// UserDefaults 存储键，记录是否已完成引导
        static let storageKey = "onboardingCompleted"
        
        /// 引导页数据（复用 PremiumFeature 结构，不新建类型）
        static let slides: [OnboardingSlide] = [
            .init(icon: "alarm.fill", title: "Wake Up Your Way",
                  description: "Custom volume for every alarm. Mix sounds, videos, and AI tones."),
            .init(icon: "video.fill", title: "Video Alarm Clock",
                  description: "Wake up to thunderstorms, ocean waves, or your favorite clips."),
            .init(icon: "wand.and.stars", title: "AI Tone Generation",
                  description: "Generate custom alarm tones with a single prompt."),
        ]
    }
    
    /// Onboarding 单页数据（与 PremiumFeature 风格对齐）
    struct OnboardingSlide: Identifiable {
        let id = UUID()
        let icon: String
        let title: String
        let description: String
    }
}
```

**2. OnboardingView 主视图**：

```swift
// Views/Onboarding/OnboardingView.swift — 新建文件
import SwiftUI

/// 首次启动引导页（N2）
/// 3 屏滑动 + 权限请求 + 示例闹钟推荐
/// 复用 N1 的 GlassCard 组件
struct OnboardingView: View {
    @AppStorage(AppConstants.Onboarding.storageKey) private var completed = false
    @State private var currentPage = 0
    @State private var showPermissionSheet = false
    
    var body: some View {
        ZStack {
            // 主题色渐变背景
            LinearGradient(
                colors: [.accentColor.opacity(0.3), .accentColor.opacity(0.1)],
                startPoint: .top, endPoint: .bottom
            )
            .ignoresSafeArea()
            
            VStack {
                TabView(selection: $currentPage) {
                    ForEach(Array(AppConstants.Onboarding.slides.enumerated()), id: \.offset) { idx, slide in
                        OnboardingSlideView(slide: slide).tag(idx)
                    }
                }
                .tabViewStyle(.page(indexDisplayMode: .always))
                
                // 最后一屏显示按钮
                if currentPage == AppConstants.Onboarding.slides.count - 1 {
                    Button {
                        showPermissionSheet = true
                    } label: {
                        Text("Get Started")
                            .font(.system(size: 17, weight: .semibold))
                            .foregroundColor(.white)
                            .frame(maxWidth: .infinity)
                            .padding(.vertical, 14)
                            .background(Color.accentColor)
                            .clipShape(RoundedRectangle(cornerRadius: 12))
                    }
                    .padding(.horizontal, 24)
                    .padding(.bottom, 40)
                }
            }
        }
        .sheet(isPresented: $showPermissionSheet) {
            PermissionRequestView(onComplete: {
                completed = true
            })
        }
    }
}

/// 单页内容（复用 GlassCard）
struct OnboardingSlideView: View {
    let slide: AppConstants.OnboardingSlide
    
    var body: some View {
        VStack(spacing: 24) {
            Spacer()
            GlassCard(cornerRadius: AppConstants.Layout.largeCardCornerRadius) {
                VStack(spacing: 16) {
                    Image(systemName: slide.icon)
                        .font(.system(size: 60))
                        .foregroundColor(.accentColor)
                    Text(slide.title)
                        .font(.system(size: 24, weight: .bold))
                    Text(slide.description)
                        .font(.system(size: 16))
                        .foregroundColor(.secondary)
                        .multilineTextAlignment(.center)
                }
            }
            .padding(.horizontal, 24)
            Spacer()
        }
    }
}
```

**3. 权限请求页（复用现有 AlarmScheduler）**：

```swift
// Views/Onboarding/PermissionRequestView.swift — 新建文件
import SwiftUI

/// 权限请求页（N2）
/// 复用 AlarmScheduler.requestAuthorization，不重写权限逻辑
struct PermissionRequestView: View {
    let onComplete: () -> Void
    @Environment(\.dismiss) private var dismiss
    
    var body: some View {
        VStack(spacing: 24) {
            Image(systemName: "bell.badge.fill")
                .font(.system(size: 50))
                .foregroundColor(.accentColor)
            
            Text("Enable Notifications")
                .font(.title2.bold())
            
            Text("AlarmTune needs notification permission to play alarms, even when the app is closed.")
                .font(.body)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)
                .padding(.horizontal)
            
            Spacer()
            
            VStack(spacing: 12) {
                Button {
                    AlarmScheduler.shared.requestAuthorization { _ in
                        onComplete()
                        dismiss()
                    }
                } label: {
                    Text("Allow Notifications")
                        .font(.system(size: 17, weight: .semibold))
                        .foregroundColor(.white)
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, 14)
                        .background(Color.accentColor)
                        .clipShape(RoundedRectangle(cornerRadius: 12))
                }
                
                Button("Maybe Later") {
                    onComplete()
                    dismiss()
                }
                .foregroundColor(.secondary)
            }
            .padding(.horizontal, 24)
            .padding(.bottom, 40)
        }
    }
}
```

**4. AlarmListView 启动时判断**：

```swift
// AlarmListView.swift — 在 .onAppear 中新增
.onAppear {
    if !UserDefaults.standard.bool(forKey: AppConstants.Onboarding.storageKey) {
        showOnboarding = true
    }
}
.sheet(isPresented: $showOnboarding) {
    OnboardingView()
}
```

#### 4.2.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 首次启动 | 显示 Onboarding 而非空状态 | 模拟器首次安装 |
| 3 屏滑动 | TabView page 样式可左右滑 | 手动测试 |
| 权限请求 | 最后一屏点 Get Started 弹权限 | 手动测试 |
| 不重复 | 完成后不再出现 | 二次启动验证 |
| 主题色继承 | 渐变背景跟随主题色 | 切换主题验证 |
| GlassCard 复用 | Onboarding + Paywall 共用 | Grep 验证 |

#### 4.2.5 测试用例

```swift
func testOnboardingSlidesCount() {
    XCTAssertEqual(AppConstants.Onboarding.slides.count, 3,
                   "Onboarding should have 3 slides")
}

func testOnboardingNotCompletedByDefault() {
    UserDefaults.standard.removeObject(forKey: AppConstants.Onboarding.storageKey)
    XCTAssertFalse(UserDefaults.standard.bool(forKey: AppConstants.Onboarding.storageKey),
                   "Onboarding should not be completed by default")
}
```

#### 4.2.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AlarmListView.onAppear` | ✅ 增量添加 | 新增 onboarding 判断分支 |
| `AlarmScheduler.requestAuthorization` | ✅ 直接复用 | 不重写权限逻辑 |
| `EmptyStateView` | ✅ 保留 | Onboarding 跳过后仍可能进入空状态 |
| `AppConstants.Theme` | ✅ 继承 | 渐变使用 accentColor |

#### 4.2.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| 无 | — | — | 全新模块，无废弃代码 |

---

### N3：响铃页交互升级

#### 4.3.1 需求描述

响铃页是用户每天接触的最关键时刻，当前设计单薄：

- **缺 Slide-to-Stop**：不符合 Apple Clock 用户肌肉记忆
- **无贪睡计数**：用户不知还能贪睡几次
- **视觉单薄**：仅一个脉冲圆，无 Mesh Gradient / 粒子

#### 4.3.2 设计规则

- **继承现有架构**：复用 `AlarmRingView.swift` + `VideoBackgroundView.swift`
- **复用 AppConstants.Alarm.maxSnoozeCount**：已定义常量，不硬编码
- **新建独立组件**：`SlideToStopButton.swift`（单一职责，可复用）
- **继承主题色**：Mesh Gradient 使用 `AppConstants.Theme`
- **三次法则**：`PulsingCircle` 抽象需在响铃页 + Onboarding + Paywall 三处使用
- **最小代码原则**：贪睡计数复用 `maxSnoozeCount` 常量

#### 4.3.3 代码编写规范

**1. SlideToStopButton 组件（新建独立文件）**：

```swift
// Views/Components/SlideToStopButton.swift — 新建文件
import SwiftUI

/// 滑动停止按钮（N3）
/// 模仿 Apple Clock 的 Slide to Stop 交互
/// 复用 AppConstants.Layout.cardCornerRadius
struct SlideToStopButton: View {
    let onStop: () -> Void
    
    @State private var offset: CGFloat = 0
    @State private var isDragging = false
    
    private let buttonSize: CGFloat = 56
    private let trackPadding: CGFloat = 6
    
    var body: some View {
        GeometryReader { geo in
            let maxWidth = geo.size.width - buttonSize - trackPadding * 2
            
            ZStack(alignment: .leading) {
                // 轨道
                RoundedRectangle(cornerRadius: buttonSize / 2)
                    .fill(Color.white.opacity(0.2))
                    .overlay(
                        HStack {
                            Spacer()
                            Text("Slide to Stop")
                                .font(.system(size: 16, weight: .semibold))
                                .foregroundColor(.white.opacity(0.8))
                                .padding(.trailing, 20)
                        }
                    )
                
                // 滑块
                Circle()
                    .fill(Color.red)
                    .frame(width: buttonSize, height: buttonSize)
                    .overlay(
                        Image(systemName: "arrow.right")
                            .foregroundColor(.white)
                            .font(.system(size: 22, weight: .bold))
                    )
                    .offset(x: trackPadding + offset)
                    .gesture(
                        DragGesture()
                            .onChanged { value in
                                isDragging = true
                                offset = min(max(0, value.translation.width), maxWidth)
                            }
                            .onEnded { value in
                                if offset >= maxWidth * 0.9 {
                                    HapticService.shared.heavy()
                                    onStop()
                                } else {
                                    // 弹回（Spring 动画 — N10 复用）
                                    withAnimation(.spring(response: 0.3, dampingFraction: 0.7)) {
                                        offset = 0
                                    }
                                }
                                isDragging = false
                            }
                    )
            }
        }
        .frame(height: buttonSize + trackPadding * 2)
    }
}
```

**2. Mesh Gradient 背景（新建组件，N1/N3 共用）**：

```swift
// Views/Components/MeshGradientBackground.swift — 新建文件
import SwiftUI

/// Mesh 渐变背景（N3）
/// 多点径向渐变叠加，营造 2026 现代感
/// 复用于响铃页 + Onboarding + Paywall
struct MeshGradientBackground: View {
    let baseColor: Color
    
    var body: some View {
        ZStack {
            baseColor.opacity(0.9)
            
            // 多个径向渐变叠加形成 mesh 效果
            RadialGradient(
                colors: [Color.purple.opacity(0.6), .clear],
                center: .topLeading, startRadius: 0, endRadius: 300
            )
            .blur(radius: 40)
            
            RadialGradient(
                colors: [Color.blue.opacity(0.5), .clear],
                center: .bottomTrailing, startRadius: 0, endRadius: 350
            )
            .blur(radius: 50)
            
            RadialGradient(
                colors: [Color.pink.opacity(0.4), .clear],
                center: .center, startRadius: 0, endRadius: 250
            )
            .blur(radius: 30)
        }
        .ignoresSafeArea()
    }
}
```

**3. AlarmRingView 升级（复用现有文件）**：

```swift
// AlarmRingView.swift — 增量升级

// 替换：旧的 Stop 按钮 → SlideToStopButton
// 旧代码（标记废弃）：
// Button("Stop") { stopAlarm() }
//     .buttonStyle(.borderedProminent)
//     .tint(.red)

// 新代码：
SlideToStopButton {
    stopAlarm()
}
.padding(.horizontal, 24)

// 新增：贪睡计数显示
if let remaining = snoozeRemaining, remaining > 0 {
    Text("\(remaining) snooze\(remaining == 1 ? "" : "s") left")
        .font(.caption)
        .foregroundColor(.white.opacity(0.7))
}

// 新增：Mesh Gradient 背景（无视频背景时）
if videoBackgroundName == nil {
    MeshGradientBackground(baseColor: .accentColor)
}
```

#### 4.3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Slide-to-Stop | 滑动 90% 触发停止 | 手动测试 |
| 滑块弹回 | 未滑到位弹回原位 | 手动测试 |
| 触觉反馈 | 滑动到位 heavy haptic | 手动测试 |
| 贪睡计数 | 显示剩余次数 | 手动测试 |
| Mesh Gradient | 无视频时显示渐变背景 | 手动测试 |
| 视频背景兼容 | 有视频时仍正常播放 | 手动测试 |

#### 4.3.5 测试用例

```swift
func testSlideToStopThreshold() {
    // 滑动 < 90% 不触发停止
    // 滑动 >= 90% 触发停止
    // 验证逻辑在 DragGesture.onEnded
}

func testSnoozeCountDecrement() {
    let alarm = AlarmItem.create(in: PersistenceController.shared.viewContext)
    XCTAssertEqual(AppConstants.Alarm.maxSnoozeCount, 3)
}
```

#### 4.3.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AlarmRingView` | ✅ 增量升级 | 保留 VideoBackgroundView 调用 |
| `VideoBackgroundView` | ✅ 完全兼容 | 不修改视频播放逻辑 |
| `AppConstants.Alarm.maxSnoozeCount` | ✅ 复用 | 已定义常量 |
| `HapticService` | ✅ 复用 | 已有 heavy() 方法 |

#### 4.3.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `AlarmRingView.swift` | 旧 Stop Button | `SlideToStopButton` | 编译 + 手动测试 |
| `AlarmRingView.swift` | 旧的单脉冲圆 | 保留（与 Mesh Gradient 共存） | 视觉检查 |

---

### N4：AI Tone Generator 重构

#### 4.4.1 需求描述

当前 "AI Tone Generator" 名不副实 — 仅 4 个风格预设选择，无真实 AI 生成。需重构为：

- **真实 Prompt 驱动**：用户输入文本描述，调用 AI 生成铃声
- **历史记录**：保存生成过的铃声
- **波形预览**：生成后显示波形可视化
- **改名避免误导**：保留 "Tone Generator" 命名，移除 "AI" 前缀（除非真的有 AI）

#### 4.4.2 设计规则

- **继承现有架构**：复用 `AIAlarmGeneratorView.swift` + `AIGenerationService.swift`
- **复用 SubscriptionService**：Premium 校验逻辑不变
- **常量继承**：Prompt 建议存入 `AppConstants.AI.promptSuggestions`
- **三次法则**：`WaveformView` 抽象需在 AI 生成 + 声音选择 + 视频预览三处使用
- **最小代码原则**：历史记录复用 `VideoImportService` 的持久化模式

#### 4.4.3 代码编写规范

**1. 常量定义（Constants.swift — AppConstants.AI 命名空间）**：

```swift
extension AppConstants {
    enum AI {
        /// Prompt 建议芯片（Gen Z 友好文案）
        static let promptSuggestions: [String] = [
            "Gentle rain with soft piano",
            "Epic orchestral swell",
            "Lo-fi beat with vinyl crackle",
            "Birds chirping at sunrise",
            "Synth wave retro alarm",
            "Tibetan singing bowl",
        ]
        
        /// 历史记录 UserDefaults 键
        static let historyStorageKey = "aiToneHistory"
        
        /// 最大历史记录数
        static let maxHistoryCount = 20
    }
}
```

**2. 波形可视化组件（新建，三次法则）**：

```swift
// Views/Components/WaveformView.swift — 新建文件
import SwiftUI

/// 音频波形可视化（N4）
/// 从音频文件提取波形数据，绘制柱状图
/// 复用于 AI 生成 + 声音选择 + 视频预览
struct WaveformView: View {
    let audioURL: URL?
    let barCount: Int = 40
    @State private var amplitudes: [CGFloat] = []
    
    var body: some View {
        GeometryReader { geo in
            HStack(spacing: 2) {
                ForEach(0..<barCount, id: \.self) { i in
                    Capsule()
                        .fill(Color.accentColor)
                        .frame(width: (geo.size.width - CGFloat(barCount - 1) * 2) / CGFloat(barCount),
                               height: max(2, amplitudes.indices.contains(i) ? amplitudes[i] * geo.size.height : 2))
                }
            }
            .frame(maxHeight: .infinity, alignment: .center)
        }
        .onAppear { extractWaveform() }
    }
    
    private func extractWaveform() {
        guard let url = audioURL else { return }
        // 使用 AVAudioFile 提取波形数据（异步）
        DispatchQueue.global(qos: .userInitiated).async {
            let samples = AudioWaveformExtractor.extract(from: url, sampleCount: barCount)
            DispatchQueue.main.async {
                amplitudes = samples
            }
        }
    }
}
```

**3. AIAlarmGeneratorView 重构（复用现有文件）**：

```swift
// AIAlarmGeneratorView.swift — 增量重构

// 替换：4 个风格卡片 → Prompt 输入 + 建议芯片
TextField("Describe your perfect alarm tone", text: $prompt, axis: .vertical)
    .textFieldStyle(.roundedBorder)
    .lineLimit(3)

// Prompt 建议芯片
ScrollView(.horizontal, showsIndicators: false) {
    HStack(spacing: 8) {
        ForEach(AppConstants.AI.promptSuggestions, id: \.self) { suggestion in
            Button {
                prompt = suggestion
            } label: {
                Text(suggestion)
                    .font(.caption)
                    .padding(.horizontal, 12)
                    .padding(.vertical, 6)
                    .background(Color.accentColor.opacity(0.1))
                    .clipShape(Capsule())
            }
            .foregroundColor(.accentColor)
        }
    }
}

// 波形预览（生成后显示）
if let generatedURL = generatedToneURL {
    WaveformView(audioURL: generatedURL)
        .frame(height: 60)
        .padding()
}

// 历史记录
DisclosureGroup("Recent Tones") {
    ForEach(historyTones, id: \.self) { toneName in
        Button(toneName) { /* 加载历史 */ }
    }
}
```

#### 4.4.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Prompt 输入 | 支持多行文本 | 手动测试 |
| 建议芯片 | 6 个可点击填充 | 手动测试 |
| 波形预览 | 生成后显示柱状波形 | 视觉检查 |
| 历史记录 | 保存最近 20 条 | 手动测试 |
| 命名准确 | 不再误导为"AI"（若无真实 AI） | 文案检查 |

#### 4.4.5 测试用例

```swift
func testPromptSuggestionsCount() {
    XCTAssertEqual(AppConstants.AI.promptSuggestions.count, 6)
}

func testMaxHistoryCount() {
    XCTAssertEqual(AppConstants.AI.maxHistoryCount, 20)
}
```

#### 4.4.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AIGenerationService` | ✅ 增量扩展 | 新增 prompt-based 生成方法 |
| `SubscriptionService` | ✅ 完全兼容 | Premium 校验不变 |
| `SoundPickerView` | ✅ 复用 | 生成结果存入 Imported Sounds |

#### 4.4.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `AIAlarmGeneratorView.swift` | 4 个风格卡片 | Prompt 输入 + 建议芯片 | 编译 + 手动测试 |
| `AIAlarmGeneratorView.swift` | 旧的 "Generating..." 纯文本 | WaveformView 预览 | 视觉检查 |

---

### N5：Live Activity + Dynamic Island

#### 4.5.1 需求描述

2026 闹钟类 app 必备功能：

- **响铃中 Live Activity**：锁屏显示停止/贪睡按钮
- **Dynamic Island**：灵动岛显示闹钟倒计时
- **锁屏控制**：无需解锁即可停止闹钟

#### 4.5.2 设计规则

- **新建独立模块**：`LiveActivity/` 文件夹（单一职责）
- **复用 AlarmScheduler**：触发时机复用现有通知逻辑
- **常量继承**：Activity ID 存入 `AppConstants.LiveActivity`
- **最小代码原则**：复用 WidgetExtension 的 App Group（与 N6 共享）

#### 4.5.3 代码编写规范

**1. 常量定义**：

```swift
extension AppConstants {
    enum LiveActivity {
        static let activityType = "com.zzoutuo.AlarmTune.alarm"
        static let appGroupId = "group.com.zzoutuo.AlarmTune"
    }
}
```

**2. ActivityAttributes（新建文件）**：

```swift
// LiveActivity/AlarmActivityAttributes.swift — 新建文件
import ActivityKit

struct AlarmActivityAttributes: ActivityAttributes {
    struct ContentState: Codable, Hashable {
        var alarmLabel: String
        var snoozeCount: Int
        var isRinging: Bool
    }
    var alarmId: String
}
```

**3. Widget Extension（新建 Target）**：

```swift
// LiveActivity/AlarmLiveActivity.swift — 新建文件
import ActivityKit
import WidgetKit
import SwiftUI

@available(iOS 16.1, *)
struct AlarmLiveActivity: Widget {
    var body: some WidgetConfiguration {
        ActivityConfiguration(for: AlarmActivityAttributes.self) { context in
            // 锁屏 / Dynamic Island UI
            AlarmLockScreenView(state: context.state, attributes: context.attributes)
        } dynamicIsland: { context in
            DynamicIsland {
                // 扩展态
                DynamicIslandExpandedRegion(.leading) {
                    Label(context.state.alarmLabel, systemImage: "alarm.fill")
                }
                DynamicIslandExpandedRegion(.trailing) {
                    Button {
                        // 停止闹钟
                    } label: {
                        Image(systemName: "stop.circle.fill")
                    }
                }
            } compactLeading: {
                Image(systemName: "alarm.fill")
            } compactTrailing: {
                Text("Alarm")
                    .font(.caption2)
            } minimal: {
                Image(systemName: "alarm.fill")
            }
        }
    }
}
```

#### 4.5.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Live Activity 启动 | 闹钟响铃时启动 | 手动测试 |
| Dynamic Island 显示 | 灵动岛显示闹钟 | 视觉检查 |
| 锁屏停止按钮 | 锁屏可直接停止 | 手动测试 |
| Activity 结束 | 停止闹钟后 Activity 消失 | 手动测试 |

#### 4.5.5 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AlarmScheduler` | ✅ 增量扩展 | 在 handleAlarmNotification 中启动 Activity |
| `AlarmRingView` | ✅ 兼容 | 停止闹钟时同时结束 Activity |

#### 4.5.6 清理范围

无废弃代码（全新模块）。

---

### N6：Lock Screen Widget

#### 4.6.1 需求描述

- **下一个闹钟卡片**：锁屏显示下一个闹钟时间 + 标签
- **快速启停**：点击 widget 打开 app

#### 4.6.2 设计规则

- **复用 N5 App Group**：与 Live Activity 共享数据
- **复用 AlarmItem 模型**：通过 App Group 读取下一个闹钟
- **新建 WidgetExtension Target**：与主 app 分离（单一职责）

#### 4.6.3 代码编写规范

**1. Widget 定义**：

```swift
// Widget/AlarmWidget.swift — 新建文件
import WidgetKit
import SwiftUI

struct AlarmWidget: Widget {
    let kind = "AlarmTuneWidget"
    
    var body: some WidgetConfiguration {
        StaticConfiguration(kind: kind, provider: AlarmWidgetProvider()) { entry in
            AlarmWidgetView(entry: entry)
        }
        .configurationDisplayName("Next Alarm")
        .description("Shows your next scheduled alarm")
        .supportedFamilies([.accessoryRectangular, .accessoryCircular, .accessoryInline])
    }
}

struct AlarmWidgetEntry: TimelineEntry {
    let date: Date
    let nextAlarmTime: String?
    let nextAlarmLabel: String?
}

struct AlarmWidgetProvider: TimelineProvider {
    func placeholder(in context: Context) -> AlarmWidgetEntry {
        AlarmWidgetEntry(date: Date(), nextAlarmTime: "7:00", nextAlarmLabel: "Morning")
    }
    
    func getSnapshot(in context: Context, completion: @escaping (AlarmWidgetEntry) -> Void) {
        completion(getEntry())
    }
    
    func getTimeline(in context: Context, completion: @escaping (Timeline<AlarmWidgetEntry>) -> Void) {
        let entry = getEntry()
        let timeline = Timeline(entries: [entry], policy: .after(Date().addingTimeInterval(60)))
        completion(timeline)
    }
    
    private func getEntry() -> AlarmWidgetEntry {
        // 通过 App Group 读取下一个闹钟
        let defaults = UserDefaults(suiteName: AppConstants.LiveActivity.appGroupId)
        let time = defaults?.string(forKey: "nextAlarmTime")
        let label = defaults?.string(forKey: "nextAlarmLabel")
        return AlarmWidgetEntry(date: Date(), nextAlarmTime: time, nextAlarmLabel: label)
    }
}
```

#### 4.6.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Widget 显示 | 锁屏添加后显示 | 手动测试 |
| 数据更新 | 添加/删除闹钟后刷新 | 手动测试 |
| 点击打开 app | 点击 widget 启动 app | 手动测试 |

---

### N7：任务式唤醒

#### 4.7.1 需求描述

Alarmy 核心竞争力 — 任务式唤醒防止用户迷迷糊糊关闹钟：

- **Math Mission**：解一道数学题才能停止
- **Shake Mission**：摇动手机 N 次
- **Memory Mission**：记住并输入数字序列

#### 4.7.2 设计规则

- **新建独立模块**：`Views/Missions/` 文件夹（单一职责）
- **继承 AlarmItem**：新增 `missionType` 字段（与 audioSource 平行）
- **常量继承**：Mission 类型枚举存入 `AppConstants.Mission`
- **三次法则**：Mission 协议抽象需在 3 种任务类型使用

#### 4.7.3 代码编写规范

**1. 常量定义**：

```swift
extension AppConstants {
    enum Mission {
        static let defaultMissionType: String = "none"
        static let defaultMathDifficulty: Int = 2  // 1-3
        static let defaultShakeCount: Int = 10
    }
    
    enum MissionType: String, CaseIterable, Identifiable {
        case none = "none"
        case math = "math"
        case shake = "shake"
        case memory = "memory"
        
        var id: String { rawValue }
        var displayName: String { /* ... */ }
        var icon: String { /* ... */ }
    }
}
```

**2. Mission 协议（三次法则）**：

```swift
// Views/Missions/MissionView.swift — 新建文件
import SwiftUI

protocol MissionView: View {
    var onCompleted: () -> Void { get }
    static func generate() -> Self
}

struct MathMissionView: MissionView {
    let onCompleted: () -> Void
    let a: Int
    let b: Int
    
    static func generate() -> Self {
        MathMissionView(onCompleted: {}, a: Int.random(in: 10...99), b: Int.random(in: 10...99))
    }
}
```

#### 4.7.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Math 任务 | 答对题目才能停止 | 手动测试 |
| Shake 任务 | 摇动次数达标 | 手动测试 |
| Memory 任务 | 输入正确序列 | 手动测试 |
| 跳过逻辑 | missionType=none 时直接停止 | 手动测试 |

---

### N8：搜索 + 收藏系统

#### 4.8.1 需求描述

声音/视频选择器缺少搜索和收藏：

- **统一搜索栏**：跨内置/导入/Apple Music 搜索
- **收藏持久化**：标记常用铃声/视频
- **收藏置顶**：收藏项在列表顶部

#### 4.8.2 设计规则

- **复用现有 Picker**：在 `SoundPickerView` + `VideoBackgroundPickerView` 增量添加
- **复用 UserDefaults**：收藏 ID 存入 UserDefaults（与 Shuffle 模式一致）
- **三次法则**：`SearchBar` 抽象需在声音 + 视频 + 历史记录三处使用

#### 4.8.3 代码编写规范

**1. 常量定义**：

```swift
extension AppConstants {
    enum Favorites {
        static let soundFavoritesKey = "soundFavorites"
        static let videoFavoritesKey = "videoFavorites"
    }
}
```

**2. SearchBar 组件（三次法则）**：

```swift
// Views/Components/SearchBar.swift — 新建文件
import SwiftUI

struct SearchBar: View {
    @Binding var text: String
    let placeholder: String
    
    var body: some View {
        HStack {
            Image(systemName: "magnifyingglass")
                .foregroundColor(.secondary)
            TextField(placeholder, text: $text)
                .textFieldStyle(.plain)
            if !text.isEmpty {
                Button { text = "" } label: {
                    Image(systemName: "xmark.circle.fill").foregroundColor(.secondary)
                }
            }
        }
        .padding(8)
        .background(Color.gray.opacity(0.1))
        .clipShape(RoundedRectangle(cornerRadius: 10))
    }
}
```

#### 4.8.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 搜索过滤 | 输入文字实时过滤 | 手动测试 |
| 收藏切换 | 点击心形图标切换 | 手动测试 |
| 收藏置顶 | 收藏项在列表顶部 | 视觉检查 |
| 持久化 | 重启后收藏保留 | 重启验证 |

---

### N9：Design Token 系统

#### 4.9.1 需求描述

当前 Typography 无 type scale，字号随意。需统一：

- **Type Scale**：定义 title/headline/body/caption 语义化字号
- **Spacing 系统**：统一间距 token
- **主题色继承**：所有颜色引用主题色

#### 4.9.2 设计规则

- **复用 AppConstants.Layout**：增量扩展
- **最小代码原则**：用 `ViewModifier` 而非每个 View 重复

#### 4.9.3 代码编写规范

```swift
extension AppConstants {
    enum Typography {
        static let largeTitle: CGFloat = 34
        static let title1: CGFloat = 28
        static let title2: CGFloat = 22
        static let headline: CGFloat = 17
        static let body: CGFloat = 17
        static let subheadline: CGFloat = 15
        static let footnote: CGFloat = 13
        static let caption: CGFloat = 12
    }
    
    enum Spacing {
        static let xs: CGFloat = 4
        static let sm: CGFloat = 8
        static let md: CGFloat = 12
        static let lg: CGFloat = 16
        static let xl: CGFloat = 24
        static let xxl: CGFloat = 32
    }
}
```

#### 4.9.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Type Scale | 所有 View 使用语义化字号 | Grep 验证 |
| Spacing | 所有间距使用 token | Grep 验证 |

---

### N10：Spring 动画工具集

#### 4.10.1 需求描述

当前几乎无动画，需统一：

- **卡片按压反馈**：tap 时缩放
- **过渡动画**：sheet/NavigationStack 过渡
- **微交互**：toggle/切换动画

#### 4.10.2 设计规则

- **新建 ViewModifier**：复用三次法则
- **最小代码原则**：用 `.modifier()` 应用

#### 4.10.3 代码编写规范

```swift
// Views/Components/PressableCard.swift — 新建文件
import SwiftUI

struct PressableCard: ViewModifier {
    @State private var isPressed = false
    
    func body(content: Content) -> some View {
        content
            .scaleEffect(isPressed ? 0.97 : 1)
            .animation(.spring(response: 0.3, dampingFraction: 0.7), value: isPressed)
            .onLongPressGesture(minimumDuration: 0, maximumDistance: 50, perform: {}, onPressingChanged: { pressing in
                isPressed = pressing
            })
    }
}

extension View {
    func pressableCard() -> some View {
        modifier(PressableCard())
    }
}
```

#### 4.10.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 按压反馈 | 卡片 tap 时缩放 | 视觉检查 |
| Spring 动画 | 平滑弹性 | 视觉检查 |

---

## 五、文件变更总览

| 文件 | 变更类型 | 涉及模块 |
|------|---------|---------|
| `Utilities/Constants.swift` | 修改（新增 6 个命名空间） | N1, N2, N4, N5, N7, N8, N9 |
| `Views/PaywallView.swift` | 修改（视觉升级 + FeatureRow） | N1 |
| `Views/Onboarding/OnboardingView.swift` | **新建** | N2 |
| `Views/Onboarding/PermissionRequestView.swift` | **新建** | N2 |
| `Views/AlarmRingView.swift` | 修改（Slide-to-Stop + Mesh Gradient） | N3 |
| `Views/Components/SlideToStopButton.swift` | **新建** | N3 |
| `Views/Components/MeshGradientBackground.swift` | **新建** | N3 |
| `Views/Components/GlassCard.swift` | **新建** | N1, N2 |
| `Views/Components/FeatureRow.swift` | **新建** | N1, N2 |
| `Views/Components/WaveformView.swift` | **新建** | N4 |
| `Views/Components/SearchBar.swift` | **新建** | N8 |
| `Views/Components/PressableCard.swift` | **新建** | N10 |
| `Views/AIAlarmGeneratorView.swift` | 修改（Prompt 驱动重构） | N4 |
| `Views/AlarmListView.swift` | 修改（Onboarding 判断） | N2 |
| `Views/SoundPickerView.swift` | 修改（搜索 + 收藏） | N8 |
| `Views/VideoBackgroundPickerView.swift` | 修改（搜索 + 收藏） | N8 |
| `LiveActivity/AlarmActivityAttributes.swift` | **新建** | N5 |
| `LiveActivity/AlarmLiveActivity.swift` | **新建** | N5 |
| `Widget/AlarmWidget.swift` | **新建** | N6 |
| `Views/Missions/MathMissionView.swift` | **新建** | N7 |
| `Views/Missions/ShakeMissionView.swift` | **新建** | N7 |
| `Views/Missions/MemoryMissionView.swift` | **新建** | N7 |
| `Persistence/PersistenceController.swift` | 修改（missionType 字段） | N7 |
| `Models/AlarmItem.swift` | 修改（missionType 属性） | N7 |

---

## 六、向后兼容性

| 变更 | 兼容策略 | 影响 |
|------|---------|------|
| `AlarmItem.missionType` | 默认 "none"，已有闹钟行为不变 | 无影响 |
| Onboarding storageKey | 已有用户跳过 Onboarding | 无影响 |
| Typography token | 现有字号渐进迁移 | 无影响 |
| Widget Extension | 新增 Target，不影响主 app | 无影响 |
| Live Activity | iOS 16.1+ 专属，低版本降级 | 无影响 |

---

## 七、用户使用流程（优化后）

```
1. 首次启动
   ↓
2. Onboarding 3 屏引导 → 权限请求
   ↓
3. 进入主界面（空状态优化版）
   ↓
4. 添加闹钟 → 选择声音/视频/Mission
   ↓
5. 闹钟响铃：
   - Mesh Gradient + 粒子背景
   - Slide-to-Stop（Apple Clock 习惯）
   - 贪睡计数显示
   - Dynamic Island 显示状态
   - 锁屏 Live Activity 可直接停止
   ↓
6. Mission 任务（可选）：
   - Math/Shake/Memory 完成后才能停止
   ↓
7. Paywall 升级：
   - Free Trial 徽章
   - Social Proof
   - 对比表
   - Glassmorphism 视觉
```

---

## 八、与现有代码的兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AlarmScheduler` | ✅ 增量扩展 | 新增 Live Activity 启动 |
| `AudioService` | ✅ 完全兼容 | 不修改 |
| `VideoBackgroundView` | ✅ 完全兼容 | 不修改 |
| `AlarmEditView` | ✅ 增量添加 | 新增 Mission 选择器 |
| `SubscriptionService` | ✅ 完全兼容 | 不修改 |
| `AppConstants.Theme` | ✅ 继承 | 所有新组件使用主题色 |
| `PersistenceController` | ✅ 增量扩展 | 新增 missionType 字段 |
| `HapticService` | ✅ 复用 | 已有方法足够 |

---

## 九、实施优先级与里程碑

### 第一周（P0 核心 — 转化与留存）

| 顺序 | 模块 | 工时 | 验收标准 |
|------|------|------|---------|
| 1 | N9 Design Token | 1天 | Typography + Spacing token 定义完成 |
| 2 | N1 Paywall 优化 | 1天 | Free Trial + Social Proof + 对比表 |
| 3 | N2 Onboarding | 2天 | 3 屏引导 + 权限请求 |
| 4 | N3 响铃页升级 | 2天 | Slide-to-Stop + Mesh Gradient |
| 5 | N4 AI 重构 | 1天 | Prompt 驱动 + 波形预览 |

**第一周验收**：美国市场符合度从 60% → 75%

### 第二周（P0 平台深度）

| 顺序 | 模块 | 工时 | 验收标准 |
|------|------|------|---------|
| 6 | N10 Spring 动画 | 1天 | PressableCard + 过渡动画 |
| 7 | N5 Live Activity | 3天 | Dynamic Island + 锁屏控制 |
| 8 | N6 Widget | 2天 | Lock Screen Widget |

**第二周验收**：美国市场符合度从 75% → 82%

### 第三周（P1 差异化）

| 顺序 | 模块 | 工时 | 验收标准 |
|------|------|------|---------|
| 9 | N7 任务式唤醒 | 2天 | Math/Shake/Memory 三种任务 |
| 10 | N8 搜索 + 收藏 | 2天 | 声音/视频统一搜索 |

**第三周验收**：美国市场符合度从 82% → 87%

---

## 十、风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|---------|
| Live Activity 兼容性 | 🟡 中 | iOS 16.1+ 专属，低版本降级为普通通知 |
| Widget Extension 复杂度 | 🟡 中 | 复用 N5 App Group，减少配置工作 |
| AI 真实生成成本 | 🔴 高 | N4 暂保留预设模式，改名避免误导；真实 AI 留待后续 |
| Mission 任务误触发 | 🟡 中 | missionType 默认 "none"，用户主动选择 |
| Typography 迁移工作量 | 🟡 中 | 渐进迁移，新代码用 token，旧代码暂保留 |

---

## 十一、检验与测试策略

### 11.1 单元测试

每个模块的常量、枚举、数据模型必须有单元测试：
- `AppConstants.Subscription.premiumFeatures` 数量
- `AppConstants.Onboarding.slides` 数量
- `AppConstants.AI.promptSuggestions` 数量
- `AlarmItem.missionType` 默认值
- 收藏持久化读写

### 11.2 UI 测试

关键流程必须有 UI 测试：
- Onboarding 3 屏滑动 + 完成
- Paywall 购买流程（沙盒）
- 响铃页 Slide-to-Stop
- Mission 任务完成
- 搜索过滤
- 收藏切换

### 11.3 兼容性测试

- iPhone 16 (iOS 18.4)
- iPad Pro 13" M5 (iOS 26.4)
- 低版本 iOS（Live Activity 降级）

---

## 十二、提交规范

每个模块独立提交，提交信息注明清理范围：

```
feat: N1 — Paywall conversion optimization (Free Trial + Social Proof)

- Add AppConstants.Subscription namespace with premiumFeatures list
- Add GlassCard + FeatureRow reusable components
- Upgrade PaywallView with Free Trial badge, social proof, comparison table
- Reuse FeatureRow in 3 places (Paywall + Onboarding + Settings)

Cleanup:
- Removed 3 inline benefit rows in PaywallView (replaced by FeatureRow)
- Removed plain white card background (replaced by GlassCard)

Tested on iPhone 16 + iPad Pro 13" M5
```

---

## 十三、验收标准

### 13.1 模块验收

每个模块完成后需通过：
1. ✅ 编译通过（0 errors, 0 warnings）
2. ✅ 单元测试通过
3. ✅ UI 测试通过
4. ✅ iPhone 16 + iPad Pro 13" M5 双端运行无错误
5. ✅ 与现有功能无退化
6. ✅ 废弃代码已清理
7. ✅ 提交信息符合规范

### 13.2 整体验收

全部模块完成后：
1. ✅ 美国市场符合度 ≥ 85%
2. ✅ 所有 P0 项已落地
3. ✅ 所有 P1 项已落地或明确延期理由
4. ✅ 无废弃代码残留
5. ✅ 无硬编码（全部使用 AppConstants）
6. ✅ Typography + Spacing token 全覆盖
7. ✅ 动画一致性（Spring 物理）

---

## 十四、总结

本计划基于 2026 美国市场 Gen Z 用户审计，将 AlarmTune 从"功能齐全但包装过时"升级为"美国 Gen Z 愿意付费的现代化闹钟"。

**核心改进**：
- **P0（第一周）**：Paywall 转化 + Onboarding + 响铃页 + AI 重构 — 解决留存与转化
- **P0（第二周）**：Live Activity + Widget — 解决平台深度
- **P1（第三周）**：Mission + 搜索收藏 — 解决差异化与易用性

**六大原则落地**：
1. ✅ 每个模块有需求描述 + 生成规则 + 检验标准 + 代码示例 + 测试用例
2. ✅ 功能模块原则（N1-N10 单一职责，文件结构语义化）
3. ✅ 代码复用（GlassCard/FeatureRow/WaveformView/SearchBar 三次法则）
4. ✅ 重构清理（每个模块注明废弃代码清理范围）
5. ✅ 与现有风格一致（主题色继承、Typography 渐进迁移）
6. ✅ 优先继承（Settings 主题色、AlarmScheduler 通知、SubscriptionService 订阅）

**预期成果**：
- 美国市场符合度：60% → 87%
- 订阅转化率：+30%（Free Trial + Social Proof）
- 新用户留存：+20%（Onboarding）
- App Store 评分：+0.5 星（现代化 UI + 平台深度集成）
