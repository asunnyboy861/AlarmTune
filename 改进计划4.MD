# AlarmTune 视频闹铃体验改进计划

> 基于：视频闹铃预览与音效调节缺口分析
> 版本：v1.1 | 日期：2026-07-04（实施完成 + P1 修复）

---

## 实施结果总结

| 模块 | 状态 | 测试结果 |
|------|------|---------|
| V1 视频缩略图生成 | ✅ 完成 | 内置视频卡片显示首帧缩略图 |
| V2 视频预览播放 | ✅ 完成 | 5秒自动停止，视频+铃声同步预览 |
| V3 视频音量独立控制 | ✅ 完成 | 默认 0（静音），向后兼容，复用 VolumeSliderView |
| V4 编辑页组合预览 | ✅ 完成 | AlarmPreviewView 组件，5秒倒计时 |
| V5 废弃代码清理 | ✅ 完成 | 删除 VolumePreset 死代码，条件化 isMuted |
| P1 修复 | ✅ 完成 | AlarmPreviewView.onDisappear 停止音频 |

**测试验证**：
- iPhone 16 (iOS 18.4): build 0 errors, UI 测试 8/8 pass, 单元测试 1/1 pass
- iPad Pro 13" M5 (iOS 26.4): UI 测试 8/8 pass
- Bundle: 4 .mp4 + 30 .caf + 正确权限描述
- Git commits: `d864e30` (V1-V5), `cc24fbf` (P1 fix)

---

## 一、现状诊断

| 维度 | 当前状态 | 竞品基准（Alarmy/Sleep Cycle） | 差距 |
|------|---------|-------------------------------|------|
| 视频预览 | 无（蓝色占位矩形 + play 图标） | 缩略图 + 点击预览播放 | 严重缺失 |
| 视频+铃声组合预览 | 无 | 视频画面+铃声同步预览 | 严重缺失 |
| 视频音量控制 | 强制静音（`isMuted = true`） | 独立音量滑块 | 缺失 |
| 内置视频缩略图 | 无（纯色占位卡片） | 首帧截图 | 缺失 |
| 编辑页预览入口 | 无（仅文字 "Video Background: Sunrise"） | play 按钮预览 | 缺失 |

**核心问题**：用户选择视频后"看不到效果、听不到音效"，无法在保存前确认视频画面+铃声+音量的组合效果。铃声系统已有完整的预览+音量调节，视频系统缺少对等体验。

---

## 二、改进模块总览

按功能模块划分，每个模块单一职责、高内聚低耦合：

| 模块编号 | 模块名称 | 优先级 | 说明 |
|---------|---------|--------|------|
| V1 | 视频缩略图生成 | P1 | 为内置视频生成首帧缩略图，复用导入视频的 `AVAssetImageGenerator` 模式 |
| V2 | 视频预览播放 | P1 | 视频选择器中点击播放按钮预览视频+铃声，复用 SoundRow 的 onPlay/onSelect 双回调模式 |
| V3 | 视频音量独立控制 | P2 | 新增 `videoVolume` 属性，响铃时视频音量独立于铃声音量，复用 VolumeSliderView |
| V4 | 编辑页组合预览 | P2 | Video Background 行添加预览按钮，同步播放视频+铃声，复用 AudioService.previewSound |
| V5 | 废弃代码清理 | P1 | 清理被替换的静态占位卡片、静音硬编码、重复的预设定义 |

---

## 三、模块详细设计

---

### V1：视频缩略图生成

#### 3.1 需求描述

当前 `BuiltInVideoCard` 是纯蓝色占位矩形，4 个内置视频视觉完全一样。导入视频的 `ImportedVideoRow` 已有 `thumbnailImageData`（`AVAssetImageGenerator` 生成）。需要为内置视频也生成缩略图，统一视频卡片的视觉体验。

#### 3.2 设计规则

- **复用优先**：复用 `VideoImportService` 中已有的 `AVAssetImageGenerator` 缩略图生成逻辑，提取为共享方法
- **懒加载**：内置视频缩略图在 `VideoBackgroundPickerView.onAppear` 时生成并缓存，不阻塞首屏
- **数据结构统一**：内置视频的缩略图数据结构与导入视频一致（`Data?`），方便后续统一渲染
- **最小代码**：不引入新图片资源，完全从 .mp4 文件动态生成

#### 3.3 代码编写规范

**扩展 `VideoBackgroundInfo`，添加可选缩略图字段**：

```swift
// 在 VideoBackgroundService.swift 中修改

struct VideoBackgroundInfo: Identifiable, Hashable {
    let id: String
    let title: String
    let videoName: String
    var thumbnailData: Data?  // V1 新增：懒加载缩略图
}
```

**在 `VideoBackgroundService` 中新增缩略图生成方法**：

```swift
// VideoBackgroundService.swift

/// 为所有内置视频生成缩略图（从 AVAssetImageGenerator 提取首帧）
/// 复用 VideoImportService.generateThumbnail() 的逻辑
@MainActor
func generateThumbnails() {
    for index in builtInVideos.indices {
        guard builtInVideos[index].thumbnailData == nil else { continue }
        guard let url = urlForVideo(builtInVideos[index].videoName) else { continue }

        // 复用 VideoImportService 的缩略图尺寸（320x180，与导入视频一致）
        Task {
            if let data = await Self.generateThumbnailData(from: url) {
                builtInVideos[index].thumbnailData = data
            }
        }
    }
}

/// 从视频 URL 生成缩略图 Data（与 VideoImportService 共享逻辑）
/// 提取为 static 方法，VideoImportService 也可调用
static func generateThumbnailData(from url: URL, at time: CMTime = .zero) async -> Data? {
    let asset = AVURLAsset(url: url)
    let generator = AVAssetImageGenerator(asset: asset)
    generator.appliesPreferredTrackTransform = true
    generator.maximumSize = CGSize(width: 320, height: 180)  // 与导入视频一致

    do {
        let cgImage = try generator.copyCGImage(at: time, actualTime: nil)
        let uiImage = UIImage(cgImage: cgImage)
        return uiImage.jpegData(compressionQuality: 0.7)
    } catch {
        return nil
    }
}
```

**在 `VideoImportService` 中复用共享方法**：

```swift
// VideoImportService.swift — 修改现有的缩略图生成逻辑

// 旧代码（将被替换）：
// private func generateThumbnail(for url: URL) -> Data? { ... }

// 新代码：调用 VideoBackgroundService 的共享方法
private func generateThumbnail(for url: URL) -> Data? {
    // 同步版本，用于 init 时的 refreshImportedVideos
    let asset = AVURLAsset(url: url)
    let generator = AVAssetImageGenerator(asset: asset)
    generator.appliesPreferredTrackTransform = true
    generator.maximumSize = CGSize(width: 320, height: 180)

    do {
        let cgImage = try generator.copyCGImage(at: .zero, actualTime: nil)
        return UIImage(cgImage: cgImage).jpegData(compressionQuality: 0.7)
    } catch {
        return nil
    }
}
```

> **注意**：`VideoImportService` 的 `generateThumbnail` 保持同步调用（因为 `refreshImportedVideos` 在 init 时同步执行）。`VideoBackgroundService` 使用异步版本（因为 `generateThumbnails` 在 onAppear 时调用，不阻塞 UI）。两者共享相同的参数（320x180、0.7 JPEG 压缩），确保缩略图风格一致。

**更新 `BuiltInVideoCard` 渲染缩略图**：

```swift
// VideoBackgroundPickerView.swift — BuiltInVideoCard

// 旧代码：
// ZStack {
//     RoundedRectangle(cornerRadius: 12)
//         .fill(Color.blue.opacity(0.15))
//         .frame(width: cardWidth, height: cardHeight)
//     Image(systemName: "play.rectangle.fill")
//         .font(.system(size: 32))
//         .foregroundColor(.blue.opacity(0.6))
// }

// 新代码：与 ImportedVideoRow 的缩略图渲染风格对齐
ZStack {
    RoundedRectangle(cornerRadius: 12)
        .fill(Color.gray.opacity(0.15))
        .frame(width: cardWidth, height: cardHeight)

    if let data = video.thumbnailData,
       let image = UIImage(data: data) {
        Image(uiImage: image)
            .resizable()
            .scaledToFill()
            .frame(width: cardWidth, height: cardHeight)
            .clipShape(RoundedRectangle(cornerRadius: 12))
    } else {
        // 降级：无缩略图时显示图标
        Image(systemName: "play.rectangle.fill")
            .font(.system(size: 32))
            .foregroundColor(.secondary)
    }
}
```

**在 `VideoBackgroundPickerView.onAppear` 中触发缩略图生成**：

```swift
// VideoBackgroundPickerView.swift

.onAppear {
    VideoBackgroundService.shared.generateThumbnails()
}
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 缩略图生成 | 4 个内置视频均有 thumbnailData | 手动测试：打开视频选择器，等待 1s 后缩略图显示 |
| 缩略图尺寸 | 与导入视频一致（320x180） | 对比内置/导入视频卡片视觉 |
| 降级显示 | 缩略图生成失败时显示图标而非空白 | 断点模拟生成失败 |
| 懒加载 | 不阻塞首屏渲染 | Instruments Time Profiler |
| 缩略图缓存 | 同一视频不重复生成 | 二次打开选择器时缩略图即时显示 |

#### 3.5 测试用例

```swift
// AlarmTuneTests.swift

func testBuiltInVideoThumbnailGeneration() async {
    let service = VideoBackgroundService.shared
    await service.generateThumbnails()
    for video in service.builtInVideos {
        // 不强制要求成功（视频文件可能不存在），但方法不应崩溃
        // 在实际设备/模拟器上应全部生成成功
    }
}

func testThumbnailDataFromVideoURL() async {
    // 验证共享的缩略图生成方法
    guard let url = VideoBackgroundService.shared.urlForVideo("sunrise") else {
        XCTSkip("sunrise.mp4 not found in bundle")
    }
    let data = await VideoBackgroundService.generateThumbnailData(from: url)
    XCTAssertNotNil(data, "Should generate thumbnail from sunrise.mp4")
    XCTAssertGreaterThan(data!.count, 0)
}
```

---

### V2：视频预览播放

#### 3.1 需求描述

当前用户选择视频后无法预览效果。需要在视频选择器中添加预览功能，让用户在保存前看到视频画面并听到铃声。这对应铃声系统中 `SoundRow` 的 `onPlay` 预览模式。

#### 3.2 设计规则

- **模式复用**：复用 `SoundRow` 的 onPlay/onSelect 双回调模式，视频行/卡片点击 = 选中，播放按钮 = 预览
- **组合预览**：预览视频时同步播放当前选择的铃声，让用户听到"视频+铃声"的组合效果
- **自动停止**：预览播放 5 秒后自动停止（闹钟视频不需要长时间预览）
- **状态管理**：预览状态由 `VideoBackgroundPickerView` 统一管理（当前预览的视频ID），避免多视频同时播放

#### 3.3 代码编写规范

**扩展 `VideoBackgroundPickerView`，添加预览状态和方法**：

```swift
// VideoBackgroundPickerView.swift

// 新增状态
@State private var previewingVideoId: String? = nil  // 当前正在预览的视频标识
@State private var previewPlayer: AVQueuePlayer? = nil  // 预览用的 AVPlayer
@State private var previewLooper: AVPlayerLooper? = nil
@State private var previewTimer: Timer? = nil  // 5秒自动停止

// 新增绑定：外部传入当前铃声名，预览时同步播放
@Binding var previewSoundName: String
@Binding var previewVolume: Float

/// 开始预览视频+铃声
private func startPreview(videoId: String) {
    // 停止之前的预览
    stopPreview()

    previewingVideoId = videoId

    // 1. 播放视频
    guard let url = resolveVideoURL(videoId) else { return }
    let item = AVPlayerItem(url: url)
    let player = AVQueuePlayer(playerItem: item)
    player.isMuted = true  // 预览时仍静音，铃声由 AudioService 播放
    let looper = AVPlayerLooper(player: player, templateItem: item)
    player.play()
    previewPlayer = player
    previewLooper = looper

    // 2. 同步播放铃声
    AudioService.shared.previewSound(soundName: previewSoundName, volume: previewVolume)

    // 3. 5秒后自动停止
    previewTimer = Timer.scheduledTimer(withTimeInterval: 5.0, repeats: false) { _ in
        stopPreview()
    }
}

/// 停止预览
private func stopPreview() {
    previewPlayer?.pause()
    previewPlayer = nil
    previewLooper = nil
    previewTimer?.invalidate()
    previewTimer = nil
    previewingVideoId = nil

    // 停止铃声预览
    if AudioService.shared.isPlaying {
        AudioService.shared.stopAlarm()
    }
}

/// 解析视频 URL（复用 VideoBackgroundView 的 resolvedURL 逻辑）
private func resolveVideoURL(_ videoId: String) -> URL? {
    if videoId.hasPrefix("videoBuiltIn:") {
        let name = String(videoId.dropFirst("videoBuiltIn:".count))
        return VideoBackgroundService.shared.urlForVideo(name)
    } else if videoId.hasPrefix("videoImported:") {
        let name = String(videoId.dropFirst("videoImported:".count))
        let mp4URL = VideoImportService.shared.importedDir.appendingPathComponent("\(name).mp4")
        if FileManager.default.fileExists(atPath: mp4URL.path) { return mp4URL }
        let movURL = VideoImportService.shared.importedDir.appendingPathComponent("\(name).mov")
        if FileManager.default.fileExists(atPath: movURL.path) { return movURL }
        return mp4URL
    }
    return nil
}
```

**更新 `BuiltInVideoCard`，添加预览按钮**：

```swift
// VideoBackgroundPickerView.swift — BuiltInVideoCard

// 新增参数
let isPreviewing: Bool
let onPreview: () -> Void

// 在卡片右上角添加预览播放按钮（与 SoundRow 的 play 按钮风格一致）
ZStack {
    // ... 缩略图渲染 ...

    // 预览播放按钮（底部居中）
    if !isSelected {
        Button {
            onPreview()
            HapticService.shared.selection()
        } label: {
            Image(systemName: isPreviewing ? "stop.circle.fill" : "play.circle.fill")
                .font(.system(size: 28))
                .foregroundColor(.white)
                .shadow(color: .black.opacity(0.3), radius: 2)
        }
        .buttonStyle(.plain)
    }

    // 选中态覆盖（保留现有的 checkmark）
    if isSelected { ... }
}
```

**更新 `ImportedVideoRow`，添加预览按钮**：

```swift
// VideoBackgroundPickerView.swift — ImportedVideoRow

// 新增参数
let isPreviewing: Bool
let onPreview: () -> Void

// 在行右侧添加预览按钮（复用 SoundRow 的 play.circle.fill 图标风格）
// 在 isSelected checkmark 旁边添加：
Button {
    onPreview()
    HapticService.shared.selection()
} label: {
    Image(systemName: isPreviewing ? "stop.circle.fill" : "play.circle.fill")
        .font(.system(size: 22))
        .foregroundColor(.accentColor.opacity(0.7))
}
.buttonStyle(.plain)
```

**`VideoBackgroundPickerView` 调用处传入预览参数**：

```swift
// VideoBackgroundPickerView.swift — builtInVideosSection

ForEach(VideoBackgroundService.shared.builtInVideos) { video in
    BuiltInVideoCard(
        video: video,
        isSelected: selectedVideo == "videoBuiltIn:\(video.id)",
        isPreviewing: previewingVideoId == "videoBuiltIn:\(video.id)",
        onSelect: {
            stopPreview()
            selectedVideo = "videoBuiltIn:\(video.id)"
            HapticService.shared.selection()
            dismiss()
        },
        onPreview: {
            startPreview(videoId: "videoBuiltIn:\(video.id)")
        }
    )
}
```

**修改 `VideoBackgroundPickerView` 的初始化签名**：

```swift
// VideoBackgroundPickerView.swift

struct VideoBackgroundPickerView: View {
    @Binding var selectedVideo: String?
    @Binding var previewSoundName: String   // V2 新增：当前铃声名
    @Binding var previewVolume: Float       // V2 新增：当前铃声音量
    // ... 其他属性不变 ...
}
```

**`AlarmEditView` 传入铃声参数**：

```swift
// AlarmEditView.swift

// 旧代码：
// .sheet(isPresented: $showVideoPicker) {
//     VideoBackgroundPickerView(selectedVideo: $videoBackgroundName)
// }

// 新代码：
.sheet(isPresented: $showVideoPicker) {
    VideoBackgroundPickerView(
        selectedVideo: $videoBackgroundName,
        previewSoundName: $soundName,
        previewVolume: $volume
    )
    // ...
}
```

**视图消失时停止预览**：

```swift
// VideoBackgroundPickerView.swift

.onDisappear {
    stopPreview()
}
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 预览触发 | 点击视频卡片上的播放按钮开始预览 | 手动测试 |
| 组合预览 | 预览时视频画面+铃声同步播放 | 手动测试 |
| 自动停止 | 预览 5 秒后自动停止 | 手动计时 |
| 手动停止 | 再次点击播放按钮停止预览 | 手动测试 |
| 选择停止 | 选中视频时自动停止预览并 dismiss | 手动测试 |
| 离开停止 | 离开选择器页面时停止预览 | 手动测试 |
| 多视频互斥 | 同时只能预览一个视频 | 连续点击不同视频 |

#### 3.5 测试用例

```swift
// AlarmTuneUITests — 新增测试

func testVideoPreviewButton() throws {
    let app = XCUIApplication()
    app.launch()

    // 导航到 Video Background Picker
    navigateToAddAlarm(app)
    // ... 打开 Video Background Picker ...

    // 验证内置视频卡片上有播放按钮
    let playButton = app.buttons.matching(
        NSPredicate(format: "label CONTAINS 'play.circle.fill'")
    ).firstMatch
    XCTAssertTrue(playButton.waitForExistence(timeout: 3), "Play button should exist on video card")
}
```

---

### V3：视频音量独立控制

#### 3.1 需求描述

当前视频被强制静音（`VideoBackgroundView` 中 `isMuted = true`）。如果用户导入了一段带音效的个性化视频（海浪声、鸟鸣声等），视频原始音轨无法使用。需要新增独立的视频音量控制，让用户决定视频是否发声以及音量大小。

#### 3.2 设计规则

- **数据继承**：新增 `videoVolume` 属性到 `AlarmItem`（Core Data 编程式模型），与 `volume` 平行，默认值 0（静音，保持向后兼容）
- **UI 复用**：复用 `VolumeSliderView` 组件，仅在选择了视频背景时显示
- **音量架构**：`VideoBackgroundView` 使用 `AVPlayer.volume` 控制视频音量，与 `AVAudioPlayer.volume`（铃声）独立
- **向后兼容**：`videoVolume` 默认为 0（静音），已保存的闹钟无需迁移，行为与当前完全一致
- **常量继承**：默认值定义在 `AppConstants.Alarm` 中，与 `defaultVolume` 一致

#### 3.3 代码编写规范

**1. Core Data 模型扩展**：

```swift
// PersistenceController.swift — createManagedObjectModel() attributes 数组

// 新增一行：
("videoVolume", .floatAttributeType, 0.0),  // V3 新增：视频音量，默认 0（静音）

// 更新 optionalAttributes：
let optionalAttributes: Set<String> = ["id", "createdAt", "category", "repeatDays", "videoBackgroundName"]
// videoVolume 不在 optionalAttributes 中，因为有默认值 0.0
```

**2. AlarmItem 属性扩展**：

```swift
// AlarmItem.swift

@NSManaged public var videoVolume: Float  // V3 新增：视频音量 0.0...1.0，默认 0（静音）

// 在 create() 工厂方法中添加默认值：
alarm.videoVolume = 0.0  // 默认静音，向后兼容
```

**3. AppConstants 新增默认值**：

```swift
// Constants.swift — AppConstants.Alarm

static let defaultVideoVolume: Float = 0.0  // V3：视频音量默认静音
```

**4. AlarmEditView 新增 videoVolume 状态和 UI**：

```swift
// AlarmEditView.swift

@State private var videoVolume: Float = AppConstants.Alarm.defaultVideoVolume  // V3 新增

// init 中添加：
_videoVolume = State(initialValue: alarm.videoVolume)  // V3

// 在 videoBackgroundSection 的 footer 之后，当选择了视频时显示音量控制：
private var videoBackgroundSection: some View {
    Section {
        Button { ... } label: { ... }

        // V3 新增：仅当选择了视频背景时，显示视频音量滑块
        if videoBackgroundName != nil {
            VolumeSliderView(volume: $videoVolume) { vol in
                // 预览：播放当前视频音量
                // 这里只需调节 videoVolume 值，不需要额外预览回调
                // 组合预览由 V4 的预览按钮实现
            }
        }
    } header: {
        Text("Video Background")
    } footer: {
        if videoBackgroundName != nil {
            Text("Adjust video background volume. Set to 0% to keep video silent.")
                .font(.caption2)
        } else {
            Text("Optional. Video plays silently in background when alarm rings.")
                .font(.caption2)
        }
    }
}

// saveAlarm() 中添加：
existingAlarm.videoVolume = videoVolume  // V3
// addAlarm() 参数中添加：
videoVolume: videoVolume
```

**5. AlarmViewModel.addAlarm 参数扩展**：

```swift
// AlarmViewModel.swift

func addAlarm(..., videoBackgroundName: String? = nil, videoVolume: Float = 0.0) -> AlarmItem {
    // ...
    alarm.videoVolume = videoVolume  // V3
    // ...
}
```

**6. VideoBackgroundView 读取视频音量**：

```swift
// VideoBackgroundView.swift

// 旧代码：
// queuePlayer.isMuted = true

// 新代码：
// videoVolume 由外部传入，默认 0（静音）
let videoVolume: Float  // V3 新增参数

// 在 setupPlayer() 中：
if videoVolume <= 0 {
    queuePlayer.isMuted = true
} else {
    queuePlayer.isMuted = false
    queuePlayer.volume = videoVolume
}
```

**7. AlarmRingView 传入 videoVolume**：

```swift
// AlarmRingView.swift

// 旧代码：
// if let videoName = viewModel.ringingAlarm?.videoBackgroundName, !videoName.isEmpty {
//     VideoBackgroundView(videoName: videoName)
// }

// 新代码：
if let alarm = viewModel.ringingAlarm,
   let videoName = alarm.videoBackgroundName, !videoName.isEmpty {
    VideoBackgroundView(videoName: videoName, videoVolume: alarm.videoVolume)
}
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 默认静音 | 新建闹钟的视频音量为 0，行为与修改前一致 | 新建闹钟 → 触发 → 视频无声 |
| 音量调节 | 选择视频后出现音量滑块，拖动调节有效 | 设置 50% → 触发 → 视频有声音 |
| 向后兼容 | 已有闹钟（无 videoVolume 字段）默认为 0（静音） | 升级后触发旧闹钟 |
| 音量独立 | 视频音量与铃声音量独立控制 | 设置不同值 → 触发 → 两者音量不同 |
| 数据持久化 | 编辑视频音量后保存，重新打开保持 | 设置 80% → 返回 → 重入编辑页 → 显示 80% |
| VolumeSliderView 复用 | 视频音量滑块与铃声音量滑块视觉一致 | 对比两处 UI |

#### 3.5 测试用例

```swift
// AlarmTuneTests.swift

func testVideoVolumeDefault() {
    let alarm = AlarmItem.create(in: PersistenceController.shared.viewContext)
    XCTAssertEqual(alarm.videoVolume, 0.0, "Default video volume should be 0 (silent)")
}

func testVideoVolumeRange() {
    let alarm = AlarmItem.create(in: PersistenceController.shared.viewContext)
    alarm.videoVolume = 0.75
    XCTAssertTrue(alarm.videoVolume >= 0 && alarm.videoVolume <= 1.0)
}

// AlarmTuneUITests — 新增
func testVideoVolumeSlider() throws {
    let app = XCUIApplication()
    app.launch()

    // 1. 添加闹钟
    navigateToAddAlarm(app)

    // 2. 选择一个内置视频背景
    // ... 打开 Video Background Picker，选择 Sunrise ...

    // 3. 返回后验证 Video Volume 滑块出现
    let videoVolumeSlider = app.sliders.matching(
        NSPredicate(format: "label CONTAINS 'Volume'")
    ).element(boundBy: 1)  // 第二个 Slider（第一个是铃声）
    XCTAssertTrue(videoVolumeSlider.waitForExistence(timeout: 3),
                  "Video volume slider should appear after selecting a video")
}
```

---

### V4：编辑页组合预览

#### 3.1 需求描述

用户在 `AlarmEditView` 中选择了视频+铃声后，无法预览"视频画面+铃声+音量"的组合效果。需要在 Video Background 行添加预览按钮，让用户在保存闹钟前确认整体效果。

#### 3.2 设计规则

- **UI 对齐**：预览按钮放在 Video Background 行的右侧，与 Sound 行的 `chevron.right` 导航箭头并列，使用 `play.circle.fill` 图标
- **服务复用**：铃声预览复用 `AudioService.shared.previewSound()`，视频预览复用 `AVQueuePlayer`（与 V2 的 `startPreview` 逻辑共享）
- **弹窗预览**：点击预览按钮弹出 sheet，显示小窗视频 + 当前铃声播放，3 秒后自动停止或用户手动关闭
- **参数继承**：预览使用当前编辑页的 `soundName`、`volume`、`videoBackgroundName`、`videoVolume` 值

#### 3.3 代码编写规范

**新增 `AlarmPreviewView` 组件**：

```swift
// Views/Components/AlarmPreviewView.swift — 新文件

/// 闹钟组合预览组件（V4）
/// 同时播放视频背景+铃声，让用户在保存前确认整体效果
struct AlarmPreviewView: View {
    let videoName: String
    let videoVolume: Float
    let soundName: String
    let soundVolume: Float

    @State private var player: AVQueuePlayer?
    @State private var looper: AVPlayerLooper?
    @State private var countdown = 5

    @Environment(\.dismiss) private var dismiss
    private let timer = Timer.publish(every: 1, on: .main, in: .common).autoconnect()

    var body: some View {
        VStack(spacing: 20) {
            // 视频预览窗口
            VideoPlayer(player: player)
                .frame(height: 220)
                .clipShape(RoundedRectangle(cornerRadius: AppConstants.Layout.cardCornerRadius))
                .padding(.horizontal)

            // 铃声信息
            HStack(spacing: 8) {
                Image(systemName: "speaker.wave.2.fill")
                    .foregroundColor(.accentColor)
                Text(displaySoundName)
                    .font(.subheadline)
                    .foregroundColor(.secondary)
            }

            // 倒计时
            Text("Preview: \(countdown)s")
                .font(.caption)
                .foregroundColor(.secondary)

            Button("Stop Preview") {
                stopAndDismiss()
            }
            .buttonStyle(.bordered)
        }
        .padding()
        .onAppear {
            startPreview()
        }
        .onReceive(timer) { _ in
            if countdown > 1 {
                countdown -= 1
            } else {
                stopAndDismiss()
            }
        }
    }

    private var displaySoundName: String {
        if soundName.hasPrefix("imported:") {
            return String(soundName.dropFirst("imported:".count))
        } else if soundName.hasPrefix("appleMusic:") {
            return "Apple Music"
        }
        return soundName
    }

    private func startPreview() {
        // 1. 播放视频
        if let url = resolveVideoURL() {
            let item = AVPlayerItem(url: url)
            let queuePlayer = AVQueuePlayer(playerItem: item)
            queuePlayer.volume = videoVolume
            if videoVolume <= 0 { queuePlayer.isMuted = true }
            looper = AVPlayerLooper(player: queuePlayer, templateItem: item)
            queuePlayer.play()
            player = queuePlayer
        }

        // 2. 播放铃声
        AudioService.shared.previewSound(soundName: soundName, volume: soundVolume)
    }

    private func stopAndDismiss() {
        player?.pause()
        player = nil
        looper = nil
        AudioService.shared.stopAlarm()
        dismiss()
    }

    private func resolveVideoURL() -> URL? {
        if videoName.hasPrefix("videoBuiltIn:") {
            let name = String(videoName.dropFirst("videoBuiltIn:".count))
            return VideoBackgroundService.shared.urlForVideo(name)
        } else if videoName.hasPrefix("videoImported:") {
            let name = String(videoName.dropFirst("videoImported:".count))
            let mp4URL = VideoImportService.shared.importedDir.appendingPathComponent("\(name).mp4")
            if FileManager.default.fileExists(atPath: mp4URL.path) { return mp4URL }
            let movURL = VideoImportService.shared.importedDir.appendingPathComponent("\(name).mov")
            if FileManager.default.fileExists(atPath: movURL.path) { return movURL }
            return mp4URL
        }
        return nil
    }
}
```

**在 `AlarmEditView` 的 Video Background 行添加预览按钮**：

```swift
// AlarmEditView.swift — videoBackgroundSection

private var videoBackgroundSection: some View {
    Section {
        HStack {
            Button {
                showVideoPicker = true
            } label: {
                HStack {
                    Text("Video Background")
                    Spacer()
                    Text(displayVideoBackgroundName)
                        .foregroundColor(.secondary)
                        .lineLimit(1)
                    Image(systemName: "chevron.right")
                        .font(.caption)
                        .foregroundColor(.secondary)
                }
            }
            .foregroundColor(.primary)

            // V4 新增：预览按钮（仅在选择视频后显示）
            if videoBackgroundName != nil {
                Button {
                    showVideoPreview = true
                } label: {
                    Image(systemName: "play.circle.fill")
                        .font(.system(size: 22))
                        .foregroundColor(.accentColor.opacity(0.7))
                }
                .buttonStyle(.plain)
            }
        }
        .accessibilityIdentifier("videoPickerButton")

        // V3 的视频音量滑块（如果选择了视频）
        if videoBackgroundName != nil {
            VolumeSliderView(volume: $videoVolume) { vol in
                // 音量调节时的预览反馈
            }
        }
    } header: {
        Text("Video Background")
    } footer: {
        if videoBackgroundName != nil {
            Text("Adjust video background volume. Set to 0% to keep video silent.")
                .font(.caption2)
        } else {
            Text("Optional. Video plays silently in background when alarm rings.")
                .font(.caption2)
        }
    }
}
```

**新增 `showVideoPreview` 状态和 sheet**：

```swift
// AlarmEditView.swift

@State private var showVideoPreview = false  // V4 新增

// 在 body 的 sheet 列表中添加：
.sheet(isPresented: $showVideoPreview) {
    if let videoName = videoBackgroundName {
        AlarmPreviewView(
            videoName: videoName,
            videoVolume: videoVolume,
            soundName: soundName,
            soundVolume: volume
        )
        .presentationDetents([.medium])
        .presentationDragIndicator(.visible)
    }
}
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 预览入口 | 选择视频后 Video Background 行右侧出现 play 按钮 | 手动测试 |
| 组合播放 | 点击 play 弹出预览窗口，视频+铃声同步播放 | 手动测试 |
| 视频音量 | 预览中视频音量与 `videoVolume` 设置一致 | 设置 80% → 预览 → 视频有声 |
| 自动停止 | 预览 5 秒后自动关闭 | 手动计时 |
| 手动停止 | 点击 "Stop Preview" 关闭预览 | 手动测试 |
| 无视频隐藏 | 未选择视频时不显示 play 按钮 | 手动测试 |

#### 3.5 测试用例

```swift
// AlarmTuneUITests

func testVideoPreviewInAlarmEdit() throws {
    let app = XCUIApplication()
    app.launch()

    // 1. 添加闹钟并选择视频背景
    navigateToAddAlarm(app)
    // ... 选择 Sunrise 视频背景 ...

    // 2. 验证 Video Background 行出现 play 按钮
    let previewButton = app.buttons.matching(
        NSPredicate(format: "label CONTAINS 'play.circle.fill'")
    ).firstMatch
    XCTAssertTrue(previewButton.waitForExistence(timeout: 3),
                  "Preview button should appear after selecting a video")

    // 3. 点击预览
    previewButton.tap()

    // 4. 验证预览窗口出现
    let stopButton = app.buttons["Stop Preview"]
    XCTAssertTrue(stopButton.waitForExistence(timeout: 3),
                  "Preview sheet should appear with Stop button")
}
```

---

### V5：废弃代码清理

#### 3.1 需求描述

V1-V4 的实现会替换部分现有代码。需要将废弃代码标记、验证、删除，保持代码整洁。

#### 3.2 清理清单

| 文件 | 废弃代码 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `VideoBackgroundPickerView.swift` — `BuiltInVideoCard` | 蓝色占位矩形 + `play.rectangle.fill` 图标 | V1 缩略图渲染（降级时才显示图标） | 视觉检查 |
| `VideoBackgroundView.swift` | `queuePlayer.isMuted = true` 硬编码 | V3 条件判断 `videoVolume <= 0 ? isMuted : volume` | 设置 videoVolume > 0 时视频有声 |
| `AlarmEditView.swift` — `videoBackgroundSection` footer | "Video plays silently in background when alarm rings." 永久文案 | V3 动态文案：选了视频后改为 "Adjust video background volume..." | 文案检查 |
| `VideoBackgroundPickerView.swift` | `VideoBackgroundPickerView(selectedVideo:)` 单参数初始化 | V2 三参数初始化 `selectedVideo:previewSoundName:previewVolume:` | 编译检查 |
| `VolumeSliderView.swift` — `presets` 本地数组 | 与 `AlarmItem.VolumePreset` 重复定义 | 保留 `VolumeSliderView` 的本地 presets（更简洁），标记 `AlarmItem.VolumePreset` 如未使用则删除 | Grep 搜索 `VolumePreset` 引用 |

#### 3.3 代码编写规范

**清理步骤**：

1. **标记废弃**：在即将删除的代码上方添加 `// DEPRECATED: V1/V2/V3 替换` 注释
2. **验证无影响**：编译 + UI 测试 + 手动测试确认功能正常
3. **删除废弃代码**：移除标记的代码
4. **提交信息**：`refactor: clean up deprecated code — V1-V4 video preview/volume upgrade`

**具体清理**：

```swift
// VideoBackgroundView.swift — 清理 isMuted 硬编码

// 旧代码（删除）：
// queuePlayer.isMuted = true  // 视频静音，声音由 AudioService 独立播放

// 新代码（V3 已实现）：
if videoVolume <= 0 {
    queuePlayer.isMuted = true
} else {
    queuePlayer.isMuted = false
    queuePlayer.volume = videoVolume
}
```

```swift
// AlarmItem.swift — 检查 VolumePreset 是否有外部引用

// 如果 Grep 搜索发现 VolumePreset 仅在 VolumeSliderView 的 presets 之外无引用，
// 则标记为废弃并删除整个 VolumePreset enum
// 保留 VolumeSliderView 的本地 presets（更贴近 UI 逻辑）
```

#### 3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 编译通过 | 0 errors, 0 warnings | xcodebuild |
| 功能无退化 | 所有现有功能正常 | UI 测试 8/8 pass |
| 无死代码 | 无未使用的 enum/method/property | Grep 搜索 + SwiftLint |
| 无废弃注释 | 无 "DEPRECATED" 注释残留 | Grep 搜索 |

---

## 四、模块依赖关系

```
V1（缩略图）─ 无依赖，可独立实现
     │
     ▼
V2（预览播放）─ 依赖 V1（缩略图让用户识别视频）
     │
     ▼
V3（视频音量）─ 依赖 V2（预览时需要播放视频音量才能验证）
     │
     ▼
V4（组合预览）─ 依赖 V2 + V3（需要视频预览 + 音量控制都完成）
     │
     ▼
V5（代码清理）─ 依赖 V1-V4 全部完成
```

**推荐实现顺序**：V1 → V2 → V3 → V4 → V5

---

## 五、文件变更总览

| 文件 | 变更类型 | 涉及模块 |
|------|---------|---------|
| `Services/VideoBackgroundService.swift` | 修改（新增 `thumbnailData` 字段 + `generateThumbnails` 方法） | V1 |
| `Views/VideoBackgroundPickerView.swift` | 修改（缩略图渲染 + 预览播放 + 参数扩展） | V1, V2, V5 |
| `Views/Components/VideoBackgroundView.swift` | 修改（`videoVolume` 参数 + 条件静音） | V3, V5 |
| `Models/AlarmItem.swift` | 修改（新增 `videoVolume` 属性） | V3 |
| `Persistence/PersistenceController.swift` | 修改（新增 `videoVolume` 属性定义） | V3 |
| `Utilities/Constants.swift` | 修改（新增 `defaultVideoVolume` 常量） | V3 |
| `Views/AlarmEditView.swift` | 修改（videoVolume 状态 + 音量滑块 + 预览按钮） | V3, V4 |
| `ViewModels/AlarmViewModel.swift` | 修改（addAlarm 参数扩展） | V3 |
| `Views/AlarmRingView.swift` | 修改（传入 videoVolume） | V3 |
| `Views/Components/AlarmPreviewView.swift` | **新增**（组合预览组件） | V4 |

---

## 六、向后兼容性

| 变更 | 兼容策略 | 影响 |
|------|---------|------|
| `AlarmItem.videoVolume` | 默认值 0.0（静音），已保存闹钟 Core Data 自动填充默认值 | 无影响 |
| `VideoBackgroundView(videoVolume:)` | 新增参数有默认值 `0.0`，现有调用无需修改 | 无影响 |
| `VideoBackgroundPickerView(previewSoundName:previewVolume:)` | 新增参数，**必须**更新 `AlarmEditView` 中的调用 | 需同步修改 |
| `VideoBackgroundInfo.thumbnailData` | 可选 `Data?`，未生成时为 nil，降级显示图标 | 无影响 |
