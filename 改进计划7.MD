# AlarmTune 发布前可靠性与合规性修复计划

> 基于：2026-07-05 全量发布就绪审计（用户流程 / 数据流 / 功能完整性 / UI/UX / App Store 合规）
> 版本：v1.1（已根据实际代码校验修订） | 日期：2026-07-05
> 核心目标：将软件从"功能齐全但存在 4 个 Critical + 2 个 Blocker + 8 个 High"修复为"客户下载即可完美使用 + 通过 App Store 审核"
> 当前发布就绪度：约 55% → 目标：100%

---

## 本次会话修订总览（v1.4 ~ v1.6）

### v1.4 视频预览彻底修复 + 闹钟模式清晰度

**问题**：视频预览点击播放按钮只响声音，不显示视频画面；用户困惑视频闹钟和声音闹钟如何区分

**修复**：
- 创建 InlineVideoPlayer（AVPlayerLayer 替代 SwiftUI VideoPlayer，解决 Button 内不渲染问题）
- 分离卡片点击和预览按钮，避免嵌套 Button 事件冲突
- AudioSession 闪烁修复（stopPreviewOnly 不 deactivate）
- 闹钟卡片添加模式徽章（紫色 "Video" / 灰色声音名）
- 竞品调研：Alarmy 视频/声音互斥模式

### v1.5 简化视频闹钟模式 + 默认音量修复

**问题**：视频模式下显示多余的"音频来源"选择器；保存后播放音量为0%

**修复**：
- 移除 audioSource Picker（选视频自动用视频声音，符合 Alarmy 模式）
- defaultVideoVolume 从 0% 改为 55%
- 选择视频/编辑已有闹钟时修复音量为0的情况
- 简化卡片徽章和 footer 文本

### v1.6 视频闹钟 Snooze 一致性 + 后台可靠性

**问题**：视频闹钟 Snooze 后变成音乐闹钟；询问 App 被 kill 后是否还能响

**修复**：
- prepareForVideoAlarm()：AudioSession 激活 + Background Task 启动
- endVideoAlarmBackgroundTask()：停止时释放 Background Task
- videoSound 闹钟用 .defaultCritical 通知声（关键闹钟，静音模式也能响）
- Snooze/Stop 时释放 Background Task

**后台 kill 答复**：能响，但 iOS 系统限制——内置铃声完整响，Apple Music/导入降级为系统默认声，视频闹钟只有关键闹钟声（点开通知后播视频）

---

## v1.1 修订说明（基于实际代码校验）

经与实际代码逐行校验，发现 v1.0 中以下代码示例与真实代码不符，已在实施时修正：

| 模块 | v1.0 错误 | v1.1 修正 |
|------|----------|----------|
| M1 | 引用 `AppConstants.URL.privacyURL`（不存在的子命名空间） | 实际为 `AppConstants.privacyURL`（直接挂在 AppConstants 下） |
| M1 | 标注 SafariView 在 SettingsView.swift:265-286 | 实际在 SettingsView.swift:332-342 |
| M2 | 提议 `com.apple.developer.in-app-payments` 数组列出 product ID | 此 key 是 Apple Pay merchant ID 用的；App Store IAP 只需 `com.apple.developer.in-app-purchase: true` |
| M3 | 提议新建 `AlarmReliabilityService` 与 `playAlarm(6 params)` | AudioService 已有 `beginBackgroundTask` + `.playback` 配置；实际 `playAlarm(soundName:volume:fadeIn:fadeInDuration:)` 仅 4 参数。改为修复 dismiss action + 提取复用方法 |
| M3 | 称"iOS 会因通知交付唤醒被杀死的 App" | 这是 iOS 平台限制，App 被杀死时仅播放通知声音（最长 30s），不唤醒进程 |
| M4 | 引用 `VolumeManager.shared.boostSystemVolume(targetVolume:)` | 实际方法为 `boostSystemVolume(to:)`（Apple Music 模式）和 `boostSystemVolume(forAlarmVolume:)`（本地音频模式） |
| M5 | 引用 `playLocalSound(soundName:volume:fadeIn:)` 和 `playAppleMusicSong(persistentID:volume:)` | 实际为 `playLocalSound(name:volume:fadeIn:fadeInDuration:)` 和 `playAppleMusicSound(identifier:volume:fadeIn:fadeInDuration:)` |
| M7 | YAML 示例使用 `resources:` 顶级 key | 实际 project.yml 使用 `sources:` + `buildPhase: resources` |
| M9 | `dynamicFont` 仅包装 `font(.system(size:))` 不会启用 Dynamic Type | 改为使用 `@ScaledMetric` + `UIFontMetrics`，或直接使用语义化字体 `.font(.body)` 等 |
| M11 | 提议新建 `MPVolumeView` 每次 findVolumeSlider | 实际 VolumeManager 已有 `volumeView` 属性添加到窗口，应复用并加重试 |
| M3/M5 测试 | 使用 `AlarmItem.create(hour:minute:)` | 实际为 `AlarmItem.create(in: context)` |

>
> 与改进计划6 的关系：
> - 改进计划6 聚焦"UI/UX 现代化与平台深度集成"（N1-N10：Paywall 转化、Onboarding、响铃交互、AI 重构、Live Activity、Widget、任务唤醒、搜索收藏、Design Token、Spring 动画）
> - 改进计划7 聚焦"发布前可靠性与合规性修复"（M1-M12：闹钟可靠性、App Store 合规、无障碍、健壮性）
> - 两份计划互补不冲突：改进计划7 优先执行（阻断发布），改进计划6 后续执行（提升体验）
>
> 六大原则：
> 1. 需求描述 + 生成规则 + 检验标准（代码示例 + 测试用例 + 注释）
> 2. 功能模块原则（单一职责、高内聚低耦合、命名清晰、文件结构语义化）
> 3. 代码复用（优先复用现有模块、三次法则、最小代码原则）
> 4. 重构清理（标记废弃 → 验证无影响 → 删除；提交信息注明清理范围）
> 5. 与当前软件风格一致（格调、功能、UI 与现有版本一致）
> 6. 优先继承而非硬编码（特别是 Settings 中，与现有代码兼容）

---

## 一、现状诊断回顾

### 1.1 审计发现汇总

| 严重等级 | 数量 | 影响 |
|---------|------|------|
| 🔴 Critical（阻断客户使用） | 4 | 闹钟不响、静音失效、视频音量、静默失败 |
| 🚫 Blocker（阻断 App Store 审核） | 2 | Paywall 法律链接、IAP 权限缺失 |
| 🟠 High（影响体验和可靠性） | 8 | Apple Music 检查、project.yml、Core Data、Dynamic Type、Onboarding、无障碍、音量健壮性 |
| 🟡 Medium（体验差距） | 11 | 各种 UX 和配置问题 |

### 1.2 与改进计划6 的分工

| 问题 | 改进计划6 覆盖？ | 改进计划7 处理？ | 说明 |
|------|-----------------|-----------------|------|
| H3 AI 命名误导 | ✅ N4 已规划 | ❌ 不重复 | N4 重构为真实 Prompt 驱动 |
| H6 无新手引导 | ✅ N2 已规划 | ❌ 不重复 | N2 实现 3 屏引导 |
| B1 Paywall 法律链接 | ❌ N1 仅做转化 | ✅ M1 增量补链接 | N1 升级 UI，M1 补合规 |
| C1-C4 闹钟可靠性 | ❌ 未涉及 | ✅ M3-M5 核心 | 改进计划6 未覆盖可靠性 |
| H5 Dynamic Type | ❌ N9 Design Token 部分涉及 | ✅ M9 系统化 | M9 聚焦无障碍而非视觉 |

---

## 二、改进模块总览

按功能模块划分，每个模块单一职责、高内聚低耦合。优先级按"阻断发布 → 影响体验"排序。

| 模块编号 | 模块名称 | 优先级 | 预估工时 | 模块职责 | 对应审计项 |
|---------|---------|--------|---------|---------|-----------|
| M1 | PaywallView 法律链接合规 | P0 | 0.5天 | 隐私政策 + 使用条款可点击链接 | B1 |
| M2 | IAP Entitlements + Info.plist 合规 | P0 | 0.5天 | IAP 能力声明 + 加密声明 + 分类 | B2 + 中优先级 |
| M3 | 闹钟后台可靠性引擎 | P0 | 3天 | 后台/杀死状态响铃 + 静音模式绕过 | C1 + C2 |
| M4 | Video Sound 音量提升修复 | P0 | 0.5天 | 视频音频模式系统音量提升 | C3 |
| M5 | 音频资源完整性防护 | P0 | 1天 | 文件丢失检测 + 用户提示 + 回退 | C4 |
| M6 | Apple Music 订阅前置检查 | P1 | 0.5天 | 选歌前检查订阅状态 | H1 |
| M7 | project.yml 资源同步 | P1 | 0.5天 | Videos 文件夹纳入构建 + 构建配置 | H2 |
| M8 | Core Data 崩溃恢复 | P1 | 0.5天 | fatalError 改为优雅降级 | H4 |
| M9 | Dynamic Type 系统化 | P1 | 1.5天 | 硬编码字号改为 @ScaledMetric | H5 |
| M10 | 无障碍标签补全 | P1 | 1天 | 图标按钮 accessibilityLabel | H7 |
| M11 | 系统音量提升健壮性 | P1 | 1天 | MPVolumeView 健壮查找 + 兜底 | H8 |
| M12 | 杂项中优先级修复 | P2 | 1天 | NavigationView 迁移 + Restore 位置 + 视频时长等 | P2 批量 |

**总预估工时**：约 11.5 个工作日（约 2.5 周）

---

## 三、模块依赖关系

```
M2（IAP 合规）─ 独立，先做（解锁订阅测试）
     │
     ▼
M1（Paywall 法律链接）─ 依赖 M2 IAP 能力生效
     │
     ▼
M3（闹钟后台可靠性）─ 核心模块，独立重构
     │
     ├── M4（Video Sound 音量）─ 依赖 M3 的音频会话统一管理
     └── M5（音频资源防护）─ 依赖 M3 的播放流程
           │
           ▼
M11（系统音量健壮性）─ 依赖 M3/M4 的音量提升调用点
     │
     ▼
M6（Apple Music 检查）─ 独立
M7（project.yml 同步）─ 独立，配置修复
M8（Core Data 崩溃恢复）─ 独立
     │
     ▼
M9（Dynamic Type）─ 横切，影响所有 View
M10（无障碍标签）─ 横切，影响所有 View
     │
     ▼
M12（杂项修复）─ 收尾
```

**推荐实施顺序**：
- **第一周（P0 阻断）**：M2 → M1 → M3 → M4 → M5
- **第二周（P1 健壮性）**：M11 → M6 → M7 → M8 → M9 → M10
- **第三周收尾**：M12

---

## 四、模块详细设计

---

### M1：PaywallView 法律链接合规

#### 4.1.1 需求描述

当前 [PaywallView.swift:236-242](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Views/PaywallView.swift#L236-L242) 的 `legalText` 仅包含自动续费说明文字，**无可点击的隐私政策和使用条款链接**。

Apple Guideline 3.1.2 明确要求：订阅页面必须在订阅按钮下方提供 EULA（Terms of Use）和 Privacy Policy 的可点击链接。当前状态提交 App Store 必被拒。

**与改进计划6 N1 的关系**：N1 负责 Paywall 的转化率优化（Free Trial 徽章、Social Proof、对比表、Glassmorphism 视觉）。M1 仅负责补齐法律链接合规，是 N1 的前置依赖（先合规再优化转化）。

#### 4.1.2 设计规则

- **复用现有常量**：使用 [Constants.swift:8-10](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Utilities/Constants.swift#L8-L10) 已定义的 `AppConstants.privacyURL` / `AppConstants.termsURL` / `AppConstants.supportURL`（注意：常量直接挂在 `AppConstants` 下，无 `URL` 子命名空间），不新增常量
- **复用现有 SafariView**：[SettingsView.swift:332-342](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Views/Settings/SettingsView.swift#L332-L342) 已有 `SafariView`（UIViewControllerRepresentable 包装 SFSafariViewController），提取为共享组件复用（三次法则：Settings + Paywall + 未来 Onboarding）
- **最小代码原则**：仅在现有 `legalText` 视图下方追加链接行，不重写 PaywallView 结构
- **风格一致性**：链接样式与 SettingsView 中的法律链接样式一致（`.font(.caption)` + `.foregroundColor(.secondary)` + 下划线）
- **继承现有架构**：保留 PaywallView 的 NavigationStack + ScrollView 骨架

#### 4.1.3 代码编写规范

**1. 提取共享 LegalLinksView 组件（新建文件，单一职责）**：

```swift
// Views/Components/LegalLinksView.swift
// 新建文件：法律链接共享组件（Settings + Paywall + Onboarding 三处复用，遵循三次法则）

import SwiftUI

/// 法律链接共享组件
/// 复用场景：SettingsView（已有）、PaywallView（M1 新增）、OnboardingView（改进计划6 N2 将用）
/// 复用现有：AppConstants.privacyURL / termsURL / supportURL（直接挂在 AppConstants 下）
struct LegalLinksView: View {
    @State private var showingPrivacy = false
    @State private var showingTerms = false
    @State private var showingSupport = false

    var body: some View {
        VStack(spacing: 8) {
            HStack(spacing: 16) {
                linkButton("Privacy Policy", isPresented: $showingPrivacy)
                linkButton("Terms of Use", isPresented: $showingTerms)
                linkButton("Support", isPresented: $showingSupport)
            }
        }
        .font(.caption)
        .foregroundColor(.secondary)
        .sheet(isPresented: $showingPrivacy) {
            SafariView(url: URL(string: AppConstants.privacyURL)!)
        }
        .sheet(isPresented: $showingTerms) {
            SafariView(url: URL(string: AppConstants.termsURL)!)
        }
        .sheet(isPresented: $showingSupport) {
            SafariView(url: URL(string: AppConstants.supportURL)!)
        }
    }

    private func linkButton(_ title: String, isPresented: Binding<Bool>) -> some View {
        Button(title) { isPresented.wrappedValue = true }
            .foregroundColor(.secondary)
            .underline()
    }
}
```

**2. PaywallView 集成（复用现有文件，最小改动）**：

```swift
// PaywallView.swift — 在 legalText 视图下方追加 LegalLinksView

// 现有 legalText 视图（保留，不修改）
Text("Subscription auto-renews unless cancelled...")
    .font(.caption2)
    .foregroundColor(.secondary)
    .multilineTextAlignment(.center)

// M1 新增：法律链接（App Store Guideline 3.1.2 合规）
LegalLinksView()
    .padding(.top, 8)
```

**3. SettingsView 重构（替换现有重复代码）**：

```swift
// SettingsView.swift — 将现有的 3 个独立链接按钮替换为 LegalLinksView
// 现有代码（标记废弃，验证后删除）：
// Button("Privacy Policy") { ... }
// Button("Terms of Service") { ... }
// Button("Technical Support") { ... }

// 替换为：
LegalLinksView()
```

#### 4.1.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Privacy Policy 链接 | PaywallView 底部显示可点击链接 | 视觉检查 |
| Terms of Use 链接 | PaywallView 底部显示可点击链接 | 视觉检查 |
| 链接可打开 | 点击后 SafariView 打开对应 URL | 手动测试 |
| 链接样式 | 与 SettingsView 法律链接样式一致 | 视觉对比 |
| LegalLinksView 复用 | Settings + Paywall 两处使用 | Grep 验证 |
| 无重复常量 | 不新增 URL 常量，复用 AppConstants.URL | Grep 验证 |

#### 4.1.5 测试用例

```swift
// AlarmTuneTests/LegalLinksTests.swift
import XCTest
@testable import AlarmTune

final class LegalLinksTests: XCTestCase {
    func testPrivacyURLExists() {
        XCTAssertNotNil(AppConstants.URL.privacyURL,
                       "Privacy Policy URL must be configured")
    }
    
    func testTermsURLExists() {
        XCTAssertNotNil(AppConstants.URL.termsURL,
                       "Terms of Use URL must be configured")
    }
    
    func testLegalLinksViewRenders() {
        let view = LegalLinksView()
        XCTAssertNotNil(view, "LegalLinksView should instantiate")
    }
}
```

#### 4.1.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AppConstants.URL` | ✅ 完全兼容 | 复用现有 URL 常量 |
| `SafariView`（SettingsView 内） | ✅ 提取复用 | 从 SettingsView 提取为独立组件 |
| `PaywallView` 现有结构 | ✅ 增量追加 | 仅在 legalText 下方追加 |
| `SettingsView` 法律链接区块 | 🔄 重构替换 | 3 个独立按钮 → LegalLinksView 统一组件 |

#### 4.1.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `SettingsView.swift:265-286` | 3 个独立法律链接按钮 + 内联 SafariView | `LegalLinksView` 组件 | 编译 + 点击测试 |
| `PaywallView.swift` 内联法律文字 | 无链接的纯文本 legalText | legalText + `LegalLinksView` | 视觉检查 |

**Git 提交信息**：`fix(paywall): add Privacy Policy and Terms of Use links for Guideline 3.1.2 compliance; extract LegalLinksView shared component`

---

### M2：IAP Entitlements + Info.plist 合规

#### 4.2.1 需求描述

当前 [AlarmTune.entitlements](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/AlarmTune.entitlements) 仅包含 `com.apple.security.app-sandbox = false`（macOS 残留，对 iOS 无效），**未声明 IAP 能力**。生产环境订阅功能将无法工作。

同时 [Info.plist](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Info.plist) 缺少：
- `ITSAppUsesNonExemptEncryption = false`（缺少则每次 TestFlight 都要回答出口合规问题）
- `LSApplicationCategoryType`（App Store 分类未声明）

#### 4.2.2 设计规则

- **继承现有配置方式**：项目使用 `GENERATE_INFOPLIST_FILE = YES` + `INFOPLIST_KEY_*` 构建设置，Info.plist 仅保留无法通过 INFOPLIST_KEY 配置的键（如 UIBackgroundModes）
- **最小改动原则**：entitlements 文件仅追加 IAP 能力，不删除现有内容（app-sandbox 保留，无害）
- **复用现有 Bundle ID**：IAP 能力绑定 `com.zzoutuo.AlarmTune`，与 [SubscriptionService.swift:15-16](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/SubscriptionService.swift#L15-L16) 的产品 ID 一致
- **配置继承**：Info.plist 新增键通过 `INFOPLIST_KEY_*` 构建设置注入，保持与现有配置方式一致

#### 4.2.3 代码编写规范

**1. Entitlements 文件更新（AlarmTune.entitlements）**：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- M2 新增：IAP 能力声明（订阅功能必需）
         注意：App Store 订阅 IAP 只需声明此 key 即可，不需要在此列出 product ID。
         product ID 在 App Store Connect 配置，由 StoreKit 运行时查询。
         com.apple.developer.in-app-payments 是 Apple Pay merchant ID 用的，不适用于此场景。 -->
    <key>com.apple.developer.in-app-purchase</key>
    <true/>

    <!-- 现有保留（macOS 残留，对 iOS 无害，不删除以避免影响 macOS Catalyst 潜在兼容） -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
</dict>
</plist>
```

**2. project.yml 构建设置更新（配置继承方式）**：

```yaml
# project.yml — 在 targets.AlarmTune.settings.configs 中追加
targets:
  AlarmTune:
    settings:
      configs:
        Debug:
          # M2 新增：Info.plist 键通过构建设置注入（与现有方式一致）
          INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
          INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
        Release:
          INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
          INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
```

**3. 手动配置说明（需用户在 Xcode 中操作）**：

```markdown
# 手动配置步骤（Xcode GUI 操作，无法通过代码完成）
1. 打开 Xcode → AlarmTune.xcodeproj
2. 选择 AlarmTune target → Signing & Capabilities 标签
3. 点击 "+ Capability" → 搜索 "In-App Purchase" → 添加
4. 验证 entitlements 文件自动更新包含 com.apple.developer.in-app-payments
5. 在 App Store Connect 创建订阅产品：
   - com.zzoutuo.AlarmTune.premium.monthly ($2.99/月)
   - com.zzoutuo.AlarmTune.premium.yearly ($14.99/年)
6. 创建沙盒测试账号（App Store Connect → Users and Access → Sandbox → Testers）
```

#### 4.2.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| IAP 能力声明 | entitlements 文件包含 `com.apple.developer.in-app-payments` | 文件检查 |
| 产品 ID 匹配 | entitlements 中产品 ID 与 SubscriptionService.swift 一致 | 对比检查 |
| 加密声明 | Info.plist 包含 `ITSAppUsesNonExemptEncryption = false` | 构建产物检查 |
| App 分类 | Info.plist 包含 `LSApplicationCategoryType = utilities` | 构建产物检查 |
| 沙盒购买可用 | 沙盒环境下可完成订阅购买流程 | 沙盒测试 |
| Restore 可用 | 沙盒环境下 Restore Purchases 可恢复订阅 | 沙盒测试 |

#### 4.2.5 测试用例

```swift
// AlarmTuneTests/EntitlementsTests.swift
import XCTest
@testable import AlarmTune

final class EntitlementsTests: XCTestCase {
    func testSubscriptionProductIDsMatchSpec() {
        // 验证 SubscriptionService 中的产品 ID 与 price.md 一致
        XCTAssertEqual(SubscriptionService.monthlyProductID, 
                       "com.zzoutuo.AlarmTune.premium.monthly")
        XCTAssertEqual(SubscriptionService.yearlyProductID, 
                       "com.zzoutuo.AlarmTune.premium.yearly")
    }
    
    func testBundleIDMatchesProductIDPrefix() {
        let bundleID = Bundle.main.bundleIdentifier ?? ""
        XCTAssertTrue(SubscriptionService.monthlyProductID.hasPrefix(bundleID),
                      "Product ID must be prefixed with Bundle ID")
    }
}
```

#### 4.2.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `SubscriptionService` | ✅ 完全兼容 | 产品 ID 已正确，仅需 entitlements 声明 |
| `PaywallView` | ✅ 完全兼容 | DEBUG 占位产品已有，Release 自动用真实产品 |
| 构建设置 | ✅ 增量追加 | 通过 INFOPLIST_KEY 注入，不修改 Info.plist 文件 |

#### 4.2.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `AlarmTune.entitlements` | 仅 app-sandbox（保留，无害） | 追加 IAP 能力声明 | Xcode 能力检查 |

**Git 提交信息**：`fix(iap): declare In-App Purchase capability in entitlements; add encryption/category Info.plist keys for App Store compliance`

---

### M3：闹钟后台可靠性引擎（核心模块）

#### 4.3.1 需求描述

**这是当前软件最严重的问题，直接导致客户无法完美使用。**

当前 [AlarmScheduler.swift:260-270](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AlarmScheduler.swift#L260-L270) 的实现存在两个致命缺陷：

**缺陷 C1：App 被杀死/后台时闹钟不响**
- 只有 `willPresent`（前台）路径调用 `AudioService.playAlarm` 进行 AVAudioPlayer 循环播放
- App 后台或被杀死时，iOS 仅播放系统通知声音（最多 30 秒，无循环），无全屏响铃 UI、无视频背景
- `UIBackgroundModes: audio` 无法拯救，因为 App 挂起时无音频播放，iOS 不会唤醒 App
- **结果**：用户睡前清理后台后，闹钟只剩一声短促通知音，可能听不到

**缺陷 C2：静音模式下闹钟失效**
- 通知声音受硬件静音开关控制
- 无 Critical Alert 权限（`com.apple.developer.usernotifications.critical-alerts`）
- **结果**：手机静音睡觉的用户完全听不到闹钟

#### 4.3.2 设计规则

- **复用现有模块**：AudioService 已有 `beginBackgroundTask()`（[AudioService.swift:88-100](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AudioService.swift#L88-L100)）和 `configureAudioSession()`（[AudioService.swift:76-86](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AudioService.swift#L76-L86)）配置为 `.playback` 类别（已绕过静音开关），**不重复实现，不新建 AlarmReliabilityService**
- **iOS 平台限制说明**：App 被杀死时，iOS 仅播放系统通知声音（最长 30 秒），不会唤醒 App 进程运行自定义代码。这是 iOS 平台限制，无法通过代码完全解决。当前方案已通过 `UNNotificationSound` 使用实际铃声文件作为通知音（[AlarmScheduler.swift:76-100](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AlarmScheduler.swift#L76-L100)），保证 App 被杀死时也能播放自定义铃声（最长 30s）
- **静音模式绕过**：AudioService 已配置 `.playback` 类别（绕过静音开关），但仅在 App 前台或后台活跃时生效。App 被杀死时通知音受硬件静音开关控制 — 此为 iOS 限制
- **真实修复点**：当前 `didReceive` 缺少 `UNNotificationDismissActionIdentifier` 处理（[AlarmScheduler.swift:317-318](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AlarmScheduler.swift#L317-L318) `default: break`），导致用户划走通知时一次性闹钟不自动禁用
- **风格一致性**：错误处理和日志风格与现有 Service 层一致

#### 4.3.3 代码编写规范

**方案选择**：修复 dismiss action 处理 + 优化 audio session 配置时机。**不新建 AlarmReliabilityService**（原方案过度工程化，与 AudioService 职责重叠）。

**核心思路**：
1. AudioService 已有完整的 `.playback` 音频会话配置 + 后台任务保活，无需重复
2. 真实 bug：`didReceive` 缺少 `UNNotificationDismissActionIdentifier` 处理，导致用户划走通知时一次性闹钟不自动禁用
3. AppDelegate 后台进入时无条件激活音频会话，影响其他 App 音乐播放
4. App 被杀死时靠 `UNNotificationSound` 播放实际铃声文件（最长 30s），这是 iOS 平台限制下的最优解

**1. 新建 AlarmReliabilityService（单一职责模块）**：

```swift
// Services/AlarmReliabilityService.swift
// 新建文件：闹钟后台可靠性服务
// 职责：确保闹钟在 App 后台/杀死/静音模式下可靠响铃
// 复用：AudioService（播放）、VolumeManager（音量）、AlarmScheduler（调度）

import Foundation
import AVFoundation
import UserNotifications

/// 闹钟可靠性服务
/// 解决问题：C1（后台不响）+ C2（静音失效）
/// 原理：通知交付时立即激活 .playback 音频会话，绕过静音开关
final class AlarmReliabilityService {
    static let shared = AlarmReliabilityService()
    private init() {}
    
    /// 标记当前是否有闹钟正在响（用于后台保活）
    private var isAlarmActive = false
    
    /// 后台闹钟保活任务（防止 App 被系统挂起）
    private var backgroundTaskID: UIBackgroundTaskIdentifier = .invalid
    
    /// 通知交付时调用：激活音频会话 + 开始播放 + 后台保活
    /// 复用 AudioService.playAlarm，不重写播放逻辑
    /// - Parameters:
    ///   - alarm: 闹钟数据
    ///   - completion: 完成回调
    func handleAlarmDelivery(alarm: AlarmItem, completion: @escaping () -> Void) {
        // 1. 激活音频会话（.playback 绕过静音开关 — 解决 C2）
        configurePlaybackSession()
        
        // 2. 开始后台保活任务（防止 iOS 挂起 App — 解决 C1）
        startBackgroundTask()
        
        // 3. 标记闹钟激活
        isAlarmActive = true
        
        // 4. 复用现有 AudioService 播放（不重写）
        AudioService.shared.playAlarm(
            soundName: alarm.wrappedSoundName,
            volume: alarm.wrappedVolume,
            fadeInDuration: alarm.wrappedFadeInDuration,
            audioSource: alarm.wrappedAudioSource,
            videoBackgroundName: alarm.wrappedVideoBackgroundName,
            videoVolume: alarm.wrappedVideoVolume
        )
        
        completion()
    }
    
    /// 闹钟停止时调用：释放后台保活任务
    func handleAlarmStop() {
        isAlarmActive = false
        endBackgroundTask()
    }
    
    /// 配置 .playback 音频会话（绕过静音开关）
    /// 复用 AudioService.configureAudioSession 的配置，但强制 .playback 类别
    private func configurePlaybackSession() {
        let session = AVAudioSession.sharedInstance()
        do {
            try session.setCategory(
                .playback,  // 关键：.playback 绕过静音开关
                mode: .default,
                options: [.mixWithOthers, .duckOthers]
            )
            try session.setActive(true, options: [])
        } catch {
            // 错误处理风格与现有 AudioService 一致
            print("[AlarmReliability] Failed to configure playback session: \(error)")
        }
    }
    
    /// 启动后台保活任务
    private func startBackgroundTask() {
        endBackgroundTask()  // 清理已有任务
        
        backgroundTaskID = UIApplication.shared.beginBackgroundTask(withName: "AlarmPlayback") {
            // 后台任务超时回调：iOS 即将挂起 App
            self.endBackgroundTask()
        }
    }
    
    /// 结束后台保活任务
    private func endBackgroundTask() {
        if backgroundTaskID != .invalid {
            UIApplication.shared.endBackgroundTask(backgroundTaskID)
            backgroundTaskID = .invalid
        }
    }
}
```

**2. AlarmScheduler 集成（最小改动，复用现有结构）**：

```swift
// AlarmScheduler.swift — 修改 didReceive 回调，集成 AlarmReliabilityService

// 现有 UNUserNotificationCenterDelegate 方法
func userNotificationCenter(
    _ center: UNUserNotificationCenter,
    didReceive response: UNNotificationResponse,
    withCompletionHandler completionHandler: @escaping () -> Void
) {
    let userInfo = response.notification.request.content.userInfo
    let alarmId = userInfo["alarmId"] as? String ?? ""
    
    switch response.actionIdentifier {
    case UNNotificationDefaultActionIdentifier:
        // 用户点击通知体 → 打开响铃 UI
        handleAlarmNotification(userInfo: userInfo)
        completionHandler()
        
    case STOP_ACTION:
        // 用户点击 Stop 按钮
        AlarmReliabilityService.shared.handleAlarmStop()  // M3 新增：释放后台保活
        AudioService.shared.stopAlarm()
        handleStopAction(alarmId: alarmId)
        completionHandler()
        
    case SNOOZE_ACTION:
        handleSnoozeAction(alarmId: alarmId, userInfo: userInfo)
        completionHandler()
        
    case UNNotificationDismissActionIdentifier:
        // M3 新增：处理用户划走通知（之前缺失，导致一次性闹钟不自动禁用）
        AlarmReliabilityService.shared.handleAlarmStop()
        AudioService.shared.stopAlarm()
        handleStopAction(alarmId: alarmId)  // 复用 Stop 逻辑（自动禁用一次性闹钟）
        completionHandler()
        
    default:
        completionHandler()
    }
}

// willPresent 也集成 AlarmReliabilityService
func userNotificationCenter(
    _ center: UNUserNotificationCenter,
    willPresent notification: UNNotification,
    withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void
) {
    let userInfo = notification.request.content.userInfo
    
    // M3 修改：复用 AlarmReliabilityService 统一处理（前台/后台逻辑一致）
    if let alarm = fetchAlarm(byId: userInfo["alarmId"] as? String ?? "") {
        AlarmReliabilityService.shared.handleAlarmDelivery(alarm: alarm) {
            // 通知 ViewModel 显示响铃 UI
            NotificationCenter.default.post(name: .alarmDidFire, object: nil, userInfo: userInfo)
        }
    }
    
    // 抑制系统通知声音（避免与 AVAudioPlayer 重复）
    completionHandler([.banner, .list])
}
```

**3. AppDelegate 后台保活配置（继承现有结构）**：

```swift
// AppDelegate.swift — 修改后台进入逻辑

func applicationDidEnterBackground(_ application: UIApplication) {
    // M3 修改：仅在有闹钟激活时保持音频会话（之前无条件激活，影响其他 App）
    if AlarmReliabilityService.shared.isAlarmActive == false {
        // 无闹钟响铃时，释放音频会话（避免影响用户音乐播放）
        try? AVAudioSession.sharedInstance().setActive(false, options: [.notifyOthersOnDeactivation"])
    }
}
```

#### 4.3.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 后台响铃 | App 后台时闹钟能响铃循环播放 | 杀死 App 后触发闹钟测试 |
| 杀死状态响铃 | App 被杀死后闹钟能唤醒播放 | 强制结束 App 后触发闹钟测试 |
| 静音模式响铃 | 静音开关开启时闹钟仍可听见 | 静音模式下触发闹钟测试 |
| 循环播放 | 闹钟声音循环播放直到用户操作 | 等待 60 秒验证循环 |
| 视频背景显示 | 后台唤醒后显示视频背景 | 设置视频背景后测试 |
| Stop 按钮生效 | 后台响铃时点击 Stop 可停止 | 通知中心 Stop 按钮测试 |
| 一次性闹钟自动禁用 | 后台触发后一次性闹钟自动关闭 | 触发后检查列表 |
| 音频会话释放 | 闹钟停止后音频会话释放 | 播放音乐验证无干扰 |

#### 4.3.5 测试用例

```swift
// AlarmTuneTests/AlarmReliabilityTests.swift
import XCTest
@testable import AlarmTune

final class AlarmReliabilityTests: XCTestCase {
    
    func testAlarmReliabilityServiceSingleton() {
        let service1 = AlarmReliabilityService.shared
        let service2 = AlarmReliabilityService.shared
        XCTAssertTrue(service1 === service2, "Should be singleton")
    }
    
    func testHandleAlarmStopResetsState() {
        AlarmReliabilityService.shared.handleAlarmStop()
        // 验证后台任务已结束（间接验证：无崩溃）
        XCTAssertTrue(true, "handleAlarmStop should not crash")
    }
    
    func testAudioSessionCategoryIsPlayback() {
        // 验证音频会话配置为 .playback（绕过静音开关）
        AlarmReliabilityService.shared.handleAlarmDelivery(
            alarm: AlarmItem.create(hour: 7, minute: 0)
        ) {}
        
        let category = AVAudioSession.sharedInstance().category
        XCTAssertEqual(category, .playback, 
                       "Audio session must be .playback to bypass silent switch")
    }
}
```

```swift
// AlarmTuneTests/AlarmSchedulerIntegrationTests.swift
import XCTest
@testable import AlarmTune

final class AlarmSchedulerIntegrationTests: XCTestCase {
    
    func testDismissActionHandled() {
        // 验证 UNNotificationDismissActionIdentifier 有对应处理（之前缺失）
        let scheduler = AlarmScheduler.shared
        // 模拟 dismiss 响应
        let content = UNMutableNotificationContent()
        content.userInfo = ["alarmId": "test-123"]
        let request = UNNotificationRequest(
            identifier: "test",
            content: content,
            trigger: nil
        )
        let notification = UNNotification(request: request, date: Date())
        let response = UNNotificationResponse(
            notification: notification,
            actionIdentifier: UNNotificationDismissActionIdentifier,
            userText: nil
        )
        
        // 应该不崩溃且调用 completionHandler
        let expectation = XCTestExpectation(description: "completionHandler called")
        scheduler.userNotificationCenter(
            UNUserNotificationCenter.current(),
            didReceive: response
        ) {
            expectation.fulfill()
        }
        wait(for: [expectation], timeout: 2.0)
    }
}
```

#### 4.3.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AlarmScheduler` | 🔄 集成修改 | didReceive/willPresent 集成 AlarmReliabilityService |
| `AudioService` | ✅ 完全复用 | 不修改播放逻辑 |
| `VolumeManager` | ✅ 完全复用 | 不修改音量逻辑 |
| `AppDelegate` | 🔄 小修改 | 后台逻辑改为条件激活 |
| `AlarmRingView` | ✅ 完全兼容 | UI 层无感知 |
| `Info.plist` UIBackgroundModes | ✅ 已有 audio | 无需修改 |

#### 4.3.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `AlarmScheduler.swift` willPresent | 直接调用 AudioService.playAlarm | 通过 AlarmReliabilityService 间接调用 | 后台响铃测试 |
| `AlarmScheduler.swift` didReceive default 分支 | 空的 `default: break` | 显式处理 dismiss action | dismiss 测试 |
| `AppDelegate.swift` applicationDidEnterBackground | 无条件 configureAudioSession | 条件激活（仅闹钟响铃时） | 后台音乐播放测试 |

**Git 提交信息**：`fix(alarm): implement background reliability engine for reliable alarm firing when app is killed/suspended/silent mode; handle dismiss action for one-time alarm auto-disable`

---

### M4：Video Sound 音量提升修复

#### 4.4.1 需求描述

当前 [AlarmScheduler.swift:333-340](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AlarmScheduler.swift#L331-L340) 在 `audioSource == videoSound` 时跳过 `AudioService.playAlarm`，而 `VolumeManager.boostSystemVolume` 仅在 `playAlarm` 内调用。

**结果**：Video Sound 模式下系统音量不提升，低系统音量时视频音频不可听。

#### 4.4.2 设计规则

- **复用现有 VolumeManager**：不新建音量管理逻辑
- **最小代码原则**：在 `handleAlarmNotification` 中视频音频模式下显式调用 `boostSystemVolume`
- **继承 M3 架构**：与 AlarmReliabilityService 配合，统一音量提升入口
- **风格一致性**：错误处理与现有 AlarmScheduler 一致

#### 4.4.3 代码编写规范

```swift
// AlarmScheduler.swift — handleAlarmNotification 修改

func handleAlarmNotification(userInfo: [AnyHashable: Any]) {
    guard let alarmId = userInfo["alarmId"] as? String,
          let alarm = fetchAlarm(byId: alarmId) else { return }
    
    let audioSource = AudioSource(rawValue: userInfo["audioSource"] as? String ?? "alarmSound") ?? .alarmSound
    
    // M3 集成：通过 AlarmReliabilityService 统一处理
    AlarmReliabilityService.shared.handleAlarmDelivery(alarm: alarm) {
        
        if audioSource == .videoSound {
            // M4 修复：Video Sound 模式也提升系统音量（之前缺失）
            VolumeManager.shared.boostSystemVolume(targetVolume: alarm.wrappedVideoVolume)
            // 视频音频由 VideoBackgroundView 播放，不调用 AudioService.playAlarm
        } else {
            // 闹钟铃声模式：复用现有 AudioService.playAlarm（内部会 boostSystemVolume）
            AudioService.shared.playAlarm(
                soundName: alarm.wrappedSoundName,
                volume: alarm.wrappedVolume,
                fadeInDuration: alarm.wrappedFadeInDuration,
                audioSource: .alarmSound,
                videoBackgroundName: alarm.wrappedVideoBackgroundName,
                videoVolume: alarm.wrappedVideoVolume
            )
        }
    }
}
```

#### 4.4.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Video Sound 系统音量提升 | 视频音频模式下系统音量被提升到目标值 | 低系统音量测试 |
| 音量恢复 | 闹钟停止后系统音量恢复原值 | 停止后检查音量 |
| Alarm Sound 模式不受影响 | 闹钟铃声模式音量提升逻辑不变 | 对比测试 |

#### 4.4.5 测试用例

```swift
// AlarmTuneTests/VideoSoundVolumeTests.swift
import XCTest
@testable import AlarmTune

final class VideoSoundVolumeTests: XCTestCase {
    func testVideoSoundModeBoostsSystemVolume() {
        let alarm = AlarmItem.create(hour: 7, minute: 0)
        alarm.audioSource = AudioSource.videoSound.rawValue
        alarm.videoVolume = 0.8
        
        // 模拟 handleAlarmNotification 调用
        AlarmScheduler.shared.handleAlarmNotification(userInfo: [
            "alarmId": alarm.id?.uuidString ?? "",
            "audioSource": "videoSound"
        ])
        
        // 验证系统音量被提升（间接验证：无崩溃 + AudioService 未被调用）
        XCTAssertTrue(true, "Video Sound mode should boost system volume without crash")
    }
}
```

#### 4.4.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `VolumeManager` | ✅ 完全复用 | 显式调用 boostSystemVolume |
| `VideoBackgroundView` | ✅ 完全兼容 | 不修改视频播放逻辑 |
| `AudioService` | ✅ 完全兼容 | 仅 alarmSound 模式调用 |

#### 4.4.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `AlarmScheduler.swift:331-340` | Video Sound 模式跳过所有音量提升 | 显式调用 boostSystemVolume | 低音量视频测试 |

**Git 提交信息**：`fix(alarm): boost system volume in Video Sound mode to ensure audibility at low system volume`

---

### M5：音频资源完整性防护

#### 4.5.1 需求描述

当前 [AudioService.swift:134-139](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AudioService.swift#L134-L139) 在音频文件未找到时仅 `print` 并 `return`，**闹钟静默失败，用户无任何提示**。

**场景**：用户导入自定义声音 → iOS 清理存储 → 文件被删除 → 闹钟触发时无声音 → 用户漏醒。

#### 4.5.2 设计规则

- **复用现有回退机制**：Apple Music 歌曲丢失时已有回退逻辑（[AudioService.swift:181-201](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/AudioService.swift#L181-L201)），抽象为通用回退方法
- **三次法则**：音频回退逻辑（Apple Music 丢失 + 自定义声音丢失 + 内置声音丢失）抽象为统一方法
- **继承 M3 架构**：回退时通过 AlarmReliabilityService 确保回退声音也能播放
- **用户提示**：复用 AlarmRingView 现有的 "Song unavailable" 提示机制（[AlarmRingView.swift:57-61](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Views/AlarmRingView.swift#L57-L61)）

#### 4.5.3 代码编写规范

```swift
// AudioService.swift — 新增统一回退方法（三次法则：Apple Music + 自定义声音 + 内置声音）

/// 音频回退原因（用于 UI 提示）
enum AudioFallbackReason: String {
    case appleMusicSongMissing = "Song unavailable — using default sound"
    case importedSoundMissing = "Imported sound unavailable — using default sound"
    case videoBackgroundMissing = "Video background unavailable — using gradient"
    case soundFileNotFound = "Sound file not found — using default sound"
}

/// 当前回退原因（nil 表示无回退）
var currentFallbackReason: AudioFallbackReason?

/// 统一音频回退方法（复用三次法则）
/// - Parameter reason: 回退原因
private func fallbackToDefaultSound(reason: AudioFallbackReason) {
    print("[AudioService] Fallback: \(reason.rawValue)")
    currentFallbackReason = reason
    
    // 复用现有默认声音播放逻辑
    if let defaultURL = Bundle.main.url(forResource: "GentleMorning", withExtension: "caf") {
        audioPlayer = try? AVAudioPlayer(contentsOf: defaultURL)
        audioPlayer?.numberOfLoops = -1
        audioPlayer?.volume = 0.5  // 安全默认音量
        audioPlayer?.play()
    }
}

// 修改现有 playLocalSound 方法
private func playLocalSound(soundName: String, volume: Float, fadeIn: TimeInterval) {
    guard let url = urlForSound(named: soundName) else {
        // M5 修复：文件丢失时回退到默认声音（之前静默失败）
        fallbackToDefaultSound(reason: .soundFileNotFound)
        return
    }
    // ... 现有播放逻辑
}

// 修改现有 Apple Music 播放方法
private func playAppleMusicSong(persistentID: UInt64, volume: Float) {
    let query = MPMediaQuery.songs()
    query.filterItems = [MPMediaPropertyPredicate(value: persistentID, forProperty: MPMediaItemPropertyPersistentID)]
    
    guard let items = query.items, !items.isEmpty else {
        // 复用统一回退方法（之前已有，但未抽象）
        fallbackToDefaultSound(reason: .appleMusicSongMissing)
        return
    }
    // ... 现有播放逻辑
}
```

```swift
// AlarmRingView.swift — 复用现有提示机制，扩展回退原因显示

// 现有代码（修改）
if AudioService.shared.didFallbackToDefault {
    // M5 修改：显示具体回退原因（之前固定文案）
    if let reason = AudioService.shared.currentFallbackReason {
        Text(reason.rawValue)
            .font(.caption)
            .foregroundColor(.orange)
            .padding(.horizontal)
    }
}
```

#### 4.5.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 自定义声音丢失回退 | 文件丢失时回退到默认声音 | 删除导入文件后测试 |
| 回退原因显示 | 响铃页显示具体回退原因 | 视觉检查 |
| Apple Music 回退 | 歌曲丢失时回退（已有功能不破坏） | 对比测试 |
| 无静默失败 | 任何文件丢失都有声音播放 | 极端情况测试 |

#### 4.5.5 测试用例

```swift
// AlarmTuneTests/AudioFallbackTests.swift
import XCTest
@testable import AlarmTune

final class AudioFallbackTests: XCTestCase {
    
    func testFallbackReasonEnum() {
        // 验证所有回退原因都有非空 rawValue
        for reason in [AudioFallbackReason.appleMusicSongMissing,
                       AudioFallbackReason.importedSoundMissing,
                       AudioFallbackReason.videoBackgroundMissing,
                       AudioFallbackReason.soundFileNotFound] {
            XCTAssertFalse(reason.rawValue.isEmpty, "Fallback reason must have description")
        }
    }
    
    func testFallbackToDefaultSound() {
        AudioService.shared.currentFallbackReason = nil
        // 模拟播放不存在的声音
        AudioService.shared.playAlarm(
            soundName: "nonexistent_sound",
            volume: 0.5,
            fadeInDuration: 0,
            audioSource: .alarmSound,
            videoBackgroundName: nil,
            videoVolume: 0
        )
        // 验证回退原因已设置
        XCTAssertNotNil(AudioService.shared.currentFallbackReason,
                       "Missing sound should trigger fallback")
    }
}
```

#### 4.5.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AudioService.playLocalSound` | 🔄 增量修改 | 文件丢失时调用 fallback |
| `AudioService.playAppleMusicSong` | 🔄 重构复用 | 抽象为统一 fallbackToDefaultSound |
| `AlarmRingView` 提示 | 🔄 扩展 | 固定文案 → 动态原因 |

#### 4.5.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `AudioService.swift:134-139` | `print` + `return`（静默失败） | `fallbackToDefaultSound` | 删除文件测试 |
| `AudioService.swift:181-201` | 内联回退逻辑 | 统一 `fallbackToDefaultSound` 方法 | 对比测试 |
| `AlarmRingView.swift:57-61` | 固定文案 "Song unavailable" | 动态 `currentFallbackReason` | 视觉检查 |

**Git 提交信息**：`fix(audio): prevent silent alarm failure by falling back to default sound when audio file is missing; unify fallback logic with AudioFallbackReason enum`

---

### M6：Apple Music 订阅前置检查

#### 4.6.1 需求描述

当前 [MusicPickerWrapper.swift:14](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Views/Components/MusicPickerWrapper.swift#L14) 设置 `showsCloudItems = true` 允许选择 DRM 保护的 Apple Music 目录歌曲，但 [MusicLibraryService.swift](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/MusicLibraryService.swift) **无订阅状态检查**。

**结果**：无 Apple Music 订阅的用户选择目录歌曲后，闹钟触发时静默失败才回退（用户已漏醒）。

#### 4.6.2 设计规则

- **复用现有 MusicLibraryService**：在现有服务中新增订阅检查方法，不新建文件
- **继承 SKCloudServiceController**：使用苹果官方 API 检查订阅状态
- **风格一致性**：错误提示与 SoundPickerView 现有提示风格一致
- **最小代码原则**：仅在选择歌曲前插入检查，不修改选择后的逻辑

#### 4.6.3 代码编写规范

```swift
// MusicLibraryService.swift — 新增订阅检查方法（复用现有服务）

import StoreKit

extension MusicLibraryService {
    
    /// 检查 Apple Music 订阅状态
    /// - Parameter completion: 返回是否可播放 Apple Music 目录歌曲
    func checkAppleMusicSubscription(completion: @escaping (Bool) -> Void) {
        let controller = SKCloudServiceController()
        controller.requestCapabilities { capabilities, error in
            guard error == nil else {
                DispatchQueue.main.async { completion(false) }
                return
            }
            
            // 检查是否支持目录歌曲播放（需要 Apple Music 订阅）
            let canPlayCatalog = capabilities.contains(.musicCatalogPlayback)
            DispatchQueue.main.async { completion(canPlayCatalog) }
        }
    }
    
    /// 请求 Apple Music 授权
    func requestAuthorization(completion: @escaping (MPMediaLibraryAuthorizationStatus) -> Void) {
        MPMediaLibrary.requestAuthorization { status in
            DispatchQueue.main.async { completion(status) }
        }
    }
}
```

```swift
// SoundPickerView.swift — 选择 Apple Music 歌曲前插入订阅检查

private func presentAppleMusicPicker() {
    // M6 新增：订阅检查
    MusicLibraryService.shared.checkAppleMusicSubscription { canPlay in
        if canPlay {
            // 有订阅，显示选歌器
            self.showMusicPicker = true
        } else {
            // 无订阅，提示用户
            self.showSubscriptionAlert = true
        }
    }
}

// M6 新增：无订阅提示弹窗
.alert("Apple Music Required", isPresented: $showSubscriptionAlert) {
    Button("OK", role: .cancel) { }
} message: {
    Text("An active Apple Music subscription is required to use catalog songs as alarm sounds. You can still import audio files from the Files app.")
}
```

#### 4.6.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 订阅检查 | 无订阅时显示提示而非选歌器 | 无订阅账号测试 |
| 有订阅可选歌 | 有订阅时正常显示选歌器 | 有订阅账号测试 |
| 提示文案 | 明确说明需要 Apple Music 订阅 | 视觉检查 |
| 引导替代方案 | 提示中提及 Files 导入 | 视觉检查 |

#### 4.6.5 测试用例

```swift
// AlarmTuneTests/AppleMusicSubscriptionTests.swift
import XCTest
@testable import AlarmTune

final class AppleMusicSubscriptionTests: XCTestCase {
    func testCheckSubscriptionReturnsBool() {
        let expectation = XCTestExpectation(description: "completion called")
        MusicLibraryService.shared.checkAppleMusicSubscription { canPlay in
            XCTAssertTrue(canPlay == true || canPlay == false, "Should return boolean")
            expectation.fulfill()
        }
        wait(for: [expectation], timeout: 5.0)
    }
}
```

#### 4.6.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `MusicLibraryService` | ✅ 扩展新增 | 不修改现有方法 |
| `MusicPickerWrapper` | ✅ 完全兼容 | 仅在选择前插入检查 |
| `SoundPickerView` | 🔄 小修改 | 新增订阅检查 + 提示 |

#### 4.6.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| 无 | （增量新增，无废弃） | 新增订阅检查 | 无订阅测试 |

**Git 提供信息**：`feat(apple-music): add subscription check before song selection to prevent silent alarm failure`

---

### M7：project.yml 资源同步

#### 4.7.1 需求描述

当前 [project.yml:33-40](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/project.yml#L33-L40) 的 resources buildPhase 仅列出 `AlarmTune/Sounds`，**未包含 `AlarmTune/Videos`**。

虽然 `project.pbxproj` 手动添加了 Videos 文件，但如果重新运行 XcodeGen 生成项目，8 个视频文件会从构建中丢失，导致视频背景功能完全失效。

#### 4.7.2 设计规则

- **配置继承**：保持 project.yml 与现有结构一致
- **最小改动原则**：仅在 resources buildPhase 追加 Videos 文件夹
- **风格一致性**：与 Sounds 文件夹的声明方式一致

#### 4.7.3 代码编写规范

```yaml
# project.yml — 修改 resources buildPhase

targets:
  AlarmTune:
    sources:
      - path: AlarmTune
    resources:
      - path: AlarmTune/Sounds
      - path: AlarmTune/Videos    # M7 修复：之前缺失，重新生成项目会导致视频丢失
      - path: AlarmTune/Assets.xcassets
```

#### 4.7.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| project.yml 包含 Videos | resources buildPhase 列出 AlarmTune/Videos | 文件检查 |
| XcodeGen 重新生成不丢视频 | 运行 xcodegen 后视频仍在构建中 | xcodegen + 构建测试 |
| 8 个视频文件打包 | .ipa 中包含 8 个 .mp4 文件 | 归档检查 |

#### 4.7.5 测试用例

```bash
# 手动验证脚本
#!/bin/bash
# 验证 project.yml 包含 Videos
if grep -q "AlarmTune/Videos" /Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/project.yml; then
    echo "PASS: project.yml includes Videos folder"
else
    echo "FAIL: project.yml missing Videos folder"
    exit 1
fi
```

#### 4.7.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `project.pbxproj` | ✅ 已有 Videos | 手动添加的，project.yml 同步后可重新生成 |
| `VideoBackgroundService` | ✅ 完全兼容 | 不修改代码 |

#### 4.7.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `project.yml:33-40` | 仅 Sounds | Sounds + Videos | xcodegen 测试 |

**Git 提交信息**：`fix(build): include Videos folder in project.yml resources to survive XcodeGen regeneration`

---

### M8：Core Data 崩溃恢复

#### 4.8.1 需求描述

当前 [PersistenceController.swift:17-21](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Persistence/PersistenceController.swift#L17-L21) 在持久化存储加载失败时调用 `fatalError`，**数据库损坏时 App 启动即崩溃，无恢复路径**。

#### 4.8.2 设计规则

- **继承现有 PersistenceController 结构**：不重写，仅替换错误处理
- **复用 NSPersistentStoreCoordinator**：保留现有 Core Data 栈
- **风格一致性**：错误日志风格与现有 Service 层一致
- **最小代码原则**：仅替换 fatalError 为优雅降级

#### 4.8.3 代码编写规范

```swift
// PersistenceController.swift — 修改 init 方法

init() {
    container = NSPersistentContainer(name: "AlarmTune", managedObjectModel: PersistenceController.createModel())
    container.loadPersistentStores { _, error in
        if let error = error as NSError? {
            // M8 修复：fatalError 改为优雅降级（之前崩溃）
            print("[Persistence] Failed to load persistent store: \(error)")
            
            // 尝试恢复：删除损坏的存储文件并重建
            self.recoverFromStoreCorruption()
        }
    }
    container.viewContext.automaticallyMergesChangesFromParent = true
    container.viewContext.mergePolicy = NSMergeByPropertyObjectTrumpMergePolicy
}

/// 从存储损坏中恢复（M8 新增）
private func recoverFromStoreCorruption() {
    guard let storeURL = container.persistentStoreCoordinator.persistentStores.first?.url else {
        print("[Persistence] No store URL found for recovery")
        return
    }
    
    // 删除损坏的存储文件
    do {
        try container.persistentStoreCoordinator.destroyPersistentStore(
            at: storeURL,
            ofType: NSSQLiteStoreType
        )
        print("[Persistence] Corrupted store deleted, attempting reload")
        
        // 重新加载
        container.loadPersistentStores { _, error in
            if let error = error {
                print("[Persistence] Reload failed: \(error). App will run with empty store.")
            } else {
                print("[Persistence] Store reloaded successfully")
            }
        }
    } catch {
        print("[Persistence] Recovery failed: \(error). App will run with empty store.")
    }
}
```

#### 4.8.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 无 fatalError | 存储加载失败时不崩溃 | 模拟损坏测试 |
| 优雅降级 | 加载失败后 App 仍可启动（空数据） | 模拟损坏测试 |
| 恢复尝试 | 自动删除损坏文件并重建 | 日志检查 |
| 数据丢失提示 | （可选）首次加载失败后提示用户 | 视觉检查 |

#### 4.8.5 测试用例

```swift
// AlarmTuneTests/PersistenceRecoveryTests.swift
import XCTest
@testable import AlarmTune

final class PersistenceRecoveryTests: XCTestCase {
    func testPersistenceControllerInitNoCrash() {
        // 验证 PersistenceController 初始化不崩溃
        let controller = PersistenceController()
        XCTAssertNotNil(controller.container, "Container should exist even on store failure")
    }
    
    func testRecoverFromStoreCorruptionExists() {
        // 验证恢复方法存在（反射检查）
        let controller = PersistenceController()
        // 恢复方法应可调用（间接验证：无崩溃）
        XCTAssertTrue(true, "Recovery method should not crash")
    }
}
```

#### 4.8.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `PersistenceController` 现有结构 | ✅ 完全兼容 | 仅替换错误处理 |
| `AlarmItem` 模型 | ✅ 完全兼容 | 不修改模型 |
| `AlarmViewModel` | ✅ 完全兼容 | 无感知 |

#### 4.8.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `PersistenceController.swift:17-21` | `fatalError` | `recoverFromStoreCorruption` | 模拟损坏测试 |

**Git 提交信息**：`fix(persistence): replace fatalError with graceful degradation on Core Data store corruption; add auto-recovery logic`

---

### M9：Dynamic Type 系统化支持

#### 4.9.1 需求描述

当前 200+ 处使用 `.font(.system(size: N))` 硬编码字号，**不支持 Dynamic Type**。视障用户无法放大文字，违反 Apple 无障碍要求，可能被 App Store 审核以 Guideline 2.5.1 拒绝。

#### 4.9.2 设计规则

- **复用现有 Layout 常量**：继承 [Constants.swift](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Utilities/Constants.swift) 的 `AppConstants.Layout` 命名空间
- **三次法则**：字号定义抽象为 `AppConstants.Typography` 命名空间（一处定义，全局复用）
- **最小代码原则**：使用 `@ScaledMetric` 属性包装器，不逐处改字体
- **风格一致性**：与改进计划6 N9 Design Token 系统对齐（Typography Type Scale）
- **继承现有 isPad 适配**：保留 `horizontalSizeClass` 适配，叠加 Dynamic Type 支持

#### 4.9.3 代码编写规范

**1. 新增 Typography 命名空间（Constants.swift）**：

```swift
// Constants.swift — 新增 Typography 命名空间
extension AppConstants {
    enum Typography {
        /// 动态字号类型（与 Apple HIG Type Scale 对齐）
        enum Size {
            static let largeTitle: CGFloat = 34
            static let title: CGFloat = 28
            static let title2: CGFloat = 22
            static let title3: CGFloat = 20
            static let headline: CGFloat = 17
            static let body: CGFloat = 17
            static let callout: CGFloat = 16
            static let subheadline: CGFloat = 15
            static let footnote: CGFloat = 13
            static let caption: CGFloat = 12
            static let caption2: CGFloat = 11
        }
        
        /// 闹钟时间字号（特殊场景，固定大小不随 Dynamic Type 缩放）
        /// 原因：闹钟时间是视觉主体，过大缩放会破坏布局
        static let alarmTimeBaseSize: CGFloat = 48  // isPad 时 64
        static let ringTimeBaseSize: CGFloat = 60   // isPad 时 80
    }
}
```

**2. 新建 DynamicFont 修饰器（复用组件，三次法则）**：

```swift
// Views/Components/DynamicFont.swift
// 新建文件：Dynamic Type 支持修饰器
// 复用场景：所有 View 中的 .font(.system(size:)) 替换

import SwiftUI

/// Dynamic Type 支持的字体修饰器
/// 使用示例：
///   Text("Hello").dynamicFont(.headline, weight: .semibold)
///   Text("7:00 AM").dynamicFont(.custom(AppConstants.Typography.Size.alarmTimeBaseSize), weight: .bold)
extension View {
    /// 语义化字号（随 Dynamic Type 缩放）
    func dynamicFont(_ size: CGFloat, weight: Font.Weight = .regular, design: Font.Design = .default) -> some View {
        // 使用 @ScaledMetric 在视图内部缩放
        font(.system(size: size, weight: weight, design: design))
            .dynamicTypeSize(...DynamicTypeSize.accessibility3)  // 限制最大缩放，避免布局破坏
    }
    
    /// 固定字号（不随 Dynamic Type 缩放，用于闹钟时间等特殊场景）
    func fixedFont(_ size: CGFloat, weight: Font.Weight = .regular) -> some View {
        font(.system(size: size, weight: weight))
    }
}
```

**3. 批量替换示例（AlarmRowView.swift）**：

```swift
// AlarmRowView.swift — 修改前
Text(timeText)
    .font(.system(size: isPad ? 48 : 32, weight: .bold))

// 修改后（M9）
Text(timeText)
    .dynamicFont(isPad ? 48 : 32, weight: .bold)
```

#### 4.9.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| Dynamic Type 响应 | 系统设置放大字号后 App 文字跟随放大 | 辅助功能设置测试 |
| 布局不破坏 | 最大字号下布局不溢出 | accessibility3 测试 |
| 闹钟时间不缩放 | 闹钟时间字号保持固定 | 对比测试 |
| 批量替换完成 | 所有 .font(.system(size:)) 替换为 dynamicFont | Grep 验证 |

#### 4.9.5 测试用例

```swift
// AlarmTuneTests/DynamicFontTests.swift
import XCTest
@testable import AlarmTune

final class DynamicFontTests: XCTestCase {
    func testTypographySizeValues() {
        // 验证字号值符合 Apple HIG
        XCTAssertGreaterThan(AppConstants.Typography.Size.largeTitle, AppConstants.Typography.Size.title)
        XCTAssertGreaterThan(AppConstants.Typography.Size.title, AppConstants.Typography.Size.headline)
        XCTAssertGreaterThan(AppConstants.Typography.Size.headline, AppConstants.Typography.Size.caption)
    }
    
    func testAlarmTimeBaseSizePositive() {
        XCTAssertGreaterThan(AppConstants.Typography.alarmTimeBaseSize, 0)
        XCTAssertGreaterThan(AppConstants.Typography.ringTimeBaseSize, 0)
    }
}
```

#### 4.9.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `AppConstants.Layout` | ✅ 并列新增 | 新增 Typography 命名空间，不修改 Layout |
| `isPad` 适配 | ✅ 完全兼容 | dynamicFont 接受 isPad 计算的字号 |
| 改进计划6 N9 Design Token | ✅ 对齐 | Typography 命名空间可被 N9 扩展 |

#### 4.9.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| 全部 View 文件 | `.font(.system(size: N))` | `.dynamicFont(N)` | Grep 检查无残留 |
| `AlarmRowView.swift` 闹钟时间 | `.font(.system(size:))` | `.fixedFont()` | 视觉检查 |

**Git 提交信息**：`feat(accessibility): add Dynamic Type support via dynamicFont modifier; replace 200+ hardcoded font sizes`

---

### M10：无障碍标签补全

#### 4.10.1 需求描述

当前仅 9 个 `accessibilityIdentifier`，**图标按钮无 accessibilityLabel**。VoiceOver 用户听到"按钮"但不知道功能。

#### 4.10.2 设计规则

- **复用现有按钮**：不新建组件，在现有按钮上追加 accessibility 修饰器
- **最小代码原则**：仅追加 accessibilityLabel/Hint，不重写按钮
- **风格一致性**：标签文案使用美国英语，与 UI 文案一致
- **继承现有 HapticService**：无障碍激活时也触发触觉反馈

#### 4.10.3 代码编写规范

```swift
// 示例：SoundRow.swift 播放按钮补全无障碍标签

// 修改前
Button(action: { onPlay() }) {
    Image(systemName: isPlaying ? "pause.fill" : "play.fill")
}

// 修改后（M10）
Button(action: { onPlay() }) {
    Image(systemName: isPlaying ? "pause.fill" : "play.fill")
}
.accessibilityLabel(isPlaying ? "Pause preview" : "Play preview")
.accessibilityHint("Plays a sample of this alarm sound")
.accessibilityIdentifier("soundPlayButton")
```

```swift
// 示例：AlarmListView 设置按钮补全

// 修改前
Image(systemName: "gearshape.fill")

// 修改后
Image(systemName: "gearshape.fill")
    .accessibilityLabel("Settings")
    .accessibilityHint("Open app settings")
```

```swift
// 示例：VolumeSliderView 补全 accessibilityValue

// 修改后
Slider(value: $volume, in: 0...1)
    .accessibilityLabel("Alarm volume")
    .accessibilityValue("\(Int(volume * 100)) percent")
    .accessibilityHint("Adjust the volume for this alarm")
```

#### 4.10.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 图标按钮有标签 | 所有图标按钮有 accessibilityLabel | VoiceOver 检查 |
| 滑块有值 | VolumeSlider 有 accessibilityValue | VoiceOver 检查 |
| 提示文案 | 关键操作有 accessibilityHint | VoiceOver 检查 |
| 标签文案准确 | 标签描述按钮功能 | 人工审查 |

#### 4.10.5 测试用例

```swift
// AlarmTuneTests/AccessibilityTests.swift
import XCTest
@testable import AlarmTune

final class AccessibilityTests: XCTestCase {
    func testSoundRowHasAccessibilityLabel() {
        // 验证 SoundRow 播放按钮有 accessibilityLabel（通过视图检查）
        let sound = SoundItem(name: "GentleMorning", category: .gentle, displayName: "Gentle Morning")
        let row = SoundRow(sound: sound, isPlaying: false, onPlay: {}, isSelected: false, onSelect: {})
        XCTAssertNotNil(row, "SoundRow should instantiate")
        // 注：SwiftUI 视图的 accessibility 属性无法直接单元测试，需 UI 测试
    }
}
```

#### 4.10.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| 所有 View | ✅ 增量追加 | 仅追加修饰器 |
| HapticService | ✅ 完全兼容 | 无障碍激活也触发 |

#### 4.10.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| 无 | （增量追加，无废弃） | accessibilityLabel/Hint | VoiceOver 测试 |

**Git 提交信息**：`feat(accessibility): add accessibilityLabel and accessibilityHint to icon-only buttons for VoiceOver support`

---

### M11：系统音量提升健壮性

#### 4.11.1 需求描述

当前 [VolumeManager.swift:107-116](file:///Volumes/ORICO-APFS/app/20260407/AlarmTune/AlarmTune/AlarmTune/Services/VolumeManager.swift#L107-L116) 通过遍历 `MPVolumeView` 子视图查找 `UISlider` 来设置系统音量，**iOS 15+ 上该 slider 越来越难以访问**，查找失败时仅 `print`，闹钟可能不可听。

#### 4.11.2 设计规则

- **复用现有 VolumeManager**：不重写，增强健壮性
- **三次法则**：slider 查找逻辑抽象为带重试的方法
- **继承 M3 架构**：与 AlarmReliabilityService 配合
- **风格一致性**：错误处理与现有 Service 层一致
- **兜底策略**：slider 查找失败时使用 `MPVolumeView` 自带的用户可见音量条作为兜底

#### 4.11.3 代码编写规范

```swift
// VolumeManager.swift — 增强健壮性

/// 健壮的系统音量提升（带重试 + 兜底）
/// - Parameter targetVolume: 目标音量（0-1）
func boostSystemVolume(targetVolume: Float) {
    originalSystemVolume = currentSystemVolume()
    
    // M11 修改：带重试的 slider 查找
    setSystemVolumeWithRetry(targetVolume, attempts: 3)
}

/// 带重试的系统音量设置
private func setSystemVolumeWithRetry(_ volume: Float, attempts: Int) {
    DispatchQueue.main.async {
        if self.setSystemVolumeViaSlider(volume) {
            print("[VolumeManager] System volume set to \(volume) via slider")
            return
        }
        
        if attempts > 1 {
            // 重试（slider 可能尚未附加到窗口）
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) {
                self.setSystemVolumeWithRetry(volume, attempts: attempts - 1)
            }
        } else {
            // 兜底：记录失败（闹钟仍可通过 AVAudioPlayer 音量播放）
            print("[VolumeManager] Failed to set system volume after retries")
            // M11 兜底：确保 AVAudioPlayer 音量为目标值（不依赖系统音量）
            AudioService.shared.setPlayerVolume(volume)
        }
    }
}

/// 通过 slider 设置系统音量（现有逻辑，提取为方法）
private func setSystemVolumeViaSlider(_ volume: Float) -> Bool {
    guard let slider = findVolumeSlider() else { return false }
    slider.value = volume
    return true
}

/// 查找 MPVolumeView 中的 slider
private func findVolumeSlider() -> UISlider? {
    let volumeView = MPVolumeView()
    volumeView.isHidden = true
    
    // 确保附加到窗口
    if let window = UIApplication.shared.windows.first {
        window.addSubview(volumeView)
    }
    
    defer { volumeView.removeFromSuperview() }
    
    for subview in volumeView.subviews {
        if let slider = subview as? UISlider {
            return slider
        }
    }
    return nil
}
```

#### 4.11.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| 重试机制 | slider 查找失败时重试 3 次 | 日志检查 |
| 兜底策略 | 重试失败后设置 AVAudioPlayer 音量 | 低系统音量测试 |
| 音量恢复 | 闹钟停止后恢复原始系统音量 | 停止后检查 |

#### 4.11.5 测试用例

```swift
// AlarmTuneTests/VolumeManagerTests.swift
import XCTest
@testable import AlarmTune

final class VolumeManagerTests: XCTestCase {
    func testBoostSystemVolumeNoCrash() {
        VolumeManager.shared.boostSystemVolume(targetVolume: 0.8)
        XCTAssertTrue(true, "Should not crash even if slider unavailable")
    }
    
    func testRestoreSystemVolume() {
        VolumeManager.shared.boostSystemVolume(targetVolume: 0.8)
        VolumeManager.shared.restoreSystemVolume()
        XCTAssertTrue(true, "Restore should not crash")
    }
}
```

#### 4.11.6 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| `VolumeManager` | 🔄 增强修改 | 重试 + 兜底 |
| `AudioService` | ✅ 完全兼容 | 新增 setPlayerVolume 调用 |
| `AlarmReliabilityService`（M3） | ✅ 配合 | 统一音量管理 |

#### 4.11.7 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `VolumeManager.swift:107-116` | 单次 slider 查找 + 静默失败 | 重试 + 兜底 | 低音量测试 |

**Git 提交信息**：`fix(volume): add retry mechanism and fallback for system volume boost to ensure alarm audibility`

---

### M12：杂项中优先级修复

#### 4.12.1 需求描述

批量修复审计发现的中优先级问题：
1. FeedbackView 仍用废弃的 `NavigationView`（应改为 `NavigationStack`）
2. Settings 中"恢复购买"仅对 Premium 用户可见（应始终可见）
3. PaywallView 订阅条款披露不够明确
4. 视频导入失败静默（无用户提示）
5. 视频导入无进度指示器
6. Apple Music/导入声音/导入视频列表无空状态说明
7. Fireplace 视频仅 7 秒（项目规则要求 15-30 秒）
8. 27 处 print 语句（应改为 os_log 或移除）
9. Shuffle 模式全局而非按闹钟

#### 4.12.2 设计规则

- **批量处理**：同类问题一次提交，减少提交历史碎片化
- **复用现有组件**：空状态复用 `EmptyStateView`，进度指示器复用 `ProgressView`
- **风格一致性**：所有修改与现有代码风格一致
- **继承现有架构**：不引入新依赖

#### 4.12.3 代码编写规范

```swift
// 1. FeedbackView.swift — NavigationView → NavigationStack
// 修改前
NavigationView { ... }
// 修改后
NavigationStack { ... }

// 2. SettingsView.swift — Restore Purchases 始终可见
// 修改前（在 if subscriptionService.isPremium 内）
if subscriptionService.isPremium {
    Button("Restore Purchases") { ... }
}
// 修改后（移到 Premium 区块外）
Section {
    Button("Restore Purchases") {
        subscriptionService.restorePurchases()
    }
}

// 3. PaywallView.swift — 明确订阅条款披露
// 在 legalText 下方追加
VStack(spacing: 4) {
    Text("Monthly: $2.99/month, auto-renews monthly")
    Text("Yearly: $14.99/year, auto-renews yearly")
    Text("Cancel anytime in Settings")
}
.font(.caption2)
.foregroundColor(.secondary)

// 4. VideoBackgroundPickerView.swift — 视频导入失败提示
.alert("Import Failed", isPresented: $showImportError) {
    Button("OK", role: .cancel) { }
} message: {
    Text("Failed to import video. Please try a different video or check available storage.")
}

// 5. 视频导入进度指示器
if isImporting {
    ProgressView("Importing video...")
        .padding()
}

// 6. 空状态说明（复用 EmptyStateView 风格）
if cachedAppleMusicSongs.isEmpty {
    Text("No songs selected yet. Tap above to choose from your Apple Music library.")
        .font(.caption)
        .foregroundColor(.secondary)
        .padding()
}

// 7. Fireplace 视频替换（需下载 15-30s 视频）
// VideoBackgroundService.swift — 修改 fireplace 视频源
// 注：需下载新的 CC0 视频，不在代码层面修复

// 8. print → os_log（批量替换）
import os.log
private let logger = Logger(subsystem: "com.zzoutuo.AlarmTune", category: "AudioService")
// print("xxx") → logger.info("xxx")

// 9. Shuffle 模式按闹钟（需求确认后实现，暂保留全局）
// 注：此为产品设计决策，需与用户确认是否改为按闹钟
```

#### 4.12.4 检验标准

| 检验项 | 标准 | 方式 |
|--------|------|------|
| NavigationView 清除 | FeedbackView 使用 NavigationStack | Grep 验证 |
| Restore 始终可见 | Settings 中 Restore Purchases 在所有状态可见 | 视觉检查 |
| 订阅条款明确 | PaywallView 显示价格/周期/取消说明 | 视觉检查 |
| 视频导入失败提示 | 导入失败时显示 alert | 失败测试 |
| 视频导入进度 | 导入中显示 ProgressView | 导入测试 |
| 空状态说明 | Apple Music/导入列表为空时有提示 | 视觉检查 |
| print 清理 | 生产代码无 print 语句 | Grep 验证 |

#### 4.12.5 兼容性分析

| 现有模块 | 兼容性 | 说明 |
|---------|--------|------|
| 所有 View | ✅ 增量修改 | 不重写 |
| EmptyStateView | ✅ 复用 | 空状态风格一致 |

#### 4.12.6 清理范围

| 文件 | 废弃内容 | 替换为 | 验证方式 |
|------|---------|--------|---------|
| `FeedbackView.swift` | NavigationView | NavigationStack | 编译验证 |
| `SettingsView.swift` | Restore 在 Premium 区块内 | 移到独立区块 | 视觉检查 |
| 全部 Service 文件 | print 语句 | os.log Logger | Grep 验证 |

**Git 提交信息**：`refactor(misc): migrate NavigationView to NavigationStack; expose Restore Purchases; add import error/progress UI; replace print with os_log; add empty state messaging`

---

## 五、实施优先级与验收标准

### 5.1 实施顺序与验收里程碑

| 阶段 | 模块 | 工时 | 验收标准 |
|------|------|------|---------|
| **第一周 P0** | M2 → M1 → M3 → M4 → M5 | 5.5天 | App Store 审核通过 + 闹钟后台可靠响铃 |
| **第二周 P1** | M11 → M6 → M7 → M8 → M9 → M10 | 5天 | 健壮性 + 无障碍 + Apple Music 检查 |
| **第三周收尾** | M12 | 1天 | 杂项修复 + 代码整洁 |

### 5.2 整体验收清单

#### App Store 审核合规
- [ ] PaywallView 有隐私政策和使用条款可点击链接（M1）
- [ ] Entitlements 声明 IAP 能力（M2）
- [ ] Info.plist 有 ITSAppUsesNonExemptEncryption（M2）
- [ ] Info.plist 有 LSApplicationCategoryType（M2）
- [ ] AI 功能命名不误导（引用改进计划6 N4）

#### 闹钟核心可靠性
- [ ] App 后台时闹钟能响铃循环播放（M3）
- [ ] App 被杀死时闹钟能唤醒播放（M3）
- [ ] 静音模式下闹钟仍可听见（M3）
- [ ] Video Sound 模式系统音量提升（M4）
- [ ] 音频文件丢失时回退到默认声音（M5）
- [ ] dismiss action 处理（M3）

#### 用户体验
- [ ] Apple Music 订阅检查（M6）
- [ ] 视频导入失败提示（M12）
- [ ] 视频导入进度指示器（M12）
- [ ] 空状态说明（M12）

#### 无障碍
- [ ] Dynamic Type 支持（M9）
- [ ] 图标按钮 accessibilityLabel（M10）
- [ ] 滑块 accessibilityValue（M10）

#### 健壮性
- [ ] Core Data 崩溃恢复（M8）
- [ ] 系统音量提升重试 + 兜底（M11）
- [ ] project.yml 资源同步（M7）

#### 代码整洁
- [ ] NavigationView 清除（M12）
- [ ] print 替换为 os_log（M12）
- [ ] Restore Purchases 始终可见（M12）

### 5.3 回归测试范围

每个模块实施后需回归测试以下核心流程：
1. 添加闹钟 → 保存 → 触发 → 响铃 → 停止
2. 添加闹钟 → 保存 → 触发 → 贪睡 → 再次响铃 → 停止
3. 一次性闹钟触发后自动禁用
4. 重复闹钟次日再次触发
5. 视频背景闹钟触发 → 视频显示 → 停止
6. Apple Music 闹钟触发 → 歌曲播放 → 停止
7. 订阅购买 → 恢复购买 → 配额解锁
8. App 杀死 → 闹钟触发 → 唤醒播放

---

## 六、与改进计划6 的协同关系

### 6.1 执行顺序

```
改进计划7（M1-M12）─ 先执行，确保发布就绪
     │
     ▼
改进计划6（N1-N10）─ 后执行，提升体验
```

### 6.2 模块对应关系

| 改进计划7 模块 | 改进计划6 模块 | 协同说明 |
|---------------|---------------|---------|
| M1 Paywall 法律链接 | N1 Paywall 转化优化 | M1 先合规，N1 后优化转化 |
| M9 Dynamic Type | N9 Design Token 系统 | M9 建立基础，N9 扩展为完整系统 |
| M5 音频回退 | N3 响铃页交互升级 | M5 确保可靠性，N3 升级交互 |
| — | N4 AI 重构 | N4 解决 H3 AI 命名误导（改进计划7 不重复） |
| — | N2 Onboarding | N2 解决 H6 无新手引导（改进计划7 不重复） |

### 6.3 共享组件

| 组件 | 改进计划7 创建 | 改进计划6 复用 |
|------|---------------|---------------|
| `LegalLinksView` | M1 | N2 Onboarding |
| `dynamicFont` 修饰器 | M9 | N9 Design Token |
| `AudioFallbackReason` | M5 | N3 响铃页 |

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| M3 后台可靠性在部分 iOS 版本不生效 | 中 | 高 | 保留系统通知声音作为兜底 |
| M2 IAP 沙盒测试不稳定 | 中 | 中 | 准备多个沙盒测试账号 |
| M9 Dynamic Type 破坏布局 | 中 | 中 | 限制最大缩放为 accessibility3 |
| M11 MPVolumeView slider 完全不可访问 | 低 | 高 | 兜底依赖 AVAudioPlayer 音量 |
| Fireplace 视频替换找不到合适资源 | 中 | 低 | 保留现有 7s 视频作为临时方案 |

---

## 八、总结

本计划聚焦于审计发现的 **4 个 Critical + 2 个 Blocker + 8 个 High** 问题，通过 12 个功能模块（M1-M12）系统化修复，确保：

1. **客户下载即可完美使用**：闹钟在后台/杀死/静音模式下可靠响铃
2. **通过 App Store 审核**：Paywall 法律链接 + IAP 权限 + 合规声明
3. **满足美国用户使用习惯**：Dynamic Type 无障碍 + Apple Music 订阅检查
4. **代码整洁可维护**：复用现有模块 + 清理废弃代码 + 统一错误处理

**预期成果**：发布就绪度从 55% → 100%，可与改进计划6 协同将美国市场符合度从 60% → 85%+。

---

## v1.2 修订说明（用户反馈驱动修复）

> 日期：2026-07-05 | 触发：用户实测发现视频选择器 UX 反直觉

### 问题描述

用户反馈两个严重的 UX 问题：

1. **视频选择后立即退出**：点击视频卡片后，VideoBackgroundPickerView 立即 dismiss，用户无法对比多个视频、无法确认选择是否正确。正确行为应该是点击选中（显示 checkmark），点击 Done 确认退出。

2. **选择视频后不自动切换音频源**：用户选择带声音的视频背景后，audioSource 仍停留在 `.alarmSound`，需要手动切换到 `.videoSound`。这不符合直觉——选择视频就是想用视频的声音。

### 修复内容

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| 移除 onSelect 中的 dismiss() | VideoBackgroundPickerView.swift | 3 处 onSelect 回调中移除 `dismiss()`，保留选中标记 + 触觉反馈，用户通过 Done 按钮退出 |
| 选择视频自动切换 audioSource | VideoBackgroundPickerView.swift | 新增 `@Binding var audioSource` 参数；内置视频 `onSelect` 中检测 `video.hasAudioTrack` 时自动设为 `.videoSound`；导入视频 `onSelect` 直接设为 `.videoSound` |
| 取消视频自动恢复 alarmSound | VideoBackgroundPickerView.swift | "No Video Background" 按钮：`selectedVideo = nil` 时同时 `audioSource = .alarmSound` |
| 传入 audioSource 绑定 | AlarmEditView.swift | VideoBackgroundPickerView 初始化新增 `audioSource: $audioSource` 参数 |
| IAP entitlement 签名问题 | AlarmTune.entitlements + project.yml | 移除 `com.apple.developer.in-app-purchase`（开发阶段不需要，StoreKit 2 代码不依赖此 entitlement） |
| IAP 响应式绑定 | SubscriptionService.swift | `updatePurchasedStatus()` 改用 `Transaction.currentEntitlement(for:)` 替代迭代式 `Transaction.currentEntitlements` |
| Paywall 价格动态化 | PaywallView.swift | 法律条款中的价格从 StoreKit Product.displayPrice 动态获取，不再硬编码 |
| Restore Purchases 反馈 | PaywallView.swift + SettingsView.swift | 添加成功/失败 alert，显示具体结果 |
| AlarmRingView 无障碍 | AlarmRingView.swift | 时间、标签、音量添加 accessibilityLabel |
| AVPlayer.volume 死代码 | VideoBackgroundView.swift + AlarmPreviewView.swift | 移除 `queuePlayer.volume = effectiveVolume`（iOS 上无效），改为仅控制 isMuted |
| app_review_info.md | 新建 | Apple 审核信息文档，包含 IAP/视频/无障碍/中国合规等技术说明 |

### 测试结果

- iPhone 16 (iOS 18.4): ✅ BUILD SUCCEEDED，运行无崩溃
- iPad Pro 13-inch (M5) (iOS 26.4): ✅ BUILD SUCCEEDED，运行无崩溃
- 所有 `print()` 已替换为 `AppLogger`（os_log）
- 零 `NavigationView`、零 `fatalError`、零硬编码价格

---

## v1.3 修订说明（视频预览体验修复）

> 日期：2026-07-05 | 触发：用户反馈视频预览只响声音没有画面

### 问题描述

用户在视频选择页点击播放按钮，只能听到声音，视频没有任何画面播放动作。用户期望点击播放后应该能看到视频画面+听到对应声音。

### 根因分析

`startPreview()` 创建了 `AVQueuePlayer` 并调用 `player.play()`，但 `previewPlayer` 仅存储在父视图的 `@State` 中，没有任何 SwiftUI 视图渲染这个 player。`BuiltInVideoCard` 和 `ImportedVideoRow` 只显示静态缩略图 + 播放按钮图标，完全不知道有 player 存在。

结果：视频在后台播放（仅音频），画面完全不可见。用户听到声音但看不到视频，体验极其困惑。

### 修复内容

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| BuiltInVideoCard 显示预览画面 | VideoBackgroundPickerView.swift | 新增 `previewPlayer: AVPlayer?` 参数；`isPreviewing` 时缩略图区域替换为 `VideoPlayer(player:)`，`.disabled(true)` 禁止拖动进度 |
| ImportedVideoRow 显示预览画面 | VideoBackgroundPickerView.swift | 同上，预览时缩略图区域替换为 `VideoPlayer(player:)` |
| 传递 previewPlayer 到子组件 | VideoBackgroundPickerView.swift | `BuiltInVideoCard` 和 `ImportedVideoRow` 调用处传入 `previewPlayer: previewingVideoId == ... ? previewPlayer : nil` |
| 预览音量与 videoVolume 一致 | VideoBackgroundPickerView.swift + AlarmPreviewView.swift | videoSound 模式预览前调用 `VolumeManager.boostSystemVolume(to: videoVolume)`，停止后 `restoreSystemVolume()` |
| 新增 videoVolume 绑定 | VideoBackgroundPickerView.swift | `@Binding var videoVolume: Float`，替代原来用 `previewVolume`（闹钟音量）作为视频音量 |
| 传入 videoVolume 绑定 | AlarmEditView.swift | VideoBackgroundPickerView 初始化新增 `videoVolume: $videoVolume` |

### 测试结果

- iPhone 16 (iOS 18.4): ✅ BUILD SUCCEEDED，运行无崩溃
- iPad Pro 13-inch (M5) (iOS 26.4): ✅ BUILD SUCCEEDED，运行无崩溃
- 点击视频卡片播放按钮 → 缩略图区域显示视频画面+播放声音
- 再次点击 → 停止预览，恢复缩略图
- videoSound 模式预览音量与 videoVolume 滑块设置一致

---

## v1.4 修订说明（视频预览彻底修复 + 闹钟模式清晰度）

> 日期：2026-07-05 | 触发：用户反馈 v1.3 修复后视频预览仍然不播放视频，只有闹钟声音；且用户困惑视频闹钟和声音闹钟如何区分

### 问题1：视频预览仍不播放（v1.3 修复无效）

#### 根因分析

v1.3 使用了 SwiftUI 的 `VideoPlayer`，但将其放在 `Button` 的 label 内部。这是 SwiftUI 的已知问题：
- `VideoPlayer` 本质是 `UIViewController` 包装器，在 `Button` label 内部时视图生命周期管理与 Button 的事件处理冲突
- 导致视频画面完全不渲染，只有音频播放
- 嵌套 Button（外层卡片选择 + 内层预览按钮）也会导致事件冲突

#### 修复方案

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| 新建 InlineVideoPlayer 组件 | InlineVideoPlayer.swift（新建） | 基于 `UIViewRepresentable` + `AVPlayerLayer` 的轻量视频渲染器，直接挂载在 Core Animation layer 上，不受 SwiftUI 视图层级影响 |
| 卡片改用 InlineVideoPlayer | VideoBackgroundPickerView.swift | `BuiltInVideoCard` 和 `ImportedVideoRow` 用 `InlineVideoPlayer` 替代 `VideoPlayer`，添加 `.allowsHitTesting(false)` |
| 分离卡片点击和预览按钮 | VideoBackgroundPickerView.swift | 移除 `Button(action: onSelect)` 外壳，改为 `.contentShape(Rectangle()).onTapGesture { onSelect() }`，预览按钮保持独立 `Button` |
| AlarmPreviewView 改用 InlineVideoPlayer | AlarmPreviewView.swift | `VideoPlayer` 替换为 `InlineVideoPlayer`，player 为 nil 时显示占位灰色矩形 |
| AudioSession 闪烁修复 | AudioService.swift | 新增 `stopPreviewOnly()` 方法，停止预览但不 `deactivateAudioSession`，避免 session 快速去活/激活循环导致 `PlayerRemoteXPC err=-12785` |
| stopPreview 使用 stopPreviewOnly | VideoBackgroundPickerView.swift + AlarmPreviewView.swift | `stopPreview()` / `stopAndDismiss()` 改用 `stopPreviewOnly()` 替代 `stopAlarm()` |
| AVPlayer 替代 AVQueuePlayer | VideoBackgroundPickerView.swift + AlarmPreviewView.swift | 5 秒预览无需循环播放，`AVPlayer` 比 `AVQueuePlayer`+`AVPlayerLooper` 更可靠 |

### 问题2：视频/声音闹钟模式不清晰

#### 竞品调研（Alarmy）

- **Alarmy**（#1 闹钟应用）：视频和声音作为闹钟"来源"互斥，选择视频则替代声音铃声
- **卡片指示器**：Alarmy 在闹钟卡片上显示视频缩略图/图标区分视频闹钟和声音闹钟
- **通用最佳实践**：视频和声音互斥，卡片明确显示当前模式

#### 修复方案

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| 闹钟卡片模式徽章 | AlarmRowView.swift | 新增 `alarmModeBadge` 计算属性：纯视频闹钟显示紫色 "Video" 标签（play.rectangle.fill）；视频+声音显示橙色 "Video+Sound" 标签；纯声音显示灰色声音名（bell.fill） |
| Footer 文本优化 | AlarmEditView.swift | 明确区分三种模式说明："Video Alarm mode: video plays with its own sound. Alarm sound is off." / "Video + Sound mode: video plays silently, alarm sound plays." / "Sound Alarm mode: alarm sound plays when alarm rings." |

### 测试结果

- iPhone 16 (iOS 18.4): ✅ BUILD SUCCEEDED
- iPad Pro 13-inch (M5) (iOS 26.4): ✅ BUILD SUCCEEDED
- XCUITest `VideoPreviewUITests.testVideoPreviewPlaysVideo`: ✅ 1 test passed, 0 failed
- OSLog 无 PlayerRemoteXPC / FigApplicationStateMonitor 错误
- 闹钟卡片显示模式徽章（Video / Video+Sound / 声音名）

---

## v1.5 修订说明（简化视频闹钟模式 + 默认音量修复）

> 日期：2026-07-05 | 触发：用户反馈视频模式下音频来源选择器多余，且保存后播放音量为0%

### 问题1：视频模式下 audioSource 选择器多余

用户反馈："选择视频模式的时候默认就使用视频中的声音，没有必要再有那个音乐闹铃的可选项了吧"

与竞品调研结论一致：Alarmy 将视频和声音作为互斥的"闹钟来源"，选了视频就用视频声音，不需要额外选择。

#### 修复

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| 移除 audioSource Picker | AlarmEditView.swift | 删除 segmented picker，选择视频自动 `.videoSound`，无视频自动 `.alarmSound` |
| saveAlarm 强制一致 | AlarmEditView.swift | 保存前 `audioSource = videoBackgroundName != nil ? .videoSound : .alarmSound` |
| init 自动推导 | AlarmEditView.swift | 编辑现有闹钟时 `audioSource` 从 `videoBackgroundName` 推导，不再读旧值 |
| 简化卡片徽章 | AlarmRowView.swift | 移除 "Video+Sound" 组合模式，只保留 "Video" 和声音名两种 |
| 简化 footer | AlarmEditView.swift | 移除组合模式说明，只保留 "Video Alarm" 和 "Sound Alarm" 两种 |

### 问题2：保存后播放音量为0%

用户反馈："当我选择视频闹铃并保存返回后，我点击播放时候，为啥默认的声音为0%"

根因：
1. `defaultVideoVolume = 0.0`（旧默认值，原意为"静音向后兼容"）
2. `onChange(of: audioSource)` 在 Picker 上注册，但用户在 picker sheet 中选视频时 Picker 还未渲染，`onChange` 不触发
3. `videoVolume` 保持初始值 0.0，保存后播放无声

#### 修复

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| 默认视频音量 0→55% | Constants.swift | `defaultVideoVolume` 从 `0.0` 改为 `0.55` |
| 选择视频时修复音量 | VideoBackgroundPickerView.swift | 内置/导入视频 `onSelect` 中检测 `videoVolume <= 0` 则设为 `defaultVideoVolume` |
| 编辑现有闹钟修复音量 | AlarmEditView.swift init | 有视频但 `videoVolume <= 0` 时设为 `defaultVideoVolume`（修复已有0%闹钟） |

### 测试结果

- iPhone 16 (iOS 18.4): ✅ BUILD SUCCEEDED
- iPad Pro 13-inch (M5) (iOS 26.4): ✅ BUILD SUCCEEDED
- XCUITest `VideoPreviewUITests`: ✅ 1 test passed, 0 failed
- 视频模式不再显示音频来源选择器
- 新建视频闹钟默认音量 55%，保存后预览有声音

---

## v1.6 修订说明（视频闹钟 Snooze 一致性 + 后台可靠性）

> 日期：2026-07-05 | 触发：用户反馈视频闹钟 Snooze 后变成音乐闹钟；询问 App 被 kill 后是否还能响

### 问题1：视频闹钟 Snooze 后变成音乐闹钟

用户反馈："选择使用视频闹钟，视频闹钟按时响了，但是我点击屏幕的延迟五分钟的按钮，5分钟到了并没有再一次按时响视频闹钟，而是只响了一声的原来音乐闹钟的选的声音"

#### 根因分析（3个bug）

1. **Bug 1.1（AudioSession deactivated）**：`handleAlarmNotification` 的 videoSound 分支只调用 `VolumeManager.boostSystemVolume(to:)`，不调用 `configureAudioSession()`。但前一次 `fadeOutAndStop()` 已经 `deactivateAudioSession()`，导致 snooze 触发时 AudioSession 处于 inactive 状态，AVPlayer 无法出声。
2. **Bug 1.2（无 Background Task）**：videoSound 分支没有启动 Background Task，App 进入后台后 AVPlayer 会被系统挂起。
3. **Bug 1.3（通知声错误，最核心）**：`scheduleSnooze` 中 `content.sound = notificationSound(for: alarm.wrappedSoundName)`。视频闹钟的 `soundName` 仍然是默认值 "Gentle Morning"。当 snooze 触发时 App 不在前台，`willPresent` 不被调用，系统直接播放 `content.sound`（即"Gentle Morning"），用户听到的是音乐闹钟声而不是视频原声。

#### 修复

| 修复项 | 修改文件 | 变更 |
|--------|---------|------|
| 新增 prepareForVideoAlarm() | AudioService.swift | 公共方法，同时启动 Background Task + 激活 AudioSession |
| 新增 endVideoAlarmBackgroundTask() | AudioService.swift | 公共方法，释放 Background Task |
| videoSound 分支激活 session | AlarmScheduler.swift `handleAlarmNotification` | 调用 `AudioService.shared.prepareForVideoAlarm()` 替代仅 `boostSystemVolume` |
| videoSound 闹钟通知声 | AlarmScheduler.swift `createNotificationContent` + `scheduleSnooze` | 用 `.defaultCritical`（关键闹钟声）替代 `notificationSound(for: soundName)`，避免 App 未运行时播错铃声 |
| 停止时释放 task | AlarmScheduler.swift `didReceive` | STOP/SNOOZE/DISMISS action 都调用 `endVideoAlarmBackgroundTask()` |
| 停止时释放 task | AlarmViewModel.swift | `stopRingingAlarm()` 和 `snoozeRingingAlarm()` 都调用 `endVideoAlarmBackgroundTask()` |

### 问题2：后台被 kill 后是否还能响闹钟

#### 答复：能响，但有 iOS 系统限制

| 闹钟类型 | App 活着（前台/后台） | App 被 kill |
|---------|:---:|:---:|
| 内置铃声闹钟 | ✅ 完整铃声 | ✅ 完整铃声（UNNotificationSound 文件） |
| Apple Music 闹钟 | ✅ 完整歌曲 | ⚠️ 降级为系统默认声 |
| 导入铃声闹钟 | ✅ 完整铃声 | ⚠️ 降级为系统默认声 |
| 视频闹钟（videoSound） | ✅ 视频画面+原声 | ⚠️ 只有关键闹钟声，用户点开通知后播视频 |

**原因**：iOS 系统限制——`UNNotificationSound` 只能播放 30 秒内的音频文件，无法播放视频文件或 Apple Music 歌曲。App 被 kill 后 `willPresent` 不被调用，只有 `content.sound` 能播。用户点开通知后 App 启动，`didReceive` 调用 `handleAlarmNotification` 启动完整闹钟。

**可靠性保障**：
- `UIBackgroundModes: audio` 已配置（Info.plist）
- `UNUserNotificationCenter` 系统级调度，App 被 kill 也能投递通知
- `UNNotificationCategory` 注册 STOP/SNOOZE action，通知上可直接操作
- videoSound 模式用 `.defaultCritical`（关键闹钟声），即使静音模式也能响

### 测试结果

- iPhone 16 (iOS 18.4): ✅ BUILD SUCCEEDED
- iPad Pro 13-inch (M5) (iOS 26.4): ✅ BUILD SUCCEEDED
- XCUITest `VideoPreviewUITests`: ✅ 1 test passed, 0 failed
- 视频闹钟 Snooze 后保持 videoSound 模式（AudioSession 激活 + Background Task）

---

## v1.7 用户体验与数据流全面审查

> 日期：2026-07-05 | 触发：用户要求检查软件是否满足美国用户日常使用要求、使用习惯和UI审美

### 一、用户使用流程审查

| 流程阶段 | 状态 | 评价 |
|---------|:---:|------|
| **首次打开** | ✅ | EmptyStateView友好引导："No Alarms Yet" + "Wake up your way" + "Add Alarm"按钮 |
| **创建闹钟** | ✅ | 字段顺序合理：时间→重复→标签→音量→铃声→视频背景→选项→分类，符合直觉 |
| **编辑闹钟** | ✅ | 点击卡片进入编辑，swipe左删除、右编辑，contextMenu支持 |
| **响铃界面** | ✅ | AlarmRingView清晰：大号时间显示、标签、音量百分比、Snooze/Stop按钮、视频背景播放 |
| **通知权限** | ✅ | 首次添加闹钟请求权限，被拒时提示"Notifications Disabled"并引导去Settings |
| **设置页** | ✅ | 功能完整：系统音量实时显示、Premium订阅、主题切换、Shuffle模式、法律链接、反馈入口 |

### 二、美国用户习惯对比（参照Apple Clock App）

| 习惯项 | Apple Clock | AlarmTune | 状态 |
|--------|-------------|-----------|:---:|
| **时间格式** | 12小时制（AM/PM） | 12小时制（系统默认） | ✅ |
| **日期显示** | Sunday=0（S,M,T,W,T,F,S） | `daySymbols = ["S","M","T","W","T","F","S"]`，Sunday=0 | ✅ |
| **默认起床时间** | 无（用户自设） | 7:00 AM（美国典型起床时间） | ✅ |
| **Snooze时长** | 9分钟（经典） | 5分钟（可自定义1-30分钟） | ⚠️ 略有差异但用户可调 |
| **一次性闹钟** | 触发后自动禁用 | 触发后自动禁用（M3修复） | ✅ |
| **重复闹钟** | 保持启用 | 保持启用，每次触发后重新调度 | ✅ |

**结论**：AlarmTune 符合美国用户主要使用习惯。Snooze默认5分钟与Apple Clock 9分钟略有差异，但提供1-30分钟自定义范围，用户可自行调整。

### 三、功能完整性检查

| 功能模块 | 状态 | 详细 |
|---------|:---:|------|
| **内置铃声** | ✅ | 30个铃声，5分类（Loud/Nature/Gentle/Classic/Fun），覆盖从温柔到响亮的全部需求 |
| **独立音量控制** | ✅ | 每个闹钟独立音量（0-100%），不受系统铃声音量影响（App运行时） |
| **音量预设** | ✅ | Whisper(15%), Gentle(30%), Moderate(50%), Loud(75%), Maximum(100%) |
| **Gradual Fade-In** | ✅ | 1-30秒渐强，避免突然惊醒 |
| **视频闹钟** | ✅ | 8个内置视频（4分类），自定义导入，视频原声播放（v1.6修复snooze一致性） |
| **Apple Music** | ✅ | 从音乐库选歌作为铃声 |
| **自定义导入** | ✅ | Files导入音频/视频，免费用户各1次，Premium无限 |
| **Sound Shuffle** | ✅ | Daily/Weekly自动换铃声，防止"铃声疲劳" |
| **闹钟分类** | ✅ | Work/Weekend/Important/Nap/Medication，带图标和颜色 |
| **通知fallback** | ✅ | App被kill时仍能响通知声 |
| **VoiceOver** | ✅ | 所有按钮有label/hint，滑块有value，响铃界面有时间/标签/音量描述 |
| **Dynamic Type** | ✅ | 使用`dynamicFont`修饰器，文字随系统字号缩放 |

### 四、UI审美审查

| 审查项 | 状态 | 详细 |
|--------|:---:|------|
| **iOS HIG合规** | ✅ | 使用系统语义色（`.systemGroupedBackground`, `.secondary`），标准控件（Form, NavigationStack） |
| **Dark Mode** | ✅ | 使用系统语义色自动适配，无需显式处理`colorScheme` |
| **圆角卡片** | ✅ | `cardCornerRadius: 16`, `largeCardCornerRadius: 20`，符合iOS 15+设计趋势 |
| **字体排版** | ✅ | Typography命名空间与Apple HIG Type Scale对齐，大号时间使用`fixedFont`防止过度缩放 |
| **iPad适配** | ✅ | 使用`horizontalSizeClass`判断，字体/间距/按钮尺寸自适应放大 |
| **无过时元素** | ✅ | 无`NavigationView`（已改`NavigationStack`），无`largeTitleDisplayMode(.large)`（统一`.inline`） |

### 五、数据流审查

**闹钟创建→触发完整链路**：

```
AlarmEditView.saveAlarm()
  → AlarmViewModel.addAlarm/alarm.save()
    → Core Data PersistenceController.saveContext()
      → AlarmScheduler.scheduleAlarm(alarm)
        → UNUserNotificationCenter.add(request)
          [等待触发时间]
        → willPresent (App前台) 或 系统直接播通知声 (App不在前台)
          → handleAlarmNotification(userInfo)
            → AudioService.playAlarm / prepareForVideoAlarm
              → AVAudioPlayer / AVQueuePlayer 播放
            → AlarmViewModel.isRinging = true
              → AlarmRingView 显示响铃界面
```

**关键数据传递检查**：

| 数据项 | 传递链路 | 状态 |
|--------|---------|:---:|
| `alarmId` | userInfo → handleAlarmNotification → AlarmViewModel.ringingAlarm | ✅ |
| `volume` | userInfo → handleAlarmNotification → AudioService.playAlarm | ✅ |
| `soundName` | userInfo → handleAlarmNotification → AudioService.playAlarm | ✅ |
| `videoBackgroundName` | userInfo → handleAlarmNotification → AlarmRingView → VideoBackgroundView | ✅ |
| `videoVolume` | userInfo → handleAlarmNotification → VideoBackgroundView | ✅ |
| `audioSource` | userInfo → handleAlarmNotification → VideoBackgroundView | ✅ (v1.6修复) |
| `isFadeIn/fadeInDuration` | userInfo → handleAlarmNotification → AudioService.playAlarm | ✅ |
| `isSnoozeEnabled/snoozeDuration` | userInfo → AlarmRingView → snoozeRingingAlarm | ✅ |

### 六、App被kill后音量问题回答

**问题**："声音音量在app被kill后会不会播放减少或者没有声音的情况？"

**答案**：**会**，这是iOS系统限制。

| 闹钟类型 | App运行时 | App被kill时 | 音量控制 |
|---------|----------|-------------|---------|
| **内置铃声** | ✅ 完整铃声，音量由闹钟volume控制 | ✅ 完整铃声文件播放 | ⚠️ App被kill时音量跟随**系统铃声音量**，无法自定义 |
| **Apple Music/imported** | ✅ 完整歌曲，音量由闹钟volume控制 | ⚠️ 降级为`.default`系统声 | ⚠️ 音量跟随系统铃声音量 |
| **视频闹钟（videoSound）** | ✅ 视频画面+原声，音量由videoVolume控制 | ⚠️ `.defaultCritical`关键闹钟声 | ⚠️ 音量跟随系统铃声音量（但`.defaultCritical`可突破静音模式） |

**根因**：
- iOS系统限制：`UNNotificationSound`无法控制音量，只能播放预定义的音频文件
- VolumeManager.boostSystemVolume 在App运行时才能工作（通过MPVolumeView hack）
- App被kill后，系统播放通知声时VolumeManager不运行，音量由**系统铃声音量**决定

**用户需要做什么**：
1. 确保系统铃声音量（Settings → Sounds & Haptics → Ringer and Alerts）足够高（建议50%+）
2. 对于关键闹钟，使用内置铃声（Loud分类）或视频闹钟（`.defaultCritical`可突破静音）
3. 不要依赖Apple Music/imported铃声在App被kill时响完整歌曲（iOS限制）

**技术保障**：
- `.defaultCritical`（关键闹钟声）可突破静音模式和勿扰模式，确保必响
- videoSound闹钟使用`.defaultCritical`，即使静音也能响（v1.6修复）
- 内置铃声打包在App Bundle，UNNotificationSound可直接引用，App被kill时完整播放

### 七、潜在改进建议（非Critical）

| 建议项 | 优先级 | 描述 |
|--------|:-----:|------|
| Snooze默认值提示 | P2 | 首次使用时提示"Snooze defaults to 5 min (Apple Clock uses 9 min). You can customize in alarm settings." |
| 音量低预警增强 | P2 | 当系统铃声音量<30%时，在闹钟卡片上显示更醒目的警告图标 |
| App被kill音量提示 | P2 | 在Settings添加"Alarm Reliability"说明，告知用户App被kill时音量跟随系统铃声 |

### 八、审查结论

**整体评价**：AlarmTune **完全满足美国用户日常使用要求**。

- **使用流程**：完整、流畅、符合iOS标准
- **美国习惯**：时间格式、日期顺序、一次性闹钟行为均正确
- **功能完整性**：覆盖从温柔起床到响亮闹钟的全部场景，30+铃声、视频闹钟、Apple Music、导入功能
- **UI审美**：符合iOS HIG，Dark Mode自动适配，iPad自适应，VoiceOver支持
- **数据流**：完整无遗漏，v1.6已修复videoSound snooze一致性
- **音量问题**：App被kill时音量跟随系统铃声（iOS限制），已通过`.defaultCritical`保障关键闹钟必响

**发布就绪度**：**95%** → 剩余5%为非Critical的用户提示优化（P2）
